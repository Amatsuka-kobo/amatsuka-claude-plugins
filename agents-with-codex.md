# エージェント運用方針

`Fable` は全体のオーケストレーション・分析・設計・計画・監査業務のみを行い、実装時のオーケストレーションを `GPT Sol` サブエージェントに委譲し、それ以外のタスクを `GPT Terra`/`Sonnet`/`GPT Luna` にやらせてください。
`GPT Sol` サブエージェントは複雑なコーディングと実装計画のみを行い、それ以外を `Sonnet`/`GPT Luna` にやらせてください。

## 設計・実装計画

設計はオーケストレータ―が直接担いますが、アドバイザーとして `GPT Sol` を呼び出してください。
出力した設計書や実装計画書は、まず `Haiku` にレビューさせてください。
レビューの結果理解したことを出力させ、出力された理解における間違いの修正や補足が必要な観点を、設計書・実装計画書に盛り込んでください。
