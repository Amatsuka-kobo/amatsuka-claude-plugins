- `Fable`/`Opus` はオーケストレーション・分析・設計・計画・監査業務・タスクアドバイス(後述・アドバイザー節)のみを行うこと
- レビューは `Sonnet` にやらせ、重いレビュー(最終レビューなど)はオーケストレーター自身が行うこと
- 複雑な実装は `GPT Sol` にやらせること
- 通常の実装とそれ以外のタスクは `GPT Terra` にやらせること
- 軽量のタスクは `GPT Luna` にやらせること
- `Fable` は設計書を元にした実装計画を立てる業務を `Opus` に委譲すること
- オーケストレータ―は、複数のタスクが同時に実行できる状況(あるタスクがのタスクに依存しない状況)である場合、可能な限り複数のサブエージェントを行使すること

### 設計・実装計画

- 出力した設計書や実装計画書は、まず `Haiku` にレビューさせること
- レビューの結果理解したことを出力させ、出力された理解における間違いの修正や補足が必要な観点を、設計書・実装計画書に盛り込むこと

### アドバイザー

- オーケストレータ―は、サブエージェントがアドバイザーに相談できるよう、Agent Tool を許可するとともに、サブエージェント自身に自分がサブエージェントであることを認識させる指示を出すこと
- サブエージェントは、自身で起動したサブエージェントに対して、Agent Tool を許可してはならない
- サブエージェントは、計画・実装・レビューを行うとき、迷ったときだけアドバイザーとして `Fable` サブエージェントを呼び出し、助言を求めること

### Claudeの設定

このエージェント運用方針は恒久的なものなので、Claude の設定を更新してください。
