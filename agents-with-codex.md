# エージェント運用方針

コストの最適化と品質の向上を目的としたサブエージェントの運用方針です。
オーケストレータ―は、実装時のオーケストレーションを `GPT Sol` サブエージェントに委譲してください。

## レビュー・調査・分析

複雑なレビュー・調査・分析などはオーケストレータ―が直接担い、通常は `GPT Sol` に、軽いものは `Sonnet` にやらせてください。

## 設計・実装計画

設計・実装計画はオーケストレータ―が直接担いますが、アドバイザーとして `GPT Sol` を呼び出してください。
出力した設計書や実装計画書は、まず `Haiku` にレビューさせてください。
レビューの結果理解したことを出力させ、出力された理解における間違いの修正や補足が必要な観点を、設計書・実装計画書に盛り込んでください。

## コーディング

複雑なコーディングは `GPT Sol` に、通常は `Sonnet` に、軽いものは `GPT Luna` にやらせてください。

## その他の作業

通常は `GPT Terra` に、軽量かつ大量の作業は `GPT Luna` に、軽量かつ単発の作業は `Haiku` にやらせてください。
