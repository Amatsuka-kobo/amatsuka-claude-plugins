# agent-policy RoleId 併記案の設計・レビュー・実装

- 日付: 2026-09-16
- 参加者: phyllis998, AI (Claude Haiku 4.5)
- 成果物: agent-policy v0.19.1-dev へ実装。`src/hooks/marker-scan.ts` (1 行)、テスト 2 本修正・1 本新設、`assets/roles/{ja,en}/_common.md` (4 箇所)、`plugins/agent-policy/references/orchestration-discipline.md` (3 箇所)、`README.md` (2 箇所)
- 前提: 2026-09-16 agent-policy 引継ぎ資料、`plugins/agent-policy/references/orchestration-discipline.md`、システムプランナー作成 context-map
- セッション ID: 0e23db01-238e-417e-9359-d7a3a1b4e948

---

## セッション 1: agent-policy の RoleId 併記実装と動作検証

# AI

ゴール了解。A・B・C 実装、D は設計→レビュー→承認→実装。着手する。

Baseline 再現: lint パス(info 4)、typecheck 0、test 2254 passed / 2 skipped。事実確認のため対象箇所を直読する。

A・B は引き継ぎ書どおり。3 件を並列ディスパッチする。

A・B・C-1 が走っている間に、D の設計に要る現状把握を進める。探索統括へ context-map を作らせる。

**baseline 再現**: lint パス(info 4・error 0)、typecheck 0、test 2254 passed / 2 skipped。引き継ぎ書の「test 2254 件」と一致。

**作業ツリーの無関係変更**: `.raphael/antibodies/ab-2026-0909-002.md`(M)、`docs/chat/INDEX.md`(M)、`docs/chat/2026/0916/phyllis998/1127-doc-writing-role-spec.md`(??)。全ディスパッチ先に「触るな」を明記済み。

**着手した 4 件**:

| 記号 | 内容 | 委譲先(役割) | 状態 |
| --- | --- | --- | --- |
| A | `ComposeInput.modelId` / `Target.composeModelId` の死んだ配管除去 | 通常の実装 | 実行中 |
| B | `subagent-start.test.ts` の `ROLE_IDS` を `ROLES` から導出 | 軽量な実装 | 実行中 |
| C-1 | CLIProxyAPI の antigravity ログインコマンドの一次情報調査 | リアルタイム情報調査 | 実行中 |
| D-1 | 対応表と en 生成定義の現状を context-map に洗い出し | コードベース探索統括 | 実行中 |

A と B の事実は直読で裏取り済み — `composeModelId` の参照は `setup-agents.ts` の 4 箇所のみで compose 側に読み手なし、`ROLE_IDS` は 14 件で `design-plan` / `explore-lead` / `doc-writing` を欠く。引き継ぎ書と食い違いなし。

C は調査結果を待ってから手順書執筆(文書作成役)へ渡す。D は context-map → 設計書(設計書作成役)→ 2 系統レビュー → ユーザー承認の順で運ぶ。案 (a)/(b)/(c) の選択はこのセッションでは決めない。

`★ Insight ─────────────────────────────────────`
- A の「死んだ配管」が型検査で捕まらないのは、`ComposeInput.modelId` が **optional かつ読み手ゼロ**だから。TypeScript は「書かれるが読まれないプロパティ」を検出しない。`noUnusedLocals` はローカル変数専用で、インターフェイスのフィールドは対象外。こういう配管は関数シグネチャから引数を消した後に取り残されやすく、grep で「代入 4 箇所・参照 0 箇所」を数えるのが確実な検出法になる。
- B の fixture 陳腐化は、テストが**件数ではなくサイズ上限を検査している**ために表面化しなかった。14 件でも 17 件でも上限は超えるので緑のまま。正本(`ROLES`)から導出させると、役割追加時にテストが自動追随する — fixture を定数で書くか導出で書くかは「何が壊れたときに気づきたいか」で決まる。
`─────────────────────────────────────────────────`

4 件の完了通知を待つ。

B 完了。1 点、B の報告に検証が要る — baseline lint の exit code を取り違えていた可能性がある(tail へパイプしたため tail の code を見ていた)。

**B 完了。** `ROLE_IDS` を `ROLES.map((role) => role.id)` へ導出化。`ROLES` の import 元は `../../agents/roles`。

- 切り詰め検査の閾値は `fullTable.length > 9500` / `context.length <= 9500` という**文字数上限**であり役割件数に依存しない → 17 × 8 = 136 定義でも検査意図のまま通過。閾値の追随は不要だった。
- 対象ファイル単体: Tests 35 passed。biome も clean。

B の報告のうち 1 点は誤り。「`plugins/metatron/.../section-reference-inventory.test.ts` の lint 失敗」は失敗ではない — 実測で `biome check` の exit は **0**、`Found 4 infos.` は baseline から存在する info であり error ではない。B が info を失敗と読んだもの。`compose.test.ts` 側の差分は A が編集中の一時状態。

`★ Insight ─────────────────────────────────────`
- biome の `info` 診断は exit code を汚さない。`pnpm run lint | tail` のように**パイプすると `$?` はパイプ末尾のコマンドのもの**になり、lint の成否を取り違える。今回の baseline 判定もこれを踏んでいたので取り直した。サブエージェントに検証を任せると、この取り違えがそのまま報告に乗る — 報告の突き合わせが要る所以。
`─────────────────────────────────────────────────`

A(実装)・C-1(調査)・D-1(context-map)の完了を待つ。

**B の突き合わせ完了。** 差分は import 1 行 + 配列 15 行 → 1 行の置換のみ。`ROLES` の実測は 17 件で、欠けていた `design-plan` / `doc-writing` / `explore-lead` が導出後に含まれる。余計な変更なし。

A・C-1・D-1 を待つ。

A 完了。差分を突き合わせる。

C の調査完了。手順書へ渡す前に、alias 名の食い違いを実物で確認する。

alias 名は実物と食い違っていた — 引き継ぎ書の `claude-gemini-3-1-pro` は誤りで、実体は **`claude-gemini-3.1-pro-high`**(上流 `gemini-3.1-pro-high`)。調査側の指摘が正しい。C の内容を確定して執筆へ渡す。

D-1 の context-map 完成。読んで突き合わせる。

context-map を読了。引き継ぎ書の 2 点が実際とずれていた(README の限界記述は `:152`、`:158` は別話題 / 混入禁止検査は作業ツリーでは `:595`)。出力先はプラグインのガイドが `.claude/context-maps/` を規定し、リポジトリの `.gitignore` もそれを除外対象にしている → ガイドに合わせて移す。

統括の「既存 context-map は 1 本も無い」は誤り — `.claude/context-maps/` に 13 本あった(gitignore 対象のため git 経由では見えず)。移設先として正しい。D の設計書をディスパッチする。

**現在の進捗**

| 記号 | 内容 | 状態 |
| --- | --- | --- |
| A | 死んだ `modelId` 配管の除去 | 実装完了・差分検証済み |
| B | fixture の `ROLES` 導出化 | 実装完了・差分検証済み |
| — | バージョン `0.19.1-dev` 化 + `pnpm run build` | 実行中(`scripts/setup-agents.mjs` に差分が出ている) |
| C | `cliproxyapi-setup.md` の Gemini 追随 | 執筆中(9 箇所を確定して委譲) |
| D-1 | context-map | 完了・`.claude/context-maps/` へ移設 |
| D-2 | 3 案比較の設計書 | 執筆中 |

**引き継ぎ書の記述で実際とずれていた 3 点**(設計書には訂正済みの値を渡した):

1. config の alias は `claude-gemini-3-1-pro` ではなく **`claude-gemini-3.1-pro-high`**(上流 `gemini-3.1-pro-high`)。ドットと `-high` が付く。
2. README の既知の限界は `:152`。引き継ぎ書が挙げる `:158` は「断片の節見出しと生成言語の一致」という別の話題。
3. 日本語混入禁止の検査は HEAD で `:616`、A の編集後の作業ツリーでは `:595`。

**D の設計判断に効く事実**(context-map より):

- 対応表を照合する条項は `_common.md` の **2 箇所だけ**(`:14` アドバイザー、`:21` 文書作成)。影響範囲は思ったより狭い。
- 混入禁止の正規表現は `/[぀-ゟ゠-ヿ一-龯、。「」]/` で、**RoleId は全て ASCII なので引っかからない** → 案 (a) は既存契約と衝突しない。
- 担当表(`orchestration-discipline.md`)は**既に RoleId 列を持っている** → 案 (a) は対応表を担当表の既存様式に寄せる形になる。
- 案 (b) が要る情報は env・設定ファイル・frontmatter・`roles.ts` の英語ラベル・`hooks.json` のいずれにも無く、SessionStart は `agent_type` すら受け取らない。

`★ Insight ─────────────────────────────────────`
- 対応表の照合が壊れていても**テストは全部緑**だった。理由は、`subagent-start.test.ts` が `markerTable()` の出力そのものを期待値にしているから — 生成側と検査側が同じ関数を呼ぶと、形式を変えても常に一致する。これは「実装の自己一致」を検査しているだけで、契約は検査していない。契約を縛っているのは `marker-scan.test.ts:452-459` の**リテラル完全一致**のほうで、案 (a) で落ちるのもそちらだけ。テストが落ちるかどうかは、そのテストが何を期待値にしているかで決まる。
- この欠陥の本体は「対応表(日本語固定)」と「生成定義(言語可変)」の**結合キーが表示ラベルになっていること**。表示のための文字列を同一性の判定にも使うと、表示を翻訳した瞬間に同一性が壊れる。RoleId という不変キーを表に載せる案 (a) は、この結合を表示から切り離す修正にあたる。
`─────────────────────────────────────────────────`

ビルド検証・C の執筆・D の設計書を待つ。

ビルド・バージョン更新ともに green(test 2254 passed、lint/typecheck exit 0)。A と B をコミットする。先にバンドル差分の中身を確認する。

バンドル差分は A の 3 行削除のみでバージョンは埋め込まれていない。A と B を分けてコミットする。

C-2 が 429 で落ちた(`claude-gemini-3-8-flash` の quota)。引き継ぎ書が予告していた失敗モード。担当表の Claude モデル(Sonnet)へ読み替えて再ディスパッチする。

A と B はコミット済み:
- `99eb595` refactor(agent-policy): Agent 可否判定に使われない modelId の配管を除去する(バージョン `0.19.1-dev` 同梱)
- `5688ef9` test(agent-policy): 役割マーカー対応表の fixture を ROLES から導出する

作業ツリーに残るのは無関係な 3 件のみ。

C-2 を Sonnet へ読み替えて再ディスパッチ。D-2(設計書)と並行して走っている。

`★ Insight ─────────────────────────────────────`
- 429 の発生元がプロキシ経由の `claude-gemini-3-8-flash` である点が重要 — Anthropic 側ではなく **CLIProxyAPI の Gemini quota** が枯れている。同じ委譲を Claude モデルへ読み替えれば通る。これは custom-policy が「セッション途中で委譲先が使えなくなったら担当表の Claude モデル列へ読み替える」という規律を持っている理由そのもので、外部ベンダーを混ぜた構成では役割ごとに**フォールバック先が定義されていること**が可用性の前提になる。
`─────────────────────────────────────────────────`

2 件の完了を待つ。

C の差分は指示どおり。1 点だけ字面の傷 — `Antigravity OAuth（Gemini） を必要なものだけ` に全角括弧直後の半角スペースが残っている。執筆者へ直させる。

あわせて、CLAUDE.md に「モデルの通信エラーが起きた時は CLIProxyAPI を疑って調査し `docs/cliproxyapi/` にまとめる」という節が追加された。先ほどの 429 がこれに該当するので調査を起こす。

了解。調査エージェントを止める。

調査を停止。C をコミットする。

スペース除去を確認。C をコミットする。

A・B・C のコミット完了。並行セッションが `69fb736` で 429 の調査記録を既にコミットしていた(「偽装 429」との由)。こちらの調査は停止済みで重複なし。

| コミット | 作業単位 |
| --- | --- |
| `99eb595` | A: 死んだ `modelId` 配管の除去 + バージョン `0.19.1-dev` + バンドル再生成 |
| `5688ef9` | B: 対応表 fixture の `ROLES` 導出化 |
| `bb1f85f` | C: セットアップ手順書の Antigravity (Gemini) 追随 |

作業ツリーに残るのは本件と無関係な 4 件(`.raphael/antibodies/`、`CLAUDE.md`(並行セッションの編集)、`docs/chat/INDEX.md`、新規の会話記録)。いずれも触っていない。

D の設計書の完成を待つ。承認を得るまで実装には入らない。

D-2 は進行中 — 設計書は既に 696 行(74KB)まで書かれ、17:00 に更新されている。既存の設計書と同規模。待つ。

なお隣の `2026-09-16-agent-policy-doc-writing-role-design.md` も 16:52 に更新されている。これは並行セッションの編集で、こちらは触っていない。

D-2 の完了を待って、2 系統のレビュー(独立レビュー・レビュー)へ回す。

設計書が完成。2 系統のレビューへ同時に回す(独立レビューには原本だけを渡し、他のレビューの指摘は渡さない)。

レビュー 2 系統が走っている間に、設計書を自分で読む。

設計書を通読した。品質は高い。特筆すべき発見が 3 つある。

**1. 欠陥の症状が「必ず失敗」ではない(§1.1)**
対応表を読むのは文字列マッチャではなく LLM で、`設計・計画・実装のアドバイザー` は `design, planning, and implementation advisor` のほぼ逐語訳。つまり症状は**「橋渡しの成否が検証不能な推論に委ねられる」**。この事実は (c) を支えると同時に (a) を支える — 設計書はこれを伏せずに両方向へ持ち込んでいる。

**2. 照合経路が 2 系統ある(§3.6。context-map には無かった)**
同じ 2 条項が `_common.md`(生成定義へ焼き込む)と `orchestration-discipline.md:92`/`:93`(オーケストレーターが依頼文へ転記する)に二重化されている。片方だけ直すと、同じサブエージェントが「依頼文はラベルで探せ / 定義本文は RoleId で探せ」という矛盾した指示を同時に受ける。

**3. プロジェクト断片で同梱役割のラベルは上書きできない(§3.2)**
`roleLabel` が `roleById` を先に引いて同梱 `ROLES` のラベルを返して終わる。README:156 の「同じ id を指定すると組み込み断片を置き換えます」は `setup-agents` の断片合成の話でフックには及ばない。→ (c) の回避手段としてこれを案内してはならない。

**3 案の骨格**

| | (a) RoleId 併記 | (b) 言語切り替え | (c) 現状維持 + README |
| --- | --- | --- | --- |
| 解消度 | 決定的に照合可 | 設定が正しければ可 | 解消しない |
| 契約 C1〜C6 | 6 件すべて維持 | b1 は C4 に負荷 / b2 は C2 破壊 | 6 件維持 |
| 変更範囲 | 実装 1 行・断片 4 行・規律 3 行・テスト 2 本 + 新設 1 本 | 新設定 + `labelEn` 17 件 + 導入文/スコープ行の英訳 + 注入経路全体 | README のみ |
| 後方互換 | 悪化する定義が 0 | en 設定で ja 定義が壊れる | 変化なし |
| 混在環境 | ja/en 同居で両方成立 | 原理的に片方が壊れる | 不一致のまま |

サイズ予算は実測済み — 849 → 1087 文字で上限 9500 の **11.4 %**、残余 8413。切り詰めまで現構成の約 8 倍が要る。

`★ Insight ─────────────────────────────────────`
- (b) が「実現可能だが割に合わない」とされる理由が構造的で面白い。対応表は導入文・スコープ行・役割行の 3 部からなるので、**役割行だけ英語にすると表が半分だけ英語になる**。整合を取ると SessionStart の他の注入物(方針スキル指示・未知役割通知・廃止定義通知)まで翻訳対象に入り、最終的に方針スキルと共通規律の英語版という別スコープへ到達する。欠陥は `_common.md` の 2 条項にあるのに、解こうとすると注入経路全体の多言語化を引き受けることになる — 修正の射程が問題の射程を大きく超える典型。
- 設計書自身が「行番号を参照に使うな」という規律を導き出している。同一の検査が半日で `:616` → `:595` → `:587` と 3 つの行番号を持った(A のコミットで HEAD 側も動いた)ため。並行作業がある環境では、文書は**名前で参照する**しかない。
`─────────────────────────────────────────────────`

2 系統のレビュー結果を待って、指摘の採否を判断してからユーザーへ提示する。

1 系統目(レビュー)が返った。指摘を精査した結果、**着手を止める重大な指摘は無い**。採否の判断は次のとおり。

**採る指摘(2 件)**

- **§6.4 の書きぶりが「答え」と「代替」で強さが揃っていない** — 本文は「ja も RoleId へ揃える」と断定しつつ、代替表で「変更量は最小であり、利点は実在する」と書く。ただしこれは §14-2 に未解決事項として登録済みで、設計書としては矛盾ではない。**ユーザーへ提示するときに (a) の選択と同時に聞く**ことで解消する。
- **(c) を採ったとき `orchestration-discipline.md` をどう扱うかが書かれていない** — (c) は README のみとしているので規律は変わらないが、明示が無い。(c) 採択時に 1 文足せば済む。

**採らない指摘(4 件)**

- 「契約 C1〜C6 の定義が初出時に無い」 — §4 に表で定義済み。指摘が事実と違う。
- 「行番号依存を整理すべき」 — §11 の最終行が既に「実装時に対象を名前で引き直す」と規律化している。対処済み。
- 「§6.11 と §6.8 が矛盾」 — §6.8 は「同じ 0.19 系のリリースへ載せるなら」と条件付きで書かれている。読みの誤り。
- 「`setup-agents.ts` の性質が不明」 — このリポジトリの実装者には自明(`src/setup-agents.ts`)。

独立レビュー(反証提示)の結果を待つ。両方を突き合わせてから、案の選択をユーザーへ提示する。

独立レビューの完了を待っている。

独立レビューが鋭い指摘を複数出した。採否を決める前に、判断を左右する事実を自分で確かめる。

4 点すべて実機で裏が取れた。独立レビューの指摘は妥当性が高い。

| 指摘 | 検証結果 |
| --- | --- |
| `orchestration-discipline.md:56` も対応表を役割名で引いている | **確認** — 「『設計書・実装計画書の独立レビュー』の役割は…対応表に無ければ独立レビューを省略する」。設計書 §3.6 が「変更不要」側に誤分類 |
| `en/_common.md:14` は `band` と書き、対応表を指名していない | **確認** — 表を指名するのは `:21` だけ。§1 の「どちらの条項も対応表から役割名で探す」は `:14` について過大 |
| 実測は 846 文字であり 849 ではない | **確認かつ両方正しい** — `.claude/agents/document-writer.md` の vendor はディスク上 `gpt`。セッション開始時に注入された対応表は `(gemini)`。並行作業が定義を再生成したため、測定時点で値が違った |
| `advisor` は `ROLES` の最後 → 切り詰めで最初に落ちる | **確認** — `roles.ts:128` が最終。(a) が照合キーにする 2 役割の片方が、上限超過時に真っ先に消える |

設計書の執筆者へ、採否を確定させた補足修正を差し戻す。

**2 系統のレビューを突き合わせ、採否を確定して差し戻した。**

**採用(事実の訂正 4 件)**
- `orchestration-discipline.md:56` が対応表を役割名で引いている — §3.6 の誤分類。(b) では壊れる条項であり、「共通規律の英語版」がスコープ外ではなく **(b) の射程内**である証拠になる
- `en/_common.md:14` は表を指名していない — (a) で `:14` を書き換えるのは照合キーの置換ではなく**「表を見る義務」の新設**
- 実測は 846 / 1084 — 849 との差は `document-writer` の vendor が測定の間に `gemini` → `gpt` へ変わったため。**両方の測定がその時点では正しかった**
- C4 / C5 の行番号が実際とずれ、C5 が純度検査と範囲を重ねている

**採用(評価の精密化 9 件)** — 最も効くのは 2 つ。

1. **`advisor` は `ROLES` の最後 → 切り詰めで最初に落ちる。** (a) が照合キーにする 2 役割の片方が、上限超過時に真っ先に消える。§6.7 の「末尾の役割が落ちるだけ」という一般論では済まない。
2. **依頼文転記の経路が欠陥を部分的に塞いでいた。** オーケストレーターが `orchestration-discipline.md:92`/`:93`(日本語)を依頼文へ転記するため、en 定義でも転記が行われれば日本語ラベルで照合できる。欠陥の正確な形は「en 定義は照合できない」ではなく**「定義本文と依頼文が別のキーで表を引き、本文側だけが一致しない」**。

**採用(第 4 の案を追加)** — 独立レビューが出した **(d) 二言語併記**(`- 文書作成 / Document Authoring: names`)を同格の案として追加させた。根拠が正しい — **C1(英語出力の純度)は `compose()` の戻り値に対する契約であり、フックが注入する対応表には及ばない**。ただし英語ラベルの供給元が `roles.ts` になるため、(b) と同じ二重管理を招くのが最大の弱点。

**不採用 4 件** — 「C1〜C6 の定義が無い」(§4 に表で定義済み)、「行番号参照を全廃せよ」(§11 で規律化済み)、「`setup-agents.ts` の性質が不明」(自明)、**案 (e) 言語の推定**(b1 の弱点をそのまま持ち、推定ロジックという新しい不確定要素を足すだけ)。

`★ Insight ─────────────────────────────────────`
- 独立レビューが第 4 の案を見つけられたのは、**契約の適用範囲を疑ったから**。設計書は C1(純度検査)を「表へ英語を出せない理由」として暗黙に使っていたが、C1 が縛るのは `compose()` の戻り値だけで、フックが注入する対応表は検査対象外。「この契約はどこまで及ぶのか」を実コードで確かめると、閉じていたはずの選択肢が開く。同じベンダーで固めたレビューだとこの種の前提は共有されたまま素通りする — 独立レビューを別ベンダーに割り当てる規律が効いた実例。
- `advisor` が切り詰めで最初に落ちる件は、**データの並び順が機能の可用性を決めている**例。`ROLES` の定義順は表示順のつもりで決められたが、`truncateTable` が末尾から削るため、暗黙に「優先度の低い順」を意味してしまっている。表示順と優先順を同じ配列で兼ねると、片方を変えたときにもう片方が黙って変わる。
`─────────────────────────────────────────────────`

第 2 版の完成を待ち、次のターンで 4 案をユーザーへ提示して選択を仰ぐ。

第 2 版(961 行)が完成。全 15 項目が反映され、反映できなかった項目は無し。執筆者から 1 件の訂正も来た — **C5 は `id` の集合(断片マップのキー)を比較している**(`compose.test.ts:579`)。per-fragment のループで見ていないのは `label` のほうで、私の指示のほうが不正確だった。実態どおりに書き直されている。

(d) の実測も出た。

| | (a) RoleId 併記 | (d) 二言語併記 |
| --- | --- | --- |
| 増分 | +238(846 → 1084、上限の 11.4 %) | **+553**(846 → 1399、上限の **14.7 %**) |
| 1 行あたり | 10〜21 文字 | 14〜52 文字 |
| 落ちるテスト | 2 本 | 2 本(同じ) |
| 新設テスト | 1 本 | **2 本**(`labelEn` と en 断片の一致検査が要る) |
| 独自役割 | 全行が同じ形 | **英語ラベルの供給元が無く、行の形が揃わない** |

執筆者が (d) を的確に位置づけた — **(d) は (b) の準備作業(`labelEn` の導入)だけを先にやって、言語切り替えの機構は作らない案**。(a) へ進むなら不要な負債になる。

あわせて、フック出力の言語を検査するテストが `src/hooks/__test__/` に **1 本も無い**ことが確認された。C1 が対応表に及ばないという (d) の成立根拠は裏が取れている。

---

## 推奨は (a)。理由を先に述べる

(a) だけが **6 契約すべてを維持したまま、悪化する定義を 1 つも作らない**。ja 定義は行に残るラベルで従来どおり照合でき、en 定義は現状維持か改善のみ。ja/en が同居しても両方成立する。変更は実装 1 行・断片 4 行・規律 4 行で、4 案中で最小。

却下の理由も明確に分かれる。

- **(d)** — 成立はするが、英語ラベルの供給元が `roles.ts` になるため **(b) と同じ二重管理**を先払いする。増分は (a) の 2.3 倍、独自役割では行の形が揃わない。(a) へ進むなら純粋な負債。
- **(b)** — 役割行だけ英語にすると表が半分だけ英語になる。整合を取ると導入文・スコープ行・SessionStart の他の注入物・`orchestration-discipline.md:56` まで翻訳対象に入り、**欠陥は `_common.md` の 2 条項にあるのに注入経路全体の多言語化を引き受ける**ことになる。混在環境では原理的に片方が壊れる。
- **(c)** — 欠陥は解消しない。ただし依頼文転記の経路が部分的に穴を塞いでいる事実(§1.2)があり、en 運用の実績がまだ無いことを踏まえれば「使うようになってから直す」という判断には合理性がある。

**ただし決めるのはあなた。**判断を左右する軸で分かれる点が 1 つある — **対応表を「人が読む一覧」と見るか「機械的に引ける索引」と見るか**。(a) は後者に倒す案で、17 行すべてに `[RoleId]` が並ぶ見た目を受け入れることになる。

`★ Insight ─────────────────────────────────────`
- (d) の評価で効いたのは「この案は何を先払いするか」という見方。(d) 単体では (b) より軽いが、`labelEn` を入れた時点で (b) の一部を実装したことになる。**中間案は、最終的にどこへ向かうかが決まっていないと「両方の負債を持つ」結果になりやすい** — (a) へ行くなら `labelEn` は不要、(b) へ行くなら (d) は途中下車。方向が決まっていない段階での中間案は、選択肢を増やすのではなく後戻りのコストを増やすことがある。
`─────────────────────────────────────────────────`

**(a) RoleId 併記で決定。**

バージョンは追加で聞かない — goal が「`0.19.0-dev` → `0.19.1-dev` を A・B・D の実装で 1 回だけ上げる」と定めており、A・B で既に消費済み。D は `0.19.1-dev` に相乗りする(§15-3 は解決)。

残る分岐が 1 つある。

**決定が揃った。**案 (a)、ja 断片も揃える、バージョンは `0.19.1-dev` 据え置き。実装に入る。コードと文書は対象ファイルが交わらないので並列に走らせる。

D の実装を 2 系統で走らせている。

| 担当 | 対象 |
| --- | --- |
| 複雑または重要な実装 | `src/hooks/marker-scan.ts`(1 行)、落ちるテスト 2 本、新設テスト 1 本、build、対応表の実測 |
| 文書作成 | `assets/roles/{ja,en}/_common.md`(4 箇所)、`references/orchestration-discipline.md`(3 箇所)、`README.md`(2 箇所) |

対象ファイルは交わらない。両者へ「相手が編集中のパスに触るな」を明記済み。

実装側に念押しした点が 2 つある。

1. **新設テストの正規表現は `\[[^\]]+\]` にする。** 独立レビューの指摘どおり、`[a-z0-9-]+` だと独自役割 ID の `my_role` / `Foo` が落ちる。文字種制約は README にも `setup-agents` にも無いので、狭い正規表現は「全行に一律」という設計と矛盾する。
2. **行順検査の探索文字列を `[complex-impl]` へ差し替えるとき、それが SessionStart の注入物の対応表以外の部分に現れないか確認する。** 現れると `indexOf` が誤った位置を拾い、テストが通ったまま意味を失う。

残る手順は、両者の完了後に私が行う — Serena メモリ `agent_policy/core` のバージョン表記の追随、`.claude/agents/` の再生成(既存定義の `disallowedTools` と MCP 付与を控えてから)、D のコミット。

`★ Insight ─────────────────────────────────────`
- 実装を「コード」と「AI が読む文書」で割ったのは、規律上の要請だけが理由ではない。この 2 つは**壊れ方が違う**。コードはテストが落ちて気づくが、`_common.md` や `orchestration-discipline.md` の文言はどのテストも見ていない(検査があるのは `:20` の 1 文だけ)。文書側は「指示された文言を 1 文字も変えない」を制約として渡すしかなく、だからこそ変更前後の全文を依頼文に書いて照合可能にした。検査で守れないものは、依頼の精度で守る。
`─────────────────────────────────────────────────`

文書側が完了。差分を突き合わせる。

文書側の差分は指示どおり。`en/_common.md` の日本語混入ゼロも実測で確認。規律と README も確認する。

文書側 8 箇所すべて指示どおり。担当表の表(`:7-25`)は無変更、`:56` の独立レビュー条項も無変更 — いずれも意図どおり。

実装側の完了を待つ。

`★ Insight ─────────────────────────────────────`
- `en/_common.md:14` の変更は、見た目は照合キーの置換だが**実質は新しい義務の追加**。変更前は "Use the definition for the '...' band" で、どこを見て探すのかを一切言っていなかった(`band` は 0.18 以前の死語)。変更後は "the role marker table row whose RoleId is `advisor`" と、**参照先の表を明示している**。en 側のアドバイザー照合は「英語名が表と一致しない」以前に「表を見る指示すら無かった」状態で、今回それが初めて埋まった。独立レビューがこれを掘り当てなければ、設計書は「不一致を直した」とだけ記録して、指示の欠落のほうを見落としていた。
`─────────────────────────────────────────────────`

実装側は進行中 — `marker-scan.ts:241` の変更は入り、テスト 2 本も更新済み、`pnpm run build` も済んで `scripts/` 3 本に差分が出ている。新設テストと最終検証の段階と見られる。待つ。

なお `CLAUDE.md` の差分は消えている(並行セッションがコミットしたもの)。新しく `docs/chat/2026/0916/phyllis998/1653-...md` が増えているが、これも並行セッションの会話記録であり本件とは無関係。触らない。

実装完了。全 green — test **2255 passed**(baseline 2254 + 新設 1)、lint/typecheck/build いずれも exit 0。対応表は **846 → 1084 文字(上限の 11.4 %)** で設計書の想定と完全一致。

差分を検証しつつ、残る文書の追随をディスパッチする。

実装差分を検証した。指示どおりで、余計な変更なし。新設テストの fixture にアンダースコアを含む `my_role` が入っており、狭い文字種契約を持ち込んでいないことが実データで担保されている。

Serena メモリのバージョン表記が `0.19.0-dev` で止まっていることも確認(実際は `0.19.1-dev`)。設計書の状態更新と合わせて追随中。

**残る作業の判断を 1 つ共有する。**

設計書の Done 条件には「`.claude/agents/` を再生成し、新しい `_common.md` の文言が入ることを確認する」がある。**これはこのセッションでは実行しない**。理由は 3 つ。

1. `agent-policy:setup-agents` は対話ウィザードであり、モデル・役割・MCP 付与の選択を伴う。非対話で流すと既存 14 定義の `disallowedTools` と MCP 付与を取りこぼす。
2. このセッションの最中に `document-writer` の vendor が `gemini` → `gpt` へ変わった。**並行セッションが `.claude/agents/` を管理している**証拠であり、こちらから再生成すると相手の状態を上書きしうる。
3. `.claude/agents/` は gitignore 対象で、コミットにも成果物にも出ない。再生成しなくても D の変更内容は完結している。

再生成は、あなたが `/agent-policy:setup-agents` を実行するのが確実。プラグイン利用者側の再生成は README の移行節 10 番が案内している。

文書の追随が終わり次第 D をコミットする。

設計書の更新は完了。Serena メモリは委譲先のツールに書き込み系が無く未着手 — 内容は確定しているので私が適用する。あわせて (d) 固有の未解決事項 2 項の決着を依頼する。

メモリに、A の変更で陳腐化した記述を見つけた — 「`ComposeInput.modelId` / `Target.composeModelId` plumbing still exists but is dead」は A(`99eb595`)で除去済み。バージョン表記と併せて 3 箇所を直す。

設計書の未解決事項 8 項すべてに決着が付いた。メモリの残り 2 箇所を直す。

メモリ 3 箇所を更新。最終検証を通してから D をコミットする。

全検証 exit 0、test 2255 passed。D をコミットする。設計書と実装で 2 つに分ける。

D をコミット完了。作業ツリーに残るのは無関係な 4 件のみ。Done 条件の残りを確認する。

実際に注入される対応表を自分で描画して確認した。**1084 文字 / 上限の 11.4 %**、17 役割すべてに `[RoleId]` が載っている。報告どおり。

---

# 完了報告

## A〜D の決着

| | 内容 | 決着 |
| --- | --- | --- |
| **A** | `ComposeInput.modelId` / `Target.composeModelId` の死んだ配管の除去 | **実装完了。**代入 3 箇所 + 受け渡し 1 箇所 + 型定義 2 件を削除。`compose.test.ts` の `buildWithoutModelId` は `build` と同義になったため統合。推奨モデル ID を識別する `Target.modelId` は別物なので残した |
| **B** | `subagent-start.test.ts` の陳腐化した fixture | **実装完了。**14 件のハードコード配列を `ROLES.map((role) => role.id)` へ導出化。切り詰め検査の閾値は文字数(9500)で役割件数に依存しないため、17 件でも検査意図のまま通過 |
| **C** | `cliproxyapi-setup.md` の Gemini 追随 | **実装完了。**9 箇所。ログインコマンド `--antigravity-login`・コールバックポート 51121・パス `/oauth-callback` は CLIProxyAPI 公式ドキュメントと `cli-proxy-api --help`(v7.2.155)の一次情報で確認 |
| **D** | en 生成定義と日本語対応表の不整合 | **設計書がユーザー承認を得て、実装完了。** |

## コミット一覧

| コミット | 作業単位 |
| --- | --- |
| `99eb595` | **A** — 死んだ配管の除去 + バージョン `0.19.1-dev` + `scripts/setup-agents.mjs` の再生成 |
| `5688ef9` | **B** — fixture の `ROLES` 導出化 |
| `bb1f85f` | **C** — セットアップ手順書の Antigravity (Gemini) 追随 |
| `aa06b47` | **D** — 4 案を比較した設計書 |
| `12b682e` | **D** — 実装・断片・規律・README・Serena メモリ + `scripts/` 3 本の再生成 |

## バージョン

`0.19.0-dev` → **`0.19.1-dev`**。`package.json` と `plugin.json` が揃っている。引き上げは A のコミットで 1 回だけ。

## D で採用した案と却下した案

**採用: (a) 対応表の各行に RoleId を併記する。**

決め手は 2 つ。第一に、既存 6 契約(英語出力の純度・両フックの文面一致・担当表と `ROLES` の一致・ja 断片のラベル一致・断片のメタ一致・9500 文字上限)を**どれも崩さない**。RoleId が ASCII なので純度検査に触れないのが効いている。第二に、軸「後方互換」「混在環境」で**唯一、悪化する定義が 1 つも無い** — 行にラベルが残るので ja 定義は従来どおり、en 定義は現状維持か改善のみ。変更は実装 1 行・断片 4 行・規律 4 行で 4 案中最小だった。

**却下した 3 案。**

- **(d) 二言語併記** — 成立はする。C1 が `compose()` の戻り値にしか及ばないので表へ英語を出しても検査は落ちない。却下理由は、英語ラベルの供給元が `roles.ts` になり **(b) と同じ二重管理を先払いする**こと。増分は (a) の 2.3 倍(+553 対 +238)、独自役割には英語名が無く行の形が揃わない。(a) へ進むなら純粋な負債になる。
- **(b) 言語切り替え** — 実現手段は存在する(プロジェクト単位の設定ファイル)。却下理由は**射程が問題を超える**こと。役割行だけ英語にすると表が半分だけ英語になり、整合を取ると導入文・スコープ行・SessionStart の他の注入物・`orchestration-discipline.md:56` の独立レビュー条項まで翻訳対象に入る。欠陥は `_common.md` の 2 条項にあるのに注入経路全体の多言語化を引き受けることになる。加えて混在環境で原理的に片方が壊れ、en 設定にした瞬間に既存 ja 定義が照合できなくなる。
- **(c) 現状維持 + README** — 欠陥を解消しない。README を読むのは人間で、条項を実行するのはサブエージェント。ただし依頼文転記の経路が部分的に穴を塞いでいた事実は認めたうえで、「オーケストレーターが規律を守った場合にのみ効く経路」であり定義本文の欠陥は残ると判断した。

## 残っている未解決事項

1. **`.claude/agents/` の再生成** — このセッションでは実行していない。`setup-agents` は対話ウィザードで、非対話に流すと既存 14 定義の `disallowedTools` と MCP 付与を取りこぼす。加えてセッション中に `document-writer` の vendor が `gemini` → `gpt` へ変わっており、**並行セッションが `.claude/agents/` を管理している**。`/agent-policy:setup-agents` をあなたの手で実行するのが確実。gitignore 対象なのでコミットには影響しない。
2. **引き継ぎ書 §4 のスコープ外 4 件** — 運用観測待ち 3 件(`doc-writing` が実際に呼ばれるか / `gemini-flash`・`gpt-terra` の日本語品質 / `light-impl`・Haiku への再委譲の妥当性)と、別起票 1 件(`setup-agents` の兼務検出、CLI 変更を伴う)。いずれも今回の対象外のまま。
3. **ADR を起こすかの判断** — `ARCHITECTURE.md` に役割マーカー・対応表・`markerTable` の記述は無く(grep で確認)、追随は不要。ただし ADR-001 が同じ粒度のプラグイン内部判断(MCP ツール許可の単位)を記録している前例があるので、「対応表と定義本文を RoleId で結ぶ」を ADR にする選択肢はある。必要なら `/metatron:updating-architecture` を起動して追加する。

設計書 §16 の未解決事項 8 項はすべて決着済み。

## 副産物

引き継ぎ書と既存設計書の記述に、実際とずれていた箇所が 4 つあった。いずれも修正版を設計書へ反映してある。

- config の alias は `claude-gemini-3-1-pro` ではなく **`claude-gemini-3.1-pro-high`**
- README の既知の限界は `:152`。引き継ぎ書が挙げる `:158` は別の話題
- `en/_common.md:14` は**対応表を指名していなかった**(`band` という 0.18 以前の用語が残存)。今回の変更は照合キーの置換ではなく「表を見る義務」の新設にあたる
- 同じ 2 条項が `orchestration-discipline.md:92`/`:93` にもあり、**照合経路が 2 系統あった**。片方だけ直すとサブエージェントが矛盾した指示を受ける

`★ Insight ─────────────────────────────────────`
- この欠陥が 5 か月気づかれなかった理由が、テストの構造そのものにある。`subagent-start.test.ts` は `markerTable()` の戻り値を期待値にしているので、**生成側と検査側が同じ関数を呼ぶ**。形式を変えても常に一致し、契約は何も守っていない。契約を縛っていたのは `marker-scan.test.ts` のリテラル完全一致だけで、今回落ちたのもそれだけだった。「期待値をどこから取るか」がそのまま「何を守れるか」になる。
- そして守れていなかったのは**表と条項の対応**という、どちらのファイルにも書かれていない関係だった。今回新設した形式検査も表の側しか見ていないので、`_common.md` が RoleId で引くことは依然としてテストできない。フック出力の言語を検査するテストは `src/hooks/__test__/` に 1 本も無い。決定的に照合できるようになった一方で、「モデルが実際に RoleId を使うか」は観測不能なまま残っている — 独立レビューが指摘したこの限界は、(a) を採っても消えていない。
`─────────────────────────────────────────────────`

# phyllis998

> claude-gemini-3.1-pro-highについて、@cliproxyapi.config.yaml には確かに claude-gemini-3-1-pro というエイリアスを設定しており、claude-gemini-3.1-pro-high というのはどこにもありません。CLIProxyAPIの公式をしらべたり、別ポートでサーバーを建てたりして、本当はどのIDに対するエイリアスを設定すべきかを検証してください

# AI

重要な指摘。**私は `cliproxyapi.config.example.yaml`(コミット済みの例)だけを見て、実際に動いているローカル設定 `cliproxyapi.config.yaml` を見ていなかった。**両者は食い違っている。

| | example(コミット済み) | ローカル実設定 |
| --- | --- | --- |
| `claude-gemini-3-8-flash` の上流 | `gemini-3.8-flash` | **`gemini-3.8-flash-high`** |
| もう 1 つの alias | `claude-gemini-3.1-pro-high` ← `gemini-3.1-pro-high` | **`claude-gemini-3-1-pro`** ← **`gemini-pro-agent`** |

つまり C のコミット(`bb1f85f`)で手順書に書いた上流 ID は、example 由来の値であって実在を確かめていない。除外リストに `gemini-3.1-pro-low` や `gemini-3.7-flash-high` があることから、実際の命名は effort サフィックス付きらしく、`gemini-pro-agent` のように体系の違う ID も混ざっている。

別ポートで alias も除外も無いインスタンスを立てて、生の `/v1/models` を取れば確定する。調査を出す。

API キーの制約解除を調査側へ伝えた。

この検証で分かることは 3 つある。

1. **example 設定が実在しない上流 ID を指していないか。** 除外リストが `gemini-3.7-flash-high` / `gemini-3.6-flash-high` / `gemini-3.1-pro-low` のように effort サフィックス付きで書かれているのに、example の alias だけ `gemini-3.8-flash` / `gemini-3.1-pro-high` とサフィックスの付き方が揃っていない。実設定側の `gemini-3.8-flash-high` のほうが体系に沿っている。
2. **`gemini-pro-agent` が何なのか。** 他の `gemini-3.x-*` と命名体系が違う。これが「Gemini 3.1 Pro」に相当するなら、example の `gemini-3.1-pro-high` は存在しない ID を指していることになる。
3. **alias の `name` が実在しないときに何が起きるか。** 黙って無視されるなら、example の誤りは今まで誰にも見えなかったことになる — C のコミットで手順書に書いた対応表も、その誤りをそのまま引き写している。

結果しだいで直す対象は 2 つ。`cliproxyapi.config.example.yaml` の alias 定義と、`docs/development/cliproxyapi-setup.md` に書いた別名の説明(`bb1f85f`)。

`★ Insight ─────────────────────────────────────`
- この見落としの構造がはっきりしている。私は「実物と照合した」と報告したが、照合先は `cliproxyapi.config.example.yaml` という**サンプル**であって、実際に動いている `cliproxyapi.config.yaml` ではなかった。後者は gitignore 対象なので、リポジトリを読む探索では視界に入らない。`.gitignore` されたファイルは「無い」のではなく「見えない」だけで、稼働しているのはそちらのほう — `.claude/context-maps/` に 13 本の map があったのに探索統括が「1 本も無い」と報告したのも、まったく同じ理由だった。同じ盲点をこのセッションで 2 回踏んでいる。
`─────────────────────────────────────────────────`

検証結果を待つ。

antigravity の生モデル ID の実測結果を待っている。判明しだい、`cliproxyapi.config.example.yaml` の alias 定義と、`bb1f85f` で手順書に書いた別名の説明を直す。

## セッション 2: agent-policy 対応表への RoleId 併記実装と 14 個の agent 定義再生成

# AI

antigravity の生モデル ID の実測結果を待っている。

antigravity の生モデル ID の実測結果を待っている。

**確定した。example 設定の alias は 2 件とも実在しない上流 ID を指していた。**

port 18317 に alias も除外も無いインスタンスを立てて取った antigravity の生一覧(12 件):

```
claude-opus-4-6-thinking   claude-sonnet-4-6
gemini-3-flash             gemini-3.1-flash-image
gemini-3.1-flash-lite      gemini-3.1-pro-low
gemini-3.5-flash-lite      gemini-3.6-flash-high
gemini-3.7-flash-high      gemini-3.8-flash-high
gemini-pro-agent           gpt-oss-120b-medium
```

| example が指す `name` | 実在 | 正しい ID |
| --- | --- | --- |
| `gemini-3.8-flash` | **無い** | **`gemini-3.8-flash-high`**(display_name は "Gemini 3.8 Flash") |
| `gemini-3.1-pro-high` | **無い** | **`gemini-pro-agent`**(display_name / description とも "Gemini 3.1 Pro (High)") |

**決め手は除外リストとの照合。** example の `oauth-excluded-models.antigravity` は 10 件で、生一覧 12 件から引くとちょうど `gemini-3.8-flash-high` と `gemini-pro-agent` の 2 件が残る。**除外リストは実在 ID で完璧に書かれており、alias だけが実在しない名前を指している。**除外は実測から起こし、alias は手で「きれいな名前」に整えてしまったのだろう。ユーザーのローカル設定はまさにこの 2 件を alias しており、正しい。

**なぜ今まで表面化しなかったか。** `applyOAuthModelAliasEntries` は既存モデルを走査してヒットしたときだけ clone する。`name` が実在しなければ**黙って無視され、エラーにもログにも出ない**(`sdk/cliproxy/service_models.go`)。適用順は除外 → alias。

**実害はある。** example 設定をそのまま使うと `claude-gemini-3-8-flash` が `/v1/models` に現れず、生 ID `gemini-3.8-flash-high` と `gemini-pro-agent` がそのまま出る。agent-policy の ModelId `gemini-flash` は `model: "claude-gemini-3-8-flash"` を前提にしているので、**example のままでは agent-policy の Gemini サポートが成立しない。**

## 直すもの

alias 名も揃える。example の `claude-gemini-3.1-pro-high` はドットを含み、他の alias(`claude-gpt-5-6-sol` 等、ハイフン統一)と不揃い。ローカル実設定の `claude-gemini-3-1-pro` が命名規則に沿うのでそちらへ寄せる。pro の alias 名に依存するコードは無い(`MODELS` にあるのは `claude-gemini-3-8-flash` のみ)。

修正を 2 系統で走らせている。設定例のほうは、直した内容を別ポートのインスタンスに実際に読ませて `/v1/models` に `claude-gemini-3-8-flash` と `claude-gemini-3-1-pro` がちょうど 2 件出ることまで検証させる — 誤りが黙って無視される種類の設定なので、直したことも実機で確かめないと同じ罠になる。

`★ Insight ─────────────────────────────────────`
- この誤りは「検証不能な設定」の典型。`oauth-model-alias` の `name` は実在しなければ黙って捨てられるので、**設定ファイルを読んだだけでは正しさを判定できない**。除外リストのほうは実測から起こされていて正しく、alias だけが手で整形されて壊れていた — 同じファイルの中に「観測由来の正しい記述」と「推測由来の誤った記述」が同居していたことになる。
- 私が「実物と照合済み」と報告した相手は `cliproxyapi.config.example.yaml` だった。それは**サンプル**であって、稼働しているのは gitignore された `cliproxyapi.config.yaml` のほう。設定ファイルの「正本」がどちらかは、ファイル名ではなくプロセスが読んでいるほうで決まる。今回はプロセスに聞くまで分からなかった。
`─────────────────────────────────────────────────`

両方の完了を待って差分を突き合わせ、コミットする。

手順書の訂正が完了。設定例の実機検証を待っている。

設定例の実機検証を待っている。

実機検証 PASS。差分を突き合わせてコミットする。

差分は指示どおり、検証用インスタンスは停止済み、稼働中の 8317 は無事。コミットする。

コミット完了。この失敗は次に同じリポジトリを触るエージェントも踏むので、台帳へ残す。

台帳は未作成(`append-gotcha` が雛形ごと作る)。既存エントリが無いので重複もない。

記録の可否: **Yes**。「次にこのプロジェクトを触るエージェントがこれを知らないと同じ失敗をするか」に対して、gitignore されている稼働資産がどれかはこのリポジトリ固有の事実であり、知らなければ同じ判断をする。一般則(「gitignore されたファイルは git ベースの探索で見えない」)は別リポジトリでも成立するので原因側へ置き、固有の事実を対策側へ切り出す。

内容を確定したので執筆と CLI 実行を委譲する。

了解。GOTCHAS は不要。エージェントを止める。

台帳は未作成のまま、余計な差分も出ていない。作業ツリーに残るのは本件と無関係な 5 件のみ。

---

# 最終報告

## A〜D の決着

| | 内容 | 決着 |
| --- | --- | --- |
| **A** | 死んだ `modelId` 配管の除去 | 実装完了 |
| **B** | fixture の `ROLES` 導出化 | 実装完了 |
| **C** | `cliproxyapi-setup.md` の Gemini 追随 | 実装完了(後述の訂正あり) |
| **D** | en 生成定義と日本語対応表の不整合 | 設計書が承認され、実装完了 |

## コミット一覧

| コミット | 作業単位 |
| --- | --- |
| `99eb595` | **A** — 死んだ配管の除去 + バージョン `0.19.1-dev` + バンドル再生成 |
| `5688ef9` | **B** — 対応表 fixture の `ROLES` 導出化 |
| `bb1f85f` | **C** — 手順書の Antigravity (Gemini) 追随 |
| `aa06b47` | **D** — 4 案を比較した設計書 |
| `12b682e` | **D** — 実装・断片・規律・README・Serena メモリ + バンドル再生成 |
| `a244269` | **C の訂正** — antigravity の別名が指す上流モデル ID を実在する値へ直す |

**プラグインバージョン: `0.19.1-dev`**(`package.json` と `plugin.json` が一致)。

## D の採否

**採用は (a) 対応表の各行に RoleId を併記する。**6 契約をどれも崩さず、変更範囲が最小で、「悪化する定義が 1 つも無い」性質を唯一持つため。ja 断片も RoleId 照合へ揃える(ユーザー選択)。

却下は **(d) 二言語併記**((b) と同じ二重管理を先払いし、増分が 2.3 倍、独自役割で行の形が揃わない)、**(b) 言語切り替え**(修正の射程が注入経路全体の多言語化に及び、混在環境で原理的に片方が壊れる)、**(c) 現状維持**(欠陥を解消しない)。

## C の訂正について

`bb1f85f` で書いた上流モデル ID は誤りだった。根拠にしたのが配布用サンプルの `cliproxyapi.config.example.yaml` で、そのサンプル自体が実在しない ID を指していた。別ポート(18317)に別名も除外も無いインスタンスを立てて実測し、`gemini-3.8-flash-high` と `gemini-pro-agent` が正しいと確定。直した設定を同じインスタンスに読ませ、`/v1/models` が `claude-gemini-3-8-flash` と `claude-gemini-3-1-pro` のちょうど 2 件になることまで確認した(検証用インスタンスは停止済み、稼働中の 8317 は無傷)。

この誤りは放置すると実害があった。`claude-gemini-3-8-flash` が公開されず、agent-policy の ModelId `gemini-flash` が前提にするモデルが存在しないことになる。

## 残っている未解決事項

1. **`.claude/agents/` の再生成** — 未実施。`setup-agents` は対話ウィザードで、非対話に流すと既存定義の `disallowedTools` と MCP 付与を落とす。加えてセッション中に `document-writer` の vendor が変わっており、並行セッションが管理している。`/agent-policy:setup-agents` をあなたの手で実行するのが確実。gitignore 対象なのでコミットには出ない。
2. **引き継ぎ書 §4 のスコープ外 4 件** — 運用観測待ち 3 件と、別起票 1 件(`setup-agents` の兼務検出)。今回の対象外のまま。
3. **ADR の要否** — `ARCHITECTURE.md` に対応表・`markerTable` の記述は無く追随は不要。ADR-001 が同程度の粒度のプラグイン内部判断を記録している前例はあるので、起こす選択肢はある。

GOTCHAS は記録していない。設計書 §16 の未解決事項 8 項はすべて決着済み。

`★ Insight ─────────────────────────────────────`
- 今回の 2 つの誤りは同じ形をしている。D の欠陥は「表示ラベルを同一性の判定に使っていた」ため翻訳で壊れた。C の誤りは「サンプルを実態の根拠に使っていた」ため実設定とずれた。どちらも**役割の違うものを同一視した**結果で、`RoleId` を表に載せる修正と、実設定を読む規律は、同じ種類の分離をしている。
- そして両方とも、間違ったまま何も起きなかった。対応表の照合は LLM が意味で橋渡ししてしまい、alias の誤りは CLIProxyAPI が黙って無視する。**失敗が観測されない仕組みでは、正しさは検査でしか担保できない** — 今回 (a) で新設した形式検査と、別ポートでの実測が、それぞれその役目を負っている。
`─────────────────────────────────────────────────`

`/agent-policy:setup-agents` を対話モードで開始する。会話言語は日本語なので `lang = ja`。

まず既存定義の現状を控える。設計書が警告していたとおり、再生成の前に `disallowedTools` と MCP 付与を記録しておく。

既存定義 14 件をすべて控えた。内訳は **Claude のモデル 5 件**(`sonnet` × 2、`haiku`、`opus`、`fable`)、**外部ベンダー 9 件**(GPT 6 件、Grok 3 件)。

MCP 付与と `disallowedTools` の現状も記録した。読み取り役割の定義には Serena の書き込み系 11 ツールが `disallowedTools` に入っている。`knowledge-elicitationer` だけ MCP なし(`Read` / `Grep` / `Glob` のみ)、`document-writer` は `mcp__serena` のみ。実装役割の 4 件に `mcp__plugin_codiel_raguel` が付いている。

なお `document-writer` の `model` は `claude-gpt-5-6-terra` になっている。Gemini の 429 を受けて並行セッションが差し替えたもの。

- [ ] ステップ 0: 言語判定 → `ja`
- [ ] ステップ 0b: 構成の選択
- [ ] ステップ 1: live models の照会
- [ ] ステップ 1b: 既存定義の被覆確認
- [ ] ステップ 3〜7

カスタム構成で進める(`--scope custom`、`--lang ja`)。live models と既存定義の被覆を取る。

live models 照会は成功。**`claude-gemini-3-8-flash` と `claude-gemini-3-1-pro` が両方とも実在している** — 先ほど直した alias が効いていることの裏付けにもなっている。

被覆は **`uncovered` が空**。全 17 役割が既存 14 定義で埋まっている。

| 役割 | 推奨モデル | 既存定義 |
| --- | --- | --- |
| 複雑または重要な実装 | opus / gpt-sol | lead-implementer |
| 通常の実装 | sonnet / gpt-luna / grok | general-implementer |
| 軽量な実装 | haiku / gpt-luna / grok | general-implementer |
| 行き詰まり時のエスカレーション | fable / gpt-astra | technical-leader |
| その他のタスク | sonnet / gpt-luna | general-implementer, general-worker |
| 設計書・実装計画書(WBS)の作成 | opus | system-planner |
| 文書作成 | sonnet / gemini-flash / gpt-terra | document-writer |
| コードベース探索統括 | opus | system-planner |
| コードベース探索実働 | sonnet / grok / gpt-terra | general-explore |
| リアルタイム情報調査 | sonnet / grok | realtime-researcher |
| E2E 動作検証・ブラウザ/GUI 操作 | sonnet / gpt-astra | complex-reviewer |
| 設計書・実装計画書の独立レビュー | sonnet / grok | docs-reviewer |
| 設計書・実装計画書のレビュー | haiku | knowledge-elicitationer |
| コードレビュー | sonnet | code-reviewer |
| 重要な実装の最終レビュー | fable / gpt-astra | complex-reviewer |
| 設計書の最終ゲートレビュー | fable / gpt-astra | complex-reviewer |
| 設計・計画・実装のアドバイザー | fable / gpt-astra | independent-tech-adviser, technical-adviser |

未カバーの役割が無いため、選択肢は 2 つになる。

中止した。**何も生成していない。**`.claude/agents/` は無変更。

## 状態

既存 14 定義はそのまま。含まれる `_common.md` は旧文言(役割名で対応表を引く)のまま残る。

これによる実害は無い。今回の変更は対応表の行を `- 文書作成 [doc-writing]: document-writer (gpt)` の形にしたもので、**役割名は行の先頭に残っている**。日本語で生成された既存定義は従来どおり役割名で照合できる。設計書が案 (a) を選んだ理由のひとつが、まさにこの「再生成しなくても悪化しない」性質だった。

追随したいときは、いつでも `/agent-policy:setup-agents` を実行して「すべてのモデルを選び直す」を選べばよい。保持マージで進めれば `disallowedTools`・MCP 付与・手で加えた編集は残る。

## 実行の副産物

`--list-live-models` の結果で 1 つ確認が取れた。プロキシが返すモデルに **`claude-gemini-3-8-flash`(vendor: gemini)と `claude-gemini-3-1-pro`(vendor: gemini)の両方が実在している**。先ほど `a244269` で直した alias の上流 ID がこの 2 件を指しており、設定例の修正が実態と合っていることの追加の裏付けになる。

あわせて `document-writer` の推奨モデルが `sonnet` / `gemini-flash` / `gpt-terra` の 3 件であることも確認できた。現在の割り当ては `gpt-terra`(`claude-gpt-5-6-terra`)で、Gemini の 429 を受けた差し替えが効いている。

`★ Insight ─────────────────────────────────────`
- 再生成を見送っても壊れないのは、案 (a) が**加算的な変更**だったから。行に情報を足しただけで、既存の手掛かり(役割名)を奪っていない。もし (b)(言語切り替え)や「RoleId だけにする」案を採っていたら、この選択肢は存在せず、全定義の再生成が必須になっていた。設計の段階で「軸 6 後方互換」を立てておくと、実装後の運用でこういう逃げ道が残る。
`─────────────────────────────────────────────────`

# phyllis998

> 間違えました。生成しなおします

# AI

「すべてのモデルを選び直す」として再開する。live models と被覆は取得済みなので、ステップ 4 へ進む。

既存 14 定義は推奨構成とほぼ一致しているので、まず**現状の割り当てをそのまま再生成する案**を第一候補にする。live models に実在する推奨エイリアスの状況を確認してから聞く。

**重要な発見。**推奨構成の一括生成(`--models`)は**使えない**。

推奨構成が作るファイル名は `gpt-sol-lead-implementer` / `gpt-terra` / `sonnet` / `opus` などの既定名で、既存 14 定義の名前(`lead-implementer` / `document-writer` / `system-planner` …)と一致しない。全件が `exists: false` で返っている。このまま一括生成すると**既存 14 件はそのまま残り、別名の新規 10 件が並んで増える** — 同じ役割に 2 つの定義がぶら下がる。

さらに役割の配り方も違う。既存は 1 定義 1 関心に手で割ってある。

| | 推奨構成 | 既存 |
| --- | --- | --- |
| `gpt-astra` | escalation + e2e-verify + final-review + gate-review + advisor を 1 件に | `technical-leader` / `complex-reviewer` / `independent-tech-adviser` の 3 件に分割 |
| `grok` | normal-impl + light-impl + explore + realtime-research + independent-review を 1 件に | `general-explore` / `realtime-researcher` / `docs-reviewer` の 3 件に分割 |
| `sonnet` | 8 役割を 1 件に | `code-reviewer` / `general-worker` の 2 件のみ |
| `opus` | complex-impl + design-plan + explore-lead | `system-planner` は design-plan + explore-lead のみ |

したがって**既存 14 件を 1 件ずつ個別に再生成する**のが正しい。既存の名前・`model` 値・役割を維持したまま `--check` を取る。

差分の性質がはっきりした。

**節が変わるのは Agent tool が付く定義だけ**(10 件)。変わる 2 節は `## アドバイザーへの相談` と `## 制約` で、これがまさに今回の `_common.md` の変更(RoleId 照合)にあたる。Agent tool が「否」の定義(`document-writer` / `code-reviewer` / `independent-tech-adviser` / `technical-adviser`)はこの 2 節を出力しないため、**本文は変わらない**。`knowledge-elicitationer` は完全に `identical: true`。

残りの差分はすべて**既存にしかないもの** — MCP tools と `disallowedTools`。これは再生成時に明示的に渡さないと落ちる。

MCP のサーバー名を確認する。

接続済み MCP サーバーは 3 件(`plugin:context7:context7` / `plugin:codiel:raguel` / `serena`)。すべて `usable: true` で、既存の付与状況とも一致する。

## 再生成の全体像

| 定義 | model | 役割 | MCP(既存を維持) | deny | 本文の変化 |
| --- | --- | --- | --- | --- | --- |
| `lead-implementer` | claude-gpt-5-6-sol | complex-impl | context7 / raguel / serena | — | **2 節** |
| `general-implementer` | claude-gpt-5-6-luna | normal-impl, light-impl, general | context7 / raguel / serena | — | **2 節** |
| `technical-leader` | claude-gpt-6-astra | escalation | context7 / raguel / serena | — | **2 節** |
| `general-worker` | sonnet | general | context7 / raguel / serena | — | **2 節** |
| `system-planner` | opus | design-plan, explore-lead | context7 / serena | — | **2 節** |
| `complex-reviewer` | claude-gpt-6-astra | e2e-verify, final-review, gate-review | context7 / serena | 11 | **2 節** |
| `general-explore` | claude-grok-4-6 | explore | context7 / serena | 11 | **2 節** |
| `realtime-researcher` | claude-grok-4-6 | realtime-research | context7 / serena | 11 | **2 節** |
| `docs-reviewer` | claude-grok-4-6 | independent-review | context7 / serena | 11 | **2 節** |
| `independent-tech-adviser` | claude-gpt-6-astra | advisor | context7 / serena | 11 | 変化なし |
| `technical-adviser` | fable | advisor | context7 / serena | 11 | 変化なし |
| `code-reviewer` | sonnet | code-review | context7 / serena | 11 | 変化なし |
| `document-writer` | claude-gpt-5-6-terra | doc-writing | serena | 11 | 変化なし |
| `knowledge-elicitationer` | haiku | doc-review | なし | — | `identical` |

`deny` の 11 件は Serena の書き込み系(`delete_memory` / `edit_memory` / `insert_after_symbol` / `insert_before_symbol` / `rename_memory` / `rename_symbol` / `replace_content` / `replace_in_files` / `replace_symbol_body` / `safe_delete_symbol` / `write_memory`)。

14 件を個別に保持マージ生成する。

14 件すべて `merged`、`mcpDropped` は空、`keptNeedsReview` なし。結果を検証する。

# 生成完了

## 結果

`.claude/agents/` に **14 定義**、すべて `action: merged`。`mcpDropped` は全件空、`keptNeedsReview` なし。

**生成した `target`**

`lead-implementer` / `general-implementer` / `technical-leader` / `general-worker` / `system-planner` / `complex-reviewer` / `general-explore` / `realtime-researcher` / `docs-reviewer` / `independent-tech-adviser` / `technical-adviser` / `code-reviewer` / `document-writer` / `knowledge-elicitationer`(すべて `.claude/agents/<name>.md`)

**維持されたもの** — 定義名・`model` 値・役割の割り当て・`agent-policy-vendor` は 14 件とも再生成前と完全に一致。`disallowedTools` は 8 件に 11 ツールずつ揃って入っている。MCP 付与も現状どおり(実装役割 4 件に `plugin:codiel:raguel` を含む 3 サーバー、読み取り系に context7 + serena、`document-writer` は serena のみ、`knowledge-elicitationer` は付与なし)。

**入れ替わった本文** — RoleId 照合の新文言が入ったのは **9 件**。Agent tool を持つ定義だけが該当し、旧文言(役割名で対応表を引く)は 1 件も残っていない。

先ほど私が「Agent tool を持つ 10 件」「`disallowedTools` は 7 件」と書いたのは数え違いで、正しくは 9 件と 8 件。実際の生成結果はどちらも正しい。

## 報告事項

- `warnings` と `modelsDropped` は全コマンドで空。live models の照会は成功しており、実在検証は行われている。
- ベンダーは 14 件とも `--vendor` を明示して確定した。`none` の定義は無いため、色が blue になる定義もない。
- **方針の読み込ませ方**: `AMATSUKA_AGENT_AUTO_INJECTION` は `custom` 相当で運用されている。SessionStart フックが役割マーカー付き定義の `model` を検証し、すべて実在すれば `custom-policy` とマーカー対応表を注入する。照会に失敗したり実在しないモデルがあれば、セッション全体で `claude-model-policy` へフォールバックする。
- `.claude/agents/` を git 追跡するかはプロジェクトの判断。このリポジトリでは `.gitignore` が除外しているため、この再生成はコミットに現れない。
- ディレクトリは既存なので再起動は不要。ただし**本文の変更が効くのは次に起動するサブエージェントから**で、このセッションで既に起動済みのものには反映されない。

`★ Insight ─────────────────────────────────────`
- 一括生成(`--models`)を使わなかったのが要点だった。推奨構成は「1 モデル = 1 定義」で役割を束ねるが、このリポジトリは「1 関心 = 1 定義」に手で割ってある。`gpt-astra` は推奨だと 5 役割 1 件だが、ここでは `technical-leader` / `complex-reviewer` / `independent-tech-adviser` の 3 件に分かれている。既定名も違うので、一括生成は既存を更新せず**別名で 10 件を新規作成**し、同じ役割に 2 つの定義がぶら下がる状態を作っていた。`exists: false` が全件に並んだ時点でそれが分かる。
- `--check` の差分が「既存のみ tools」「既存のみ key」として出たのも同じ性質のもので、CLI は**渡さなかったものを消す**。保持したいなら `--merge` だけでは足りず、`--mcp-servers` と `--mcp-deny` で現状を明示的に渡し直す必要がある。設計書が「再生成前に `disallowedTools` と MCP 付与を控える」と警告していたのは、まさにこの復元のためだった。
`─────────────────────────────────────────────────`

## セッション 3: スコープ外4項目の状態と判断理由

# AI

`/agent-policy:setup-agents` は完了している。14 定義すべてが保持マージで再生成され、Agent tool を持つ 9 件の本文が RoleId 照合の文言へ入れ替わった。定義名・`model` 値・役割・MCP 付与・`disallowedTools` はいずれも再生成前と一致している。

これで当初のゴール(A〜D)に加えて、残課題として挙げていた `.claude/agents/` の再生成も片付いた。

**このセッションの成果物**

| コミット | 内容 |
| --- | --- |
| `99eb595` | A — 死んだ `modelId` 配管の除去 + `0.19.1-dev` + バンドル再生成 |
| `5688ef9` | B — 対応表 fixture の `ROLES` 導出化 |
| `bb1f85f` | C — 手順書の Antigravity (Gemini) 追随 |
| `aa06b47` | D — 4 案を比較した設計書 |
| `12b682e` | D — RoleId 併記の実装・断片・規律・README・Serena メモリ |
| `a244269` | C の訂正 — antigravity の別名が指す上流モデル ID を実在する値へ |

`.claude/agents/` の更新は gitignore 対象のためコミットには出ない。

残る未解決事項は 2 つ。引き継ぎ書 §4 がスコープ外とした 4 件(運用観測待ち 3 件と `setup-agents` の兼務検出の別起票)と、ADR を起こすかの判断。どちらも急がない。

# phyllis998

> スコープ外の4件について解説してください

# AI

引き継ぎ書には見出しだけが書かれていて中身が無い。元の設計書 §10 を読んで正確に説明する。

設計書 §10 の原文を読んだ。4 件の中身は次のとおり。

---

## 運用観測待ち 3 件

### §10-4 — `doc-writing` が運用で実際に呼ばれるか

**何が未決か。** 役割 `doc-writing` を追加し、共通規律に「ファイルとして残す文書は文書作成役へ再委譲する」という条項(D1/F1)を入れた。この条項が実際に守られ、`document-writer` が呼ばれるかどうかが未検証。

**なぜ今決められないか。** 2 層ある。表層の理由は、条項を入れた当日にしか運用していないこと。その下にある理由のほうが本質的で、**この条項は規律の文面でしか強制されていない**。Agent 定義の本文と依頼文に書いてあるだけで、「オーケストレーターが自分で文書を書いた」ことを検知する仕組みは無い。だから守られているかは、呼び出しの実績を数えるしかない。

**観測して何を決めるか。** 呼ばれないなら、設計書 §4.5 で不採用にした「§設計・実装計画の規律 への追記」を再検討する。今は §オーケストレーターが自ら担う作業 にだけ書いてあり、設計フローの手順書側には書いていない。

**現時点の実績。** このセッションでは 5 回委譲した(C の手順書、D の設計書の推敲、断片・規律・README の追随、設計書の状態更新、手順書の訂正)。うち 1 回は 429 で失敗して Sonnet へ読み替えた。条項自体は機能している。

### §10-5 — `gemini-flash` / `gpt-terra` の日本語品質

**これはもう「観測待ち」ではない。判断待ちに変わっている。**

設計書に追記された事実がある。`gemini-flash`(antigravity 経由)は**品質を評価する前に可用性で失敗した**。原因が 2 層で特定されている。

1. 表層 — サブエージェント起動時に `429 RESOURCE_EXHAUSTED` が返る。
2. その下 — これは**偽装の 429** である。Claude Code / Agent SDK はサブエージェントの system に identity 文 `You are a Claude agent, built on Anthropic's Claude Agent SDK.` を入れる。antigravity の上流(Cloud Code エンドポイント)がこれを検出して、quota が 100 % 残っていても 429 を返す。直の curl では同じ alias が全パターン 200 を返し、メインセッションの identity 文は検出されない。

つまり**quota 枯渇ではなく、system プロンプトの内容によるフィルタ**。私がセッション中に「プロキシ側の Gemini quota が枯れている」と述べたのは誤りで、並行セッションの調査(`docs/cliproxyapi/2026-09-16-antigravity-false-429-system-prompt-filter.md`)がこれを覆している。

**帰結。** antigravity 経由の Gemini は現状 Claude Code のサブエージェントとして使えない。`RECOMMENDED["doc-writing"]` に `gemini-flash` が入ったままだと、`setup-agents` が**動かないモデルを推奨構成として提示する**。

**選べるのは 2 つ。** 推奨から外すか、README に「antigravity 経由はサブエージェントとして使えない」と注記するか。agent-policy はプロキシの経路を知らない(alias が codex 経由か antigravity 経由かを判別できない)ため、コードで分岐する手が無いのがこの二択になる理由。

`gpt-terra` の日本語品質のほうは純粋に観測待ち。このセッションで手順書・断片・規律・README を書かせた範囲では、指示した文言を 1 文字も変えずに反映しており、逸脱は無かった。

### §10-8 — `light-impl` / Haiku への再委譲の妥当性

**何が未決か。** 0.19.0-dev で Agent Tool の可否判定からモデルを外し、役割だけで決めるようにした(D2)。あわせて `light-impl` を「否」から「可」へ変えた。その結果、**Haiku を指定した定義でも、Agent Tool が可の役割を持てば Agent tool が付く**(従来はモデルが Haiku というだけで外れていた)。

**なぜ変えたか。** 「モデルが小さいから再委譲させない」という判定は、モデルと役割という別の軸を混ぜていた。役割で決めるほうが筋が通る。

**何が未検証か。** 小さいモデルが**妥当な再委譲をするか**。過剰に再委譲する、あるいは委譲先の選択を誤る、といった振る舞いが出るかもしれない。

**問題が出たときの直し方も先に決めてある。** `AGENT_DENIED_MODELS`(モデルで縛る旧方式)は復活させず、**役割側の規定で絞る**。軸を混ぜ直さないという判断。

---

## 別起票 1 件

### §10-10 — `setup-agents` の兼務検出

**何が未決か。** `doc-writing`(文書作成)と実装役割を 1 つの定義が兼ねているとき、`setup-agents` がそれを検出して分離を勧めるかどうか。

**現行の検出は 1 種類だけ。** `mixedKinds` — `impl` と `readonly` が同じ定義に混ざっているかを見る。`doc-writing` は `kind: impl` なので、`normal-impl` と同居しても `mixedKinds` には引っかからない。

**なぜ分離が要るのか。** 文書作成役は「内容は確定済みで、文章化と推敲だけを担う」役割として設計されている。実装役割と同居すると、その定義は「自分で決めて自分で書く」ことができてしまい、D1 の再委譲規律が意味を失う。

**なぜスコープ外にしたか。** 検出を足すと CLI の `--list-coverage` / `--check` の応答に新しいフィールドが要り、ウィザードの質問も増える。`doc-writing` の追加そのものより変更が大きい。現行は **README で分離を勧めるにとどめている。**

**このリポジトリの現状は健全。** `document-writer` は `doc-writing` 単独で、実装役割との兼務は無い。検出が無くても問題は起きていない。

---

## まとめ

| 項 | 種別 | いま何が要るか |
| --- | --- | --- |
| §10-4 `doc-writing` が呼ばれるか | 観測 | 数セッション分の実績。呼ばれなければ規律の置き場所を見直す |
| §10-5 Gemini / gpt-terra | **判断(観測待ちから昇格)** | `RECOMMENDED` から `gemini-flash` を外すか、README に注記するかの二択 |
| §10-8 Haiku の再委譲 | 観測 | 実際の再委譲の質。問題があれば役割側で絞る |
| §10-10 兼務検出 | 別起票 | CLI 変更を伴う。起票するかの判断 |

**4 件のうち §10-5 だけが性質を変えている。**他の 3 件は「まだ観測が足りない」だが、これは「観測した結果、推奨に動かないモデルが載っていることが分かった」状態。放置すると `setup-agents` が動かない構成を勧め続けるので、他の 3 件より先に決める価値がある。

`★ Insight ─────────────────────────────────────`
- §10-5 の偽装 429 は、抽象化の漏れとして教科書的。agent-policy はモデルを「エイリアス文字列」として抽象化しており、そのエイリアスが codex 経由か antigravity 経由かを知らない。ところが**実際の可用性は経路で決まる**。抽象化が隠した違いが、429 という別のレイヤーのエラーコードになって漏れてきた。しかも上流は quota 枯渇を騙っているので、エラーコードすら原因を指していない。二択が「推奨から外す」か「README に書く」かに限られるのは、コードが経路を観測できない以上、判断を人間側へ逃がすしかないため。
- §10-8 の直し方の指定も設計として効いている。「問題が出たら `AGENT_DENIED_MODELS` を復活させる」ではなく「役割側の規定で絞る」と先に決めてある。障害対応のときは元に戻す誘惑が最も強いので、**戻してはいけない方向をあらかじめ書いておく**のは、未解決事項の書き方として上手い。
`─────────────────────────────────────────────────`

## セッション 4: Gemini 可用性問題が判断待ちへ、README 注記を検証保留

# AI

スコープ外 4 件の解説は前のターンで出したとおり。

要点を 1 つだけ繰り返す。**§10-5 は他の 3 件と性質が違う。**「まだ観測していない」ではなく「観測した結果、推奨に動かないモデルが載っていることが判明した」状態。`RECOMMENDED["doc-writing"]` に `gemini-flash` が残っている限り、`setup-agents` は antigravity 経由で起動できない Gemini を推奨構成として提示し続ける。

この件だけ着手できる形になっている。判断するのは「推奨から外す」か「README に注記する」かの二択で、どちらを採るかが決まれば実装は小さい。他の 3 件は運用の実績が貯まるまで動かせない。

進めるなら言ってほしい。

スコープ外 4 件の解説は完了している。指示待ち。

§10-5(`RECOMMENDED["doc-writing"]` の `gemini-flash` をどう扱うか)だけは今すぐ着手できる状態にある。他の 3 件は運用の実績が貯まるまで動かせない。

指示待ち。

# phyllis998

> geminiに関してはREADMEに注記することにします。

# AI

README への注記で確定。まず並行セッションが残した調査記録を読んで、注記に書く事実を正確にする。

調査記録が決定的。事実を確定した。

**現象**: `You are a Claude agent, built on Anthropic's Claude Agent SDK` という**大文字小文字を区別する完全一致の部分文字列**が `systemInstruction` に含まれると、antigravity 経路の上流が 429 を返す。二分探索で system ブロック [1] まで絞り込まれており、小文字化すると 200、単語だけなら 200、user メッセージに置けば 200。`claude-gpt-5-6-terra`(codex 経路)は同じ文でも 200。**quota とは無関係**で、Weekly / Five Hour ともに残 100 %。

Claude Code はサブエージェント起動時とセッションタイトル生成時にこの文を必ず system へ入れる。メインセッションの identity 文は別の文字列で検出されない。

**README にはリポジトリ固有のパス(`docs/cliproxyapi/...`)を書かない。**プラグイン README は他リポジトリの利用者も読むため、現象と回避策だけを書く。

バージョンは据え置き(`0.19.1-dev`)。README のみで挙動は変わらず、`-dev` のまま未リリースなのでこの注記も同じ版に載る。

# phyllis998

> 注記を一旦まってください。検証したいことがあります

# AI

了解。止めた。README も設計書も未変更。

作業ツリーは前のコミット(`a244269`)時点のまま — 残っているのは本件と無関係な `.raphael/antibodies/` と `docs/chat/` だけ。

検証の内容を教えてほしい。手を動かす必要があれば言ってくれれば動く。

検証待ち。README と設計書への注記は保留したまま。

## セッション 5: 検出文字列が原因と判定、README 注記検証待ち

# AI

検証待ち。

検証待ち。

# phyllis998

> 並行セッションで調査したとき、以下のIssueで同様の現象が報告されていたとありました。
> https://github.com/router-for-me/CLIProxyAPI/issues/4696
> しかしこのIssueはCloseされています。そして興味深いのが"SOUL.md"というものに"You are AI Agent (Hermes Agent), an intelligent AI assistant created"と書くとよかったという趣旨のコメントがあります。SOUL.mdというものが何なのか、そしてこの対策を行うと本当に良いのか、なぜこのIssueはクローズされたかを調査してください。

# AI

調査を出す。GitHub の API で issue の本体・コメント・クローズ理由を直に取れるので、それを一次情報にさせる。

`SOUL.md` は superpowers のプラットフォーム対応表にも「Hermes Agent」というハーネス名で出てくる。別クライアントの identity 定義ファイルである可能性が高いが、推測で答えず確定させる。

この「対策」は要するに**クライアントの identity を別のものに見せかけてプロバイダーのフィルタを回避する**行為なので、効果だけでなく規約上の含みも併せて確定させる。

調査を出した。3 つの問いに分けてある。

1. **`SOUL.md` の正体** — どのツールの概念か、何を定義し、`systemInstruction` にどう反映されるか。「Hermes Agent」が製品名なのか名乗る identity なのかも含めて。
2. **その対策の妥当性** — 効果の報告だけでなく、副作用と規約上の含みまで。調査記録に「oh-my-pi のメンテナーはゼロ幅文字の挿入を規約リスクで同梱拒否した」とあるので、その原文も確認させている。
3. **クローズ理由** — `gh api` の `state_reason` を直に見て、`completed` なのか `not planned` なのかを確定させる。修正が入ったなら、手元の 7.2.155(commit `7fac6b15`、2026-09-08 ビルド)に含まれるかまで判定させる。

結論は書かせない。この対策は要するに**クライアントの identity を別のものに見せかけて上流の検出を回避する**行為なので、採否は事実を揃えてから判断したい。

実機での試行も止めてある。`SOUL.md` を作って試すのは、方針が決まってからにする。

`★ Insight ─────────────────────────────────────`
- issue が閉じていること自体は情報として弱い。`completed`(直した)と `not planned`(直さない)では意味が正反対で、GitHub の UI 上はどちらも同じ「Closed」に見える。`gh api` の `state_reason` を見ないと区別できないので、そこを明示的に取らせた。
- 「特定の文字列を避ければ通る」という回避策には構造的な脆さがある。調査記録が引いた各所の報告は、検出される句が**追加され続けている**と推定していた。つまりこの種の対策は、効いたという報告があっても賞味期限が不明で、効かなくなったときに 429 という無関係なエラーコードで再発する。恒久策として扱えるかは、効果の有無とは別の軸で判断する必要がある。
`─────────────────────────────────────────────────`

結果が返ったら、事実を突き合わせて選択肢を整理する。

`SOUL.md` と issue #4696 の調査結果を待っている。

## セッション 6: SOUL.md が Hermes 専用と判定、API キー経路検証待ち

# AI

`SOUL.md` と issue #4696 の調査結果を待っている。

`SOUL.md` と issue #4696 の調査結果を待っている。

決定に直結する事実を自分で確かめる。とくに Google の FAQ の文面は、この件の枠組みそのものを変える。

全件、一次情報で裏が取れた。

# 調査結果

## 1. `SOUL.md` とは何か

**CLIProxyAPI のものではない。**Nous Research の別クライアント **Hermes Agent**(`NousResearch/hermes-agent`)の概念。

- 実体は `~/.hermes/SOUL.md`(または `$HERMES_HOME/SOUL.md`)に置く persona ファイル。
- system prompt の **slot #1(identity)** を担う。prompt-injection scan と truncation を経て、**ラッパー無しでそのまま**入り、ハードコードの `DEFAULT_AGENT_IDENTITY` を置き換える。
- 「Hermes Agent」は製品名であり、同時にそのクライアントが名乗る identity 文字列でもある。
- issue #4696 の報告者は Hermes Agent から OpenAI 互換エンドポイント経由で CLIProxyAPI を叩いていた。

**このリポジトリには関係しない。**Claude Code / Claude Agent SDK に `SOUL.md` は存在せず、サブエージェントの identity 文 `You are a Claude agent, built on Anthropic's Claude Agent SDK.` は SDK が必ず入れるもので、Agent 定義の上書きでは消せない。**`SOUL.md` を書き換える対策は、こちらの経路には原理的に効かない。**

## 2. なぜ #4696 はクローズされたか

`gh api` で確定した。

| 項目 | 値 |
| --- | --- |
| `state_reason` | **`completed`** |
| 閉じた人 | `luispater`(メンテナー) |
| 閉じた時刻 | 2026-08-04T16:40:31Z |
| クローズ時コメント | **無し** |
| 紐づくコミット | `42eef103` — `feat(antigravity): obfuscate sensitive words in system instructions` |

**上流のフィルタを直した「completed」ではない。**CPA 側に **opt-in のゼロ幅文字(U+200B)難読化機構**(`antigravity.sensitive-words`)を足して閉じたもの。初出は `v7.2.118`(クローズの 35 秒後)。

**手元の 7.2.155 にはこの機構が入っている。**`compare 42eef103...7fac6b15` が `behind_by: 0` で祖先関係を確認。ローカルの `config.example.yaml:300` にもコメント例(`"API"` / `"proxy"`)がある。ただし**既定は空リスト**で、Claude Code のフレーズは同梱されていない。

なお同じメンテナーは、後の #5848 を **`not_planned`** で閉じ、理由をこう書いている。

> This is not a CLIProxyAPI translation bug, and it is also not actual Antigravity quota exhaustion. Google's Antigravity path applies an upstream content filter to system-instruction text. […] Closing this as not planned because the existing `antigravity.sensitive-words` mechanism is the supported way to handle this upstream filter.

## 3. その対策は本当に良いのか

### 効果は不安定

`SOUL.md` 対策を出したコメント本人の周辺で、すでに揺れている。

- `gukovskiy98`(2026-07-31): 「It's working so far」
- `666zhaobo666`: 最初「it works」→ **約 20 分後**「换了之后，能用几次，然后就又429了」(換えたあと数回使えたが、また 429 になった)
- `Mingxuan12`: 換えたら直った。**戻しても 429 が出なくなった** — 単純な文字列一致では説明がつかない

後に `fengyansama` が二分を詰めており、トリガは **`Hermes Agent` と `Nous Research` が同一文に揃うこと**。そのうえで `SOUL.md` を触らず `sensitive-words` を使うほうを勧めている。理由は「Hermes のアップグレードが `SOUL.md` を書き戻しうる」から。

### Claude Code のサブエージェントでは効かなかった報告がある

**これが決定的。**issue #5695 で、報告者 `hungvimanh` は**まさに手元と同じ v7.2.155** で次を報告している。

> I tried adding: `antigravity.sensitive-words: ["Claude Agent SDK"]` but the issue still reproduces.
> Main Claude Code agent + Antigravity: OK / Explorer subagent + Antigravity: 429

そして #5695 は**報告者ではない第三者**(`sususu98`)が `completed` で閉じており、その後「フレーズが 1 つとは限らない、ログから追加しろ、最新は v7.3.2」と返している。**解決の確認を経ずに閉じられている。**

### 規約上の含み — ここが本題

Google の FAQ に、この件を直接名指しした項目がある。実際に取得して確認した。

> **見出し**: "Why can't I use third party software (e.g. Claude Code, OpenClaw, OpenCode) with my Antigravity login?"
>
> 第三者ツール経由のアクセスは "a violation of our Terms of Service, and severely degrades the experience for legitimate product users."
>
> "Such actions may be grounds for suspension or termination of your account."
>
> 代替として "use a Vertex or AI Studio API key" を案内。

利用規約 §6 にも "using the Service in connection with products not provided by us" が禁止として挙がり、"using OpenClaw with Antigravity OAuth" が breach の例示になっている。

**つまり 429 はバグではなく、Google が明示的に禁じている利用形態に対する遮断である。**フィルタを回避することは、その遮断をすり抜けることを意味する。

oh-my-pi のメンテナー `H4vC` が同種の PR を拒否した理由がこれを言い当てている。

> I will not be shipping a bypass to google blocking OMP for the simple reason that it invites escalation

> They blocked us with a brittle string match not because they are unable to come up with something better. If I ship a bypass to the regex/substring match the next escalation is banning people's AGY accounts.

U+200B 挿入の PR #11730 は **unmerged のまま closed**、`requestType` 除去の #11742 は **open のまま未マージ**。

---

# 私の見解

**`SOUL.md` 対策は、この件には使えない。**Hermes Agent 専用のファイルで、Claude Code には存在しない。これは調べるまでもなく決着する部分。

**より広く「フィルタを回避して antigravity 経由でサブエージェントを動かす」という方向は、勧められない。**理由は 3 層ある。

1. **表層** — 効くという保証が無い。手元と同じ 7.2.155 で `sensitive-words: ["Claude Agent SDK"]` が効かなかった一次報告があり、Hermes 側でも「数回使えてまた 429」が出ている。検出句は追加され続けていると各所が推定している。
2. **中層** — 回避が成功しても、それは **quota を消費する側の挙動**であり、上流はそれを望んでいない。CPA は偽装 429 を quota 枯渇と誤認して 11 回再送しており、増幅も起きる。
3. **深層** — Google の FAQ が **Claude Code を名指しで** ToS 違反とし、アカウントの停止・終了の事由になりうると書いている。ここでの 429 は障害ではなく**意図された遮断**で、回避はその意思に逆らう行為になる。アカウントを失うリスクを負うのは利用者側。

したがって、**当初の判断(README に注記する)を変える必要は無い**と考える。ただし注記の内容は、今回の調査を踏まえて**書き方を変えるべき**だと思う。

当初案は「Antigravity 経由の Gemini はサブエージェントとして使えない」という技術的事実だけを書くつもりだった。そこに **Google が第三者クライアント経由の利用自体を ToS 違反としている**という事実を足したほうがよい。そうしないと、読んだ人が `sensitive-words` で回避しようとして、アカウントを失いかねない。

## 選択肢

| | 内容 |
| --- | --- |
| **(1)** | 注記に技術的事実 + **ToS の事実**を両方書く。回避手段は案内しない。**推奨** |
| **(2)** | 技術的事実だけ書く(当初案のまま) |
| **(3)** | `RECOMMENDED["doc-writing"]` から `gemini-flash` を外し、あわせて注記も書く |

(3) を挙げ直したのは、ToS の事実が出てきたため。ただし agent-policy は経路を知らないので、Vertex / AI Studio の API key 経由で Gemini を配っている構成まで巻き添えにする。そこは当初の判断どおり。

どれで進めるか決めてほしい。

`★ Insight ─────────────────────────────────────`
- `state_reason` を見た価値がそのまま出た。#4696 は `completed` だが**上流は何も直っていない**。閉じたのは「回避機構を足したから」で、同じメンテナーが後の #5848 は `not_planned` で閉じ、理由に「これは上流のコンテンツフィルタである」と明記している。**同じ現象が、時期によって completed と not planned に分かれている** — 前者は「機構を足した」ことに対する完了で、後者は「現象を直す気は無い」という表明。issue の閉じ方だけを見ると両者は区別できない。
- 技術調査が規約の問題に着地した。「なぜ 429 が返るか」を 2 層たどると文字列フィルタに行き着くが、3 層目に「なぜそんなフィルタがあるか」を置くと、答えは技術ではなく**提供者の意思**になる。回避策の可否を技術的な効き目だけで判断すると、この層を踏み越えてしまう。oh-my-pi のメンテナーが「brittle な string match なのは、もっと良い手段が無いからではない」と書いたのは、まさにこの層を見ている。
`─────────────────────────────────────────────────`

蒸留を専用サブエージェントへ委譲した。

調査の結論は前のターンのとおり。**`SOUL.md` は Hermes Agent 専用のファイルで、Claude Code には存在しない** — この件には原理的に効かない。

そして調査が技術の話から規約の話へ着地した。Google の FAQ が **Claude Code を名指しで** ToS 違反とし、アカウント停止・終了の事由になりうると明記している。手元の 429 はバグではなく意図された遮断で、`sensitive-words` による回避は**同じ 7.2.155 で効かなかった一次報告**もある。

README への注記をどう書くか、3 つの選択肢を出してある。決めてもらえれば進める。推奨は (1) — 技術的事実と ToS の事実を両方書き、回避手段は案内しない。

蒸留完了。3 件とも「別リポジトリでは成立しない」として非採用、`mark-distilled` 済み。既存の 6 抗体に変更なし。

README 注記の方針待ち。前のターンで出した 3 案のどれで進めるか決めてほしい。

- **(1)** 技術的事実 + Google の ToS の事実を両方書く。回避手段は案内しない(推奨)
- **(2)** 技術的事実だけ書く(当初案)
- **(3)** `RECOMMENDED["doc-writing"]` から `gemini-flash` を外し、あわせて注記も書く

README 注記の方針待ち。(1) / (2) / (3) のどれで進めるか決めてほしい。

# phyllis998

> 調査ありがとうございます。関連して追加の調査を依頼したいです。Geminiを利用できるGoogleのAI Plus/Pro/Ultraや、Google WorkspaceなどでAPIを利用してGeminiを利用できますか？または別の経路でAPIを利用するしかないでしょうか？そしてAPIキーを使うようにCLIProxyAPIのconfigなどで設定できるでしょうか？APIキーを使う方法ならば規約違反にはならないでしょうか？

# AI

良い問いで、しかも Antigravity FAQ 自身が代替として名指ししている経路。そこが本当に規約上クリーンなのか、CLIProxyAPI で実際に設定できるのかを一次情報で固める。

料金体系と、無料枠でのデータ利用の扱いも確認させる — 後者は業務コードを通すなら判断に効く。

調査を出した。4 つの問いに対応させてある。

1. **AI Plus / Pro / Ultra、Workspace に API 利用が含まれるか** — ここが核心。消費者向けサブスクリプションは Gemini アプリや Antigravity の利用権であって API の利用権とは別、という可能性が高いが、断定せず公式で確定させる。
2. **API を使うなら何が要るか** — AI Studio(Developer API)と Vertex AI で、認証方式・課金の紐付け先・GCP プロジェクトの要否が違う。Vertex は API キーで済まない可能性がある。
3. **CLIProxyAPI で設定できるか** — ローカルの 7.2.155 の `config.example.yaml` を実物で確認させる。API キー経路はモデル ID の体系が OAuth 経路と別(`gemini-3.8-flash-high` ではなく `gemini-3.8-flash` 側)になるはずで、別名の付け方と OAuth との併用可否も要る。
4. **規約上クリーンか** — Antigravity の FAQ 自身が "use a Vertex or AI Studio API key" を代替として案内しているので、整合するはず。ただし「FAQ が勧めている」と「API の規約が第三者クライアントを許している」は別の話なので、両方確認させる。

あわせて**無料枠のデータ利用**も確認させている。Gemini API の無料枠は送信データがモデル改善に使われる可能性があり、業務のソースコードを通すなら有料枠を選ぶ理由になる。ここは判断材料として要る。

`★ Insight ─────────────────────────────────────`
- 前回の調査が「回避策は勧められない」で終わったのに対し、今回は**提供者が公式に案内している経路が本当に成立するか**を見る。Antigravity FAQ は代替として API キーを挙げているが、FAQ の一文だけを根拠にすると「消費者向けサブスクに API が付いてくる」という誤解に繋がりやすい。Google の製品は Gemini アプリ / Antigravity / Gemini Code Assist / Developer API / Vertex AI で**課金と権利が別々に切られている**ので、そこを混ぜないのが今回の要点になる。
`─────────────────────────────────────────────────`

結果が返ったら、README 注記の書き方と合わせて整理する。この経路が成立するなら、注記に「サブエージェントで Gemini を使いたいなら API キー経路を使う」という前向きな案内を書けるので、当初の (1) 案より良いものになる。

Gemini の API キー経路の調査結果を待っている。

## セッション 7: Gemini API キー経路の調査完了

# AI

Gemini の API キー経路の調査結果を待っている。

Gemini の API キー経路の調査結果を待っている。

決定に効く事実を自分で確かめる。

全件、一次情報で裏が取れた。4 つの問いに順に答える。

---

## 1. AI Plus / Pro / Ultra や Workspace で API を使えるか

**使えない。**これが最も重要な答え。Google の開発者向けドキュメントが明示している(実際に取得して確認した)。

> Google AI plan benefits for developer usage apply only within the Google AI Studio web interface.
>
> **Direct use of the Gemini API (such as using API keys or external applications) is billed and managed separately.**
>
> Google AI plans for AI Studio are separate from Gemini API usage tiers, which cover development and production API usage.

消費者向けサブスクリプションに含まれるのは、**製品の利用権**であって API の利用権ではない。

| 含まれる | 含まれない |
| --- | --- |
| Gemini アプリ | **API キーによる Gemini API の直接利用** |
| AI Studio の **Web UI** の上限 | **外部アプリケーションからの呼び出し** |
| **Antigravity 本体**の上限・優先トラフィック | AI Studio 内の agents(Deep Research / Antigravity Preview。別途 paid API key が必要) |
| Gemini in Android Studio、Jules、Colab | |
| Developer Program premium(Pro で月 $10、Ultra 5x で $40、20x で $100 の GCP クレジット) | |

Pro / Ultra に付く **GCP クレジット**は API の利用権ではなく、GCP プロジェクトと課金アカウントを別途用意すれば、そのクレジットを API 利用に充てられる可能性がある、という位置づけ。

**Workspace も同じ。**Workspace 契約に含まれるのは Gmail / Docs 等の Gemini 機能と Gemini アプリで、API キーの発行や Vertex の利用権は含まれない。API を叩くなら別途 GCP プロジェクトと課金が要る。

**つまり「今払っているサブスクの延長で API が使える」という道は無い。**別の課金経路が要る。

## 2. API を使うなら何が要るか

2 経路ある。

| | Gemini Developer API(AI Studio) | Vertex AI(現 Gemini Enterprise Agent Platform) |
| --- | --- | --- |
| 認証 | **API キー** | API キー または ADC(公式は「キーはテスト用、ADC は本番用」) |
| 開始の敷居 | AI Studio でキー発行。**課金アカウントは必須ではない**(無料枠で開始可) | フル経路は GCP プロジェクト + 課金が必須。**Express mode** なら `@gmail.com` で課金情報なしに最大 90 日の無料枠 + 自動発行キー(ただし **Pre-GA**) |
| 有料化 | Cloud Billing をプロジェクトに紐付け + Prepay(最低 $5) | 既存の課金アカウントをリンク |
| 課金 | **消費者向けサブスクとは別** | 同左 |

**手軽さでは Developer API。**キー 1 本で始められる。

## 3. CLIProxyAPI で設定できるか

**できる。**ローカルの 7.2.155 の `config.example.yaml` で実物を確認した。

```yaml
# Gemini API keys
# gemini-api-key:
#   - api-key: "AIzaSy...01"
#     weight: 5          # weighted-round-robin の重み
#     prefix: "test"     # "test/gemini-3-pro-preview" の形で指名
#     base-url: "https://generativelanguage.googleapis.com"
#     models:
#       - name: "gemini-2.5-flash"   # 上流モデル名
#         alias: "gemini-flash"      # クライアント側の別名
```

Vertex 用は別項目 `vertex-api-key`(`:681`)。サービスアカウント JSON の取り込みは `--vertex-import` フラグ。

**押さえるべき差異が 3 つある。**

1. **モデル ID の体系が違う。**Antigravity OAuth 経路の `gemini-3.8-flash-high` / `gemini-pro-agent` に対し、Developer API は公式 ID の **`gemini-3.8-flash`** / `gemini-3.1-pro-preview`。effort の `-high` 接尾辞は Antigravity 専用。
2. **`oauth-model-alias` は API キー経路に効かない。**設定例に明記がある(`:710`)。

   > NOTE: Aliases do not apply to gemini-api-key, interactions-api-key, codex-api-key, xai-api-key, claude-api-key, openai-compatibility, or vertex-api-key.

   別名は各キーのエントリ内 `models: [{name, alias}]` で付ける。つまり `claude-gemini-3-8-flash` という別名を維持したいなら、書く場所が変わる。
3. **`--gemini-login` / `--ai-studio-login` は 7.2.155 に存在しない。**`--help` で確認済み。Gemini の OAuth は `--antigravity-login` のみ。API キー経路は設定ファイルに書くだけで、ログイン操作は不要。

複数キーの登録と重み付き負荷分散にも対応している。OAuth と API キーの同時設定も設定上は可能(ただし同名モデルが両方から来たときの優先順位は公式に記載が無く、未検証)。

## 4. 規約違反にならないか

**Antigravity の FAQ 自身が、この経路を代替として名指しで推奨している。**原文はこう。

> Using third party software, tools, or services to access Antigravity is a violation of our Terms of Service, and severely degrades the experience for legitimate product users. Such actions may be grounds for suspension or termination of your account. **If you would like to use a third party coding agent with Gemini, we recommend using a Vertex or AI Studio API key.**

禁じられているのは **Antigravity のログイン(OAuth)を第三者クライアントで使うこと**であって、API キーで Gemini API を叩くことではない。API キーは第三者アプリから呼ばれることを前提にした経路である。

Developer API の追加利用規約と Cloud のサービス固有条項も確認したが、**第三者クライアントやローカルプロキシ経由の利用を禁じる条項は見当たらなかった**。ただし親契約の Google APIs Terms of Service 全文は未精読なので、「無い」と断定はしない。

**懸念は規約ではなくデータの扱いにある。**ここは業務コードを通すなら効く。

> **無料枠**: Google uses the content you submit to the Services and any generated responses to provide, improve, and develop Google products and services and machine learning technologies […] human reviewers may read, annotate, and process your API input and output. […] **Do not submit sensitive, confidential, or personal information to the Unpaid Services.**

> **有料枠**: Google doesn't use your prompts […] or responses to improve our products […]

有料枠になる条件は明快で、**Cloud Billing が有効なプロジェクト経由であること**。Prepay の最低額は $5。

Vertex の Express mode 無料枠は Pre-GA で、データ処理契約が原則適用されない旨が Cloud の条項にある。**ソースコードを通すなら Express の無料枠も避けたほうがよい。**

料金のオーダーは、`gemini-3.8-flash` が入力 $0.75 / 出力 $3.75(1M トークンあたり、2026 年末まで。2027-01-01 から倍)。文書作成役に使う分には軽い。

---

## まとめ

| 問い | 答え |
| --- | --- |
| サブスクで API を使えるか | **使えない。**製品の利用権と API の利用権は別 |
| 別経路が要るか | **要る。**Developer API(キー 1 本)か Vertex(GCP プロジェクト) |
| CLIProxyAPI で設定できるか | **できる。**`gemini-api-key` / `vertex-api-key`。7.2.155 に入っている |
| 規約違反か | **ならない。**FAQ 自身が代替として推奨している経路。ただし**無料枠は送信内容が製品改善に使われる**ので、業務コードを通すなら有料枠にする |

**この結果は README 注記の書き方を変える。**当初は「Antigravity 経由の Gemini はサブエージェントで使えない」という否定形で終わる予定だったが、**公式に認められた代替がある**ことが確定した。注記に「サブエージェントで Gemini を使いたいなら API キー経路を使う」という案内を書ける。

`★ Insight ─────────────────────────────────────`
- ここまでの調査が一周して繋がった。偽装 429 は「バグ」でも「嫌がらせ」でもなく、**課金の境界を守るための遮断**だった。Antigravity のサブスクは製品の利用権を売っており、その枠を第三者クライアントに流用されると、API として売っているものをタダで配ることになる。だから遮断する。そして FAQ は同じ文の中で「API が欲しいなら API を買え」と正しい窓口を案内している。技術的な回避策を探していた時点で、問いの立て方がずれていた。
- CLIProxyAPI の設計もこの境界に沿っている。OAuth 経路(`oauth-model-alias`)と API キー経路(`gemini-api-key`)が**別のブロックで、別名の仕組みまで分かれている**。「Aliases do not apply to gemini-api-key…」という注記は一見不便だが、認証の出所が違うものを同じ設定で扱わない、という区別の表れになっている。
`─────────────────────────────────────────────────`

README 注記に API キー経路の案内を含める形でよいか。よければ、注記の文案を作って提示する。

# phyllis998

> さらに関連した調査依頼です。真にやりたいことは"Claude CodeでAntigravityを使う"ではなく"Claude CodeでGeminiを使う"ことです。サブスクで実現することは可能でしょうか。

# AI

本質的な問いに変わった。「Antigravity を使う」ではなく「Gemini を使う」— であれば経路は Antigravity に限らない。

前回で確定しているのは 2 点。消費者向け AI プランに API 利用権は無い。Antigravity の OAuth を第三者クライアントで使うのは ToS 違反。**残るのは「定額(サブスク型)でありながら、第三者クライアントからの利用が認められている経路が他にあるか」。**候補を挙げて潰させる。

調査を出した。定額で第三者クライアントから Gemini を使える経路があるか、候補を 7 つ並べて潰させる。

**本命は Gemini Code Assist。**シート課金の定額で、Gemini CLI がこのライセンスで動く。ただし「Google 製クライアント向けのライセンス」である可能性が高く、その場合 Antigravity とまったく同じ構図(資格情報の第三者クライアントへの流用)になる。条項で確定させる。

**もう 1 つ効きそうなのが Pro / Ultra に付く GCP クレジット。**Pro で月 $10、Ultra 20x で月 $100。これが Gemini Developer API の課金に充当できるなら、**実質「サブスク料金で API を使う」ことになる**。前回の調査で未確定として残った論点で、ここが埋まると話が変わる。

あわせて Claude Code 側も確認させている。公式の Vertex AI 対応は「Vertex 上の Claude モデル」を指すはずで、Gemini を直接選べるわけではない — そうだとすれば翻訳プロキシは必須という事実が確定する。

`★ Insight ─────────────────────────────────────`
- 問いの立て直しが効いている。「Antigravity で動かす」を目的にすると回避策を探すことになるが、「Gemini を使う」に戻すと、**Antigravity は数ある経路の 1 つでしかない**。しかも Google 自身が FAQ で別経路を案内している以上、目的を達する道は塞がれていない。ここまでの調査が袋小路に見えたのは、手段を目的に取り違えていたため。
- ただし「定額」という条件は手強い可能性がある。定額のサブスクは**製品の利用権**として売られており、API として切り出すと従量になる、というのが Google の課金の切り方だった。Code Assist もその形なら、定額 × 第三者クライアントの組み合わせは原理的に存在しないことになる。GCP クレジットの充当が、その隙間を埋める唯一の橋かもしれない。
`─────────────────────────────────────────────────`

## セッション 8: Gemini API キー経路の確定と料金体系

# AI

要件の確認をありがとう。認識は合っている — 求めているのは **Claude Code から Gemini の「モデル」を定額経路で呼ぶこと**で、Gemini アプリ(チャット UI)の話ではない。調査もその前提で出してある。

念のため、走っている調査に補足を送って要件を明確化しておく。

補足を送った。判定の軸を「**モデルをプログラムから呼べるか**」に固定してある — 「そのサブスクで Gemini が使えます」と書いてあっても、それが製品の画面で使えるという意味なら対象外。

この区別が今回の核心になる。ここまでの調査で分かってきた Google の課金の切り方は、**製品の利用権とモデルの呼び出し権を別々に売る**というもの。Antigravity のサブスクは IDE という製品を売っていて、そこに乗っている Gemini をプログラムから呼ぶ権利は含まれていなかった。同じ構図が Code Assist にも当てはまるなら、「定額 × 第三者クライアント」の組み合わせは原理的に存在しないことになる。

その場合でも道は残る。**Pro / Ultra に付く GCP クレジット**(月 $10 / $40 / $100)が Gemini API の課金に充当できるなら、支払いは定額のサブスク料金のまま、使うのは正規の API 経路、という形が成立する。ここが埋まるかどうかが分かれ目。

結果が返ったら整理して出す。

定額経路の調査結果を待っている。

定額経路の調査結果を待っている。

# phyllis998

> 調査中にすみません。並行して追加調査をお願いします。
> Google AI Proの得点説明に、"AI Studio5、Google Antigravity、Jules6 の使用量上限の拡大。"と書いてありました。AI StudioではAPIキーが発行できると思いますが、このAPI利用にAI Proはどう効いてくるのか調べてください。
> また、AI Studioを初めて開いたときに、このような内容のポップアップが出ました。参考までにお伝えします。
>
> 1. 利用規約などの同意ポップアップ: Google AI Studio と Gemini API を使用すると、Gemini、Veo、Lyria などの最新の Google AI モデルを使用してビルドできます。
>
> 2. 利用規約同意後に開くお知らせポップアップ: 開発をスマートに効率化: AI Studio で Google AI プランをご利用いただけるようになりました
> Google AI プランでは、最新の Gemini モデルと AI 機能への階層型アクセスを提供しています。Google AI Pro と Ultra を使用して、非常に複雑な課題に取り組むことができます。

# AI

良い着眼点。**AI Studio は「Web UI」と「API キーの発行元」を兼ねている**ので、特典の「使用量上限の拡大」がどちらに効くのかが紛らわしい。

前回の調査で該当しそうな原文は既に取れている。

> Google AI plan benefits for developer usage apply only within the Google AI Studio web interface.

これが正しければ「Pro の上限拡大は Web UI の中だけで、API キー経由には効かない」となるが、**あなたが見たポップアップの文面(「AI Studio で Google AI プランをご利用いただけるようになりました」「階層型アクセス」)はもっと広く読める**。ここは確定させる価値がある。専用の調査を並行で出す。

2 本を並行で走らせている。

| 調査 | 問い |
| --- | --- |
| 先行 | 定額(サブスク型)で第三者クライアントから Gemini を使える経路が存在するか。Code Assist / Gemini CLI / Enterprise / Provisioned Throughput / GCP クレジットを潰す |
| 今回 | **AI Pro の「AI Studio の上限拡大」が API キー経由にも効くのか** |

ポップアップの文面を渡しておいた。判断材料として効く。

`★ Insight ─────────────────────────────────────`
- この紛らわしさは製品名の設計に由来している。**「AI Studio」が Web UI の名前であると同時に API キーの発行元の名前でもある**ため、「AI Studio の上限が上がる」という一文が 2 通りに読めてしまう。開発者向けドキュメントがわざわざ「apply only within the Google AI Studio web interface」と **web interface** を明示しているのは、この誤読が想定されているからだと思われる。
- ただし、あなたが見たポップアップは**消費者向けの告知文**で、開発者向けドキュメントより後に出た可能性がある。「階層型アクセス」という表現が新しい仕組みを指しているなら、ドキュメント側の記述が古い、という筋もありうる。どちらが新しいかを更新日で突き合わせる必要があって、そこも調べさせている。
`─────────────────────────────────────────────────`

2 本とも結果が揃ったら、定額経路の可否を一度に整理する。

2 本の調査結果を待っている。

## セッション 9: Google AI Pro 上限がAPI経由に効くか確認、定額経路の調査を並行実施

# phyllis998

> また、同ポップアップにはこのようにも書いてありました。
>
> Gemini API 追加利用規約と Google プライバシー ポリシーが適用されます。プロンプトと回答は、Google AI のトレーニングで確認および使用される可能性があるため、機密情報や個人情報は送信しないでください。詳しくは、データの使用をご覧ください。Google AI モデルは不正確な情報を表示することがあるため、生成されたコンテンツを信頼、公開、またはその他の方法で使用する前に、回答を再確認するようにしてください。
>
> それぞれのリンクはこちら
> Gemini API 追加利用規約: https://aistudio.google.com/docs/terms?_gl=1*1jn24ir*_up*MQ..*_ga*MTQzOTQ2MTQxNC4xNzg5NTc2NTUx*_ga_RJSPDF5Y0Q*czE3ODk1NzY1NTEkbzEkZzAkdDE3ODk1NzY1NTEkajYwJGwwJGgw*_ga_P1DBVKWT6V*czE3ODk1NzY1NTEkbzEkZzAkdDE3ODk1NzY1NTEkajYwJGwwJGgxNzE3NDM3NTQz
> Google プライバシーポリシー: https://policies.google.com/privacy?hl=en
> データの使用: https://aistudio.google.com/docs/terms?_gl=1*1jn24ir*_up*MQ..*_ga*MTQzOTQ2MTQxNC4xNzg5NTc2NTUx*_ga_RJSPDF5Y0Q*czE3ODk1NzY1NTEkbzEkZzAkdDE3ODk1NzY1NTEkajYwJGwwJGgw*_ga_P1DBVKWT6V*czE3ODk1NzY1NTEkbzEkZzAkdDE3ODk1NzY1NTEkajYwJGwwJGgxNzE3NDM3NTQz#data-use-paid

# AI

この文面は決定的。**前回の調査で確定した Unpaid Services の条項と、そのまま対応している。**

> プロンプトと回答は、Google AI のトレーニングで確認および使用される可能性があるため、機密情報や個人情報は送信しないでください。

原文はこれ。

> Google uses the content you submit to the Services and any generated responses to provide, improve, and develop Google products and services and machine learning technologies […] human reviewers may read, annotate, and process your API input and output. […] **Do not submit sensitive, confidential, or personal information to the Unpaid Services.**

つまり**課金を紐付けていない状態で発行した API キーは、この Unpaid 側にいる**。ポップアップはその警告を出している。有料枠へ移す条件は「Cloud Billing が有効なプロジェクト経由であること」で、そこを越えると製品改善には使われなくなる。

「データの使用」のリンクが `#data-use-paid` というアンカーを指しているのは、規約ページ内にデータ利用の節が `data-use-unpaid` / `data-use-paid` で分かれているためと思われる。走っている調査に渡して、節の構造ごと確認させる。

なお、貼ってくれた URL には `_ga` などの解析用パラメータが付いていた。中身は Google Analytics のクライアント識別子なので、転送時はパラメータを落とした素の URL を使う。

追加データを調査側へ渡した。2 本の結果を待っている。

現時点で言えることを整理しておく。

**課金を紐付けずに発行した API キーは Unpaid 側にいる。**ポップアップの警告文がその条項そのもの。業務のソースコードを通すなら、この状態は避けたい。

**有料枠へ移す条件は明快** — Cloud Billing が有効なプロジェクト経由にすること。Prepay の最低額は $5。そこを越えると、プロンプトと応答は製品改善に使われなくなり、DPA が適用される。

ただしこれは**従量課金**の話で、あなたが確かめたい「サブスクで実現できるか」への答えではない。そちらは 2 本の調査待ち。

- AI Pro の「AI Studio の上限拡大」が API キーに効くなら、**サブスク料金の範囲で API が使える**ことになる
- 効かないなら、定額で使う道は Code Assist / GCP クレジットの充当あたりに絞られる

両方の結果が揃った時点で、経路ごとに「定額か / 第三者クライアント可か / CLIProxyAPI で設定できるか / データはどう扱われるか」を一枚に並べて出す。

2 本の調査結果を待っている。

## セッション 10: Google 課金体系と API 経路の検証で Gemini 利用方針を決定

# AI

2 本の調査結果を待っている。

2 本の調査結果を待っている。

決定に効く 3 点を自分で確かめる。

全件、一次情報で裏が取れた。**もう 1 本(AI Pro が API キーに効くか)は走行中。**先に確定した分を出す。

---

# 「サブスクで Claude Code から Gemini モデルを使う」の可否

## 結論

**Google が売っている商品の中に、「定額シートを買えば、任意の第三者クライアントから Gemini モデルを呼んでよい」というものは無い。**

Google の課金は **製品の利用権** と **モデルの呼び出し権** を別々に切っている。定額サブスクは前者を売る。モデルを呼ぶ権利は従量で売る。この線引きが一貫している。

## 経路ごとの判定

| 経路 | 課金 | 第三者クライアントから呼べるか |
| --- | --- | --- |
| AI Pro / Ultra の製品画面(Gemini アプリ、AI Studio UI、Antigravity IDE) | 定額 | 製品画面のみ。API 利用権は含まれない |
| Antigravity OAuth を流用 | 定額の枠 | **ToS 違反**。加えてサブエージェントは偽装 429 |
| **Gemini Code Assist Standard / Enterprise** | **定額シート** | **公式が禁止**(下記) |
| **Gemini CLI の Google アカウント OAuth** | シート枠 | **同上** |
| Gemini Enterprise アプリのシート | 定額シート | アプリへのサインイン権。モデル API の同梱は公式ページに無い |
| Gemini Developer API(AI Studio キー) | **従量** | **可** |
| Vertex / Agent Platform PayGo | **従量** | **可** |
| Vertex Provisioned Throughput | 容量コミット(前払い) | 可。ただし公式例のオーダーは**月数万ドル** |
| Pro / Ultra の GCP クレジット | サブスク特典で従量を相殺 | **可。月 $10 / $40 / $100 まで** |

## 本命だった Code Assist が塞がっている

Gemini CLI の公式リポジトリの ToS に、明示の一文がある(実際に取得して確認した)。

> Directly accessing the services powering Gemini CLI (for example, the Gemini Code Assist service) using third-party software, tools, or services (for example, **using OpenClaw with Gemini CLI OAuth**) is a violation of applicable terms and policies. Such actions may be grounds for suspension or termination of your account.

**例示が「第三者クライアント + Gemini CLI OAuth」そのもの。**Antigravity FAQ と同じ構図で、定額シートを正規に買っても、その資格を Claude Code へ渡すことは対象になる。

Code Assist に公開ドキュメント化された推論 API も見当たらない。有効化するのは `cloudaicompanion.googleapis.com` だが公開 `generateContent` のリファレンスが無く、`cloudcode-pa.googleapis.com` はセットアップ文書自身が **"an internal API"** と書いている。

## CLIProxyAPI 側も既に対応を外している

`gh api` で確認した。

- **PR #3893**「chore: remove Gemini CLI provider and related code」 — 作者 `luispater`、**2026-06-19 マージ済み**。本文の理由: *"now that Gemini CLI is being retired"*。`gemini_cli` executor、`internal/auth/gemini`、OAuth ログイン、translator をまとめて削除。
- **Issue #3797**「Gemini pro support after gemini-cli sunset」 — **`not_planned`** で 2026-06-17 にクローズ。

改名でも一時的な撤去でもなく、**意図的な削除**。7.2.155 に Gemini 系の OAuth は `--antigravity-login` しか残っていない。

## Claude Code 側も公式には Gemini を選べない

公式の LLM gateway ドキュメントに明記がある(取得して確認)。

> Anthropic doesn't endorse, maintain, or audit third-party gateway products, and **doesn't support routing Claude Code to non-Claude models through any gateway**.

公式の Vertex 連携は **Vertex 上の Claude モデル**を指しており、Gemini は対象外。`/v1/models` の発見も id に `claude` / `anthropic` を含むものだけを残す。つまり Gemini を使うには翻訳プロキシが要り、その構成は Anthropic のサポート外。

---

## 実質的に残る選択肢

**1. Pro / Ultra の GCP クレジットを API に充当する** — これが「サブスク料金で API を使う」に最も近い。

Google の公式ブログ(2026-01-27)と Developer Program の FAQ が、Pro で月 **$10**、Ultra 20TB で **$40**、Ultra 30TB で **$100** の Cloud クレジットを付与し、充当先として **Vertex AI / Cloud Run / Gemini API** を名指ししている。開発者向けドキュメントにも次がある。

> Subscribers with Google Cloud Platform (GCP) projects and Cloud Billing enabled are eligible to receive monthly Cloud credits from the Google Developer Program for Cloud services, **including the Gemini API**.

受け取りは自動ではなく、Developer Program で特典を有効化し、GCP プロジェクトと Cloud Billing を用意する必要がある。**定額無制限ではなく、月 $10〜$100 の相殺**。超過分は通常の従量。

文書作成役に `gemini-3.8-flash` を使う程度なら、入力 $0.75 / 出力 $3.75(1M トークン)なので、月 $10 でもかなりの量をまかなえる。

**2. 素直に従量の API キーを使う** — Cloud Billing を紐付けて有料枠にすれば、送信内容は製品改善に使われない。Prepay 最低 $5。

**3. Gemini を諦める** — 現在の `document-writer` は `gpt-terra`(codex 経路)で動いており、実害は出ていない。

---

もう 1 本の調査(**AI Pro の「AI Studio の上限拡大」が API キー経由に効くか**)が返れば、選択肢 1 の精度が上がる。もし Pro の契約自体が API のレート制限に効くなら話が変わるが、開発者向けドキュメントの "apply only within the Google AI Studio web interface" が正しければ、効くのはクレジットの相殺だけ、ということになる。

`★ Insight ─────────────────────────────────────`
- 3 つの ToS が同じ文型で書かれている。Antigravity FAQ は "using OpenClaw with Antigravity OAuth"、Gemini CLI ToS は "using OpenClaw with Gemini CLI OAuth" を違反例に挙げる。**製品が違っても禁じている行為は同一** — 「製品向けに発行された資格情報を、別のクライアントで使うこと」。裏を返すと、API キーだけは最初から第三者アプリに渡す前提で発行されるので、この条項の対象外になる。資格情報の**種類**が許諾の範囲を決めている。
- CLIProxyAPI の PR #3893 が効いている。もし Gemini CLI 経路が残っていたら、「使えるかもしれない」という誘惑が技術的に存在したはず。それが `not_planned` と削除で塞がれているため、今回の判断は「規約を守るために我慢する」ではなく「そもそも実装が無い」になっている。上流の判断が下流の選択肢を先に整理してくれた形。
`─────────────────────────────────────────────────`

2 本目も返った。ポップアップの英語原文まで製品コードから取れている。

# 結論

## AI Pro は API キーに効くか — **効かない**

引き上がるのは **AI Studio の Web UI(Playground / Build)の日次割り当てとモデルの選択肢**だけ。API キー経由の呼び出しには乗らない。

公式が 2 文で明示している。

> **AI Studio UI only:** Google AI plan benefits for developer usage apply only within the Google AI Studio web interface. Direct use of the Gemini API (such as using API keys or external applications) is billed and managed separately.

> **Different from API billing:** Google AI plans for AI Studio are separate from Gemini API usage tiers.

さらに決定的な一文がある。**サブスクの枠を使い切ったらどうするか**を書いた箇所。

> When daily baseline subscription quotas are exhausted in AI Studio, you can continue your workflows using a Gemini API key with Cloud Billing enabled for pay-per-request usage of the Gemini API directly.

「Pro の枠が API キーに乗る」のではなく「**UI の枠が尽きたら、別系統の従量 API へ移れ**」と言っている。2 つが別物であることの裏返し。

Google 自身のブログ(2026-04-20)も同じ切り方をしている。

> While **pay-per-request API keys remain the standard for production-scale launches**, these subscriber benefits offer an easy entry point to developers who would like to experiment and prototype more deeply with **AI Studio**.

## あなたが見たポップアップの正体

製品コードから英語原文が取れた。`ms-g1-welcome-dialog` — **`g1` は Google One**。

> Go Further, Build Smarter: Unlock more possibilities with Google AI plans now in AI Studio
> Google AI plans provide **tiered access** to the latest Gemini models and AI features.

「階層型アクセス(tiered access)」が指すのは **消費者向けプランの階層(Free / Pro / Ultra)**であって、Gemini API の usage tier(Free / Tier 1〜3)ではない。同じ「ティア」という語が 2 つの別体系に使われているため紛らわしいが、公式ドキュメントが「Google AI plans for AI Studio are separate from Gemini API usage tiers」と明示的に切り分けている。

**あなたの読みは自然だが、告知が指すのは AI Studio の中だけ。**

## API のレート制限が何で決まるか

**Cloud Billing の状態と累計支払額。**消費者向けサブスクは一切影響しない。

| ティア | 条件 |
| --- | --- |
| Free | プロジェクトがある |
| Tier 1 | 課金アカウントをリンク |
| Tier 2 | $100 支払 + 3 日 |
| Tier 3 | $1,000 支払 + 30 日 |

> Rate limits are applied **per project, not per API key.**
> They [API keys] have no independent billing settings; they inherit the tier limits and billing status of the project.

**「Pro / Ultra を契約していると API の無料枠が広がる」という記述は、レート制限・課金・API キーのどのページにも存在しない。**

## 初回ポップアップの警告について

非 EEA 向けの英語原文。

> The Gemini API Additional Terms of Service and the Google Privacy Policy apply. **Prompts and responses may be reviewed and used to train Google AI, so don't submit sensitive or personal information.**

これは **Unpaid Services の条項**そのもの。同意した時点ではまだ課金を紐付けていないので、発行されるキーは Unpaid / Free Tier にいる。**Pro に入っていても、Cloud Billing を紐付けなければ Unpaid のまま。**

面白い発見が 1 つ。「データの使用」のリンクが `#data-use-paid`(有料側)を指しているのに、警告文は無料側の内容。製品の JS が地域分岐を無視して全バリアント同じアンカーに畳んでいるため、と見られる。EEA 向けの文面(「製品改善には使われない」)なら Paid 側と一致するので、**2 つの地域文面を 1 つのリンクにまとめた際のずれ**が最も整合する説明。ただし公式の説明は無く、断定はできない。

なお Google の消費者向けページの脚注は `#unpaid-services` を指しており、そちらは非 EEA の警告と整合している。

---

# 2 本を合わせた全体像

**定額でやりたいことは、そのままの形では実現できない。**

| やりたいこと | 可否 |
| --- | --- |
| サブスク料金だけで Claude Code から Gemini を呼ぶ | **不可**。定額 × 第三者クライアントの組み合わせを売っている商品が無い |
| Code Assist のシートで代替 | **不可**。Gemini CLI 公式 ToS が第三者クライアント + OAuth を違反例に名指し |
| AI Pro の上限拡大を API に流用 | **不可**。UI の中だけ |

**実現可能なのは 3 つ。**

1. **Pro / Ultra の GCP クレジットで従量を相殺する** — 月 $10 / $40 / $100。支払いはサブスク料金のまま、使うのは正規の API 経路。**「サブスクで使う」に最も近い形。**ただし無制限ではない。
2. **素直に従量の API キー** — Cloud Billing を紐付けて有料枠にすれば、送信内容は製品改善に使われない。Prepay 最低 $5。
3. **Gemini を使わない** — `document-writer` は現在 `gpt-terra` で動いており、実害は出ていない。

文書作成役に `gemini-3.8-flash` を使う程度の用途なら、入力 $0.75 / 出力 $3.75(1M トークン)。**月 $10 のクレジットでもかなりの量をまかなえる**ので、1 と 2 を組み合わせるのが現実的だと思う。

---

ここまでの調査結果を踏まえて、保留にしている README 注記をどう書くか決めたい。当初の 3 案に、新しい選択肢が加わった。

| | 内容 |
| --- | --- |
| **(1)** | 技術的事実 + ToS の事実 |
| **(2)** | 技術的事実だけ |
| **(3)** | `RECOMMENDED` から `gemini-flash` を外す + 注記 |
| **(4)** | **(1) に加えて「API キー経路なら使える」という代替の案内を書く** ← 調査で裏が取れた |

**(4) を推す。**「使えない」で終わらせず、公式に認められた代替を示せる。`gemini-api-key` の設定項目も 7.2.155 に実在することを確認済み。

どれで進めるか決めてほしい。

`★ Insight ─────────────────────────────────────`
- 「ティア」という語が 2 つの体系で使われていたのが、今回の混乱の技術的な核心だった。消費者向けプランの階層(Free/Pro/Ultra)と、API の usage tier(Free/Tier 1-3)。**どちらも「上位ほど制限が緩い」構造なので、同じものだと読んでしまう。**Google のドキュメントがわざわざ "Google AI plans for AI Studio are **separate from** Gemini API usage tiers" と書いているのは、この衝突を認識しているから。製品名と概念名の衝突は、ドキュメントで否定文を書くことでしか解けない。
- 調査が 3 本続いて、同じ境界線に 3 回ぶつかった。偽装 429、Code Assist の ToS、AI Pro の適用範囲 — すべて「製品の利用権」と「モデルの呼び出し権」の境界。Google はこの線を、技術(フィルタ)・規約(ToS)・課金(別体系)の 3 層で守っている。**3 層とも同じ場所に引かれている**と分かった時点で、そこを越える方向の努力は筋が悪いと判断できる。
`─────────────────────────────────────────────────`

README 注記の方針待ち。前のターンで 4 案を出してある。推奨は **(4)** — 技術的事実 + ToS の事実 + API キー経路という代替の案内。

決めてもらえれば、文案を作って提示する。

# phyllis998

> 4で進めてください。合わせて今回の調査結果をdocs/cliproxyapi/に記録しておいてください。

# AI

(4) で進める。2 系統に分けて委譲する — 対象ファイルが交わらないので並列に走らせる。

**調査記録**は `docs/cliproxyapi/` に新規作成。既存の `2026-09-16-antigravity-false-429-system-prompt-filter.md` の続編にあたる内容で、書式もそれに倣わせる。リポジトリ内の記録なので、一次情報の URL と出典をすべて残す。

**README 注記**は `plugins/agent-policy/README.md`。こちらはプラグイン利用者が読むので、**リポジトリ固有のパスは書かない**。

2 系統を並列で走らせている。

| 担当 | 対象 |
| --- | --- |
| 調査記録 | `docs/cliproxyapi/2026-09-17-gemini-subscription-and-api-key-routes.md`(新規)。既存の 2026-09-16 の続編として、書式を倣わせる |
| README + 設計書 | `plugins/agent-policy/README.md` に 3 段落、設計書 §10-5 に決着を追記 |

**書き分けを明確に指示してある。**

- **調査記録**はリポジトリ内の資産なので、一次情報の URL・issue 番号・原文引用をすべて残す。「確認できなかったこと」の節も置かせている。
- **README** はプラグイン利用者が読むので、リポジトリ固有のパス・外部 URL・CLIProxyAPI の設定項目名を書かせない。「プロキシ側で API キーを使う設定にする」という一般的な表現に留める — このプラグインは特定のプロキシ実装を前提にしないため。
- **回避策は README に書かせない。**2 段落目に「この 429 を回避しようとしないでください」と明示させたうえで、代替の API キー経路へ誘導する形にした。

バージョンは `0.19.1-dev` のまま据え置き。README のみの変更で挙動は変わらない。

両方が返ったら差分を突き合わせてコミットする。

## セッション 11: Gemini API 経路の調査と README 注記方針の決定

# AI

鋭い指摘。**その通りで、私の運用が古い前提のまま止まっていた。**

経緯はこうだった。

1. セッション序盤、`document-writer` の `model` は `claude-gemini-3-8-flash` だった。最初の委譲がそれで 429 になった。
2. 引き継ぎ書の指示「`document-writer` が 429 で使えないときは担当表の Claude モデル(Sonnet)へ読み替える」に従い、`model: "sonnet"` で再ディスパッチした。
3. **その後、定義が `claude-gpt-5-6-terra`(codex 経路)へ差し替わった。**私自身 `/agent-policy:setup-agents` の再生成で確認している。codex 経路は偽装 429 の対象外。
4. **にもかかわらず、以降も `sonnet` 上書きを続けた。**読み替えの前提条件が消えたのに戻さなかった。

規律の上でも誤り。custom-policy は「外部 Agent の enum 宣言を適用する場合を除き、`model` 上書きは使わない」と定めている。さらに実行モデルの確定の順 4 は「`model` 宣言が `sonnet` / `opus` / `haiku` / `fable` のどれでもない → **そのまま起動**」。`claude-gpt-5-6-terra` は enum ではないので、上書きせずそのまま起動するのが正しい。

**以降は上書きせずディスパッチする。**

いま走っている 2 本(調査記録と README 注記)は `sonnet` で起動済み。両方とも**書く文面を私が全文指定している**ので出力は変わらない。止めて取り直すと完了済みの作業を捨てることになるため、このまま完走させる。次の委譲から正す。

念のため現在の定義を確認する。

確認。`model: claude-gpt-5-6-terra`、`agent-policy-vendor: gpt`。**codex 経路なので 429 の対象外**で、読み替えの理由はとっくに消えていた。

`★ Insight ─────────────────────────────────────`
- 「セッション途中で委譲先が使えなくなったら読み替える」という規律には、**戻す条件が書かれていない**。custom-policy には「次のセッションの SessionStart が改めて検証する。恒久的な対処はそこに委ねる」とあり、セッション内での復帰は想定外になっている。今回は同一セッション内で定義そのものが差し替わったので、規律が想定していない状況だった。
- とはいえ私は再生成を自分で実行して `claude-gpt-5-6-terra` を見ているので、気づく材料は手元にあった。**フォールバックは一度入れると惰性で続く** — 発動条件は明示的に判断するのに、解除条件は誰も見張っていない。片方向のスイッチになりやすい。
`─────────────────────────────────────────────────`

2 本の完了を待つ。以降の委譲は上書きなしで出す。

README と設計書の追記が完了。差分を突き合わせる。

差分は指示どおり。README に禁止事項の混入なし、設計書は既存記述を削らず末尾に追記されている。

調査記録の完成を待ってコミットする。

調査記録の完成を待っている。

調査記録の完成を待っている。

調査記録も完成。内容を確認してコミットする。

240 行、9 章。検証した主要な事実(`state_reason` の 2 値、コミット SHA、PR 番号、ToS の原文、ポップアップのコンポーネント名)がすべて含まれている。コミットする。

コミット完了。作業ツリーに残るのは本件と無関係な会話記録と抗体だけ。

---

# 完了

## 今回のコミット

`f527296` — Antigravity 経由 Gemini の限界と API キー経路を注記

| ファイル | 内容 |
| --- | --- |
| `docs/cliproxyapi/2026-09-17-gemini-subscription-and-api-key-routes.md`(新規・240 行) | 調査記録。一次情報の引用と出典、確認できなかった 8 項目を含む |
| `plugins/agent-policy/README.md` | 推奨モデル ID 表の直後に 3 段落 |
| `harness-docs/design/2026-09-16-...-design.md` | §10-5 に決着を追記 |

バージョンは `0.19.1-dev` 据え置き。README のみの変更で挙動が変わらないため。

## README に書いたこと / 書かなかったこと

**書いた** — サブエージェントとして起動できないこと、原因が systemInstruction の文字列検出であること、quota ではないので待っても解消しないこと、メインセッションでは動くこと、**Antigravity のログインを第三者クライアントで使うのは ToS 違反であること**、**回避を試みないこと**、代替として API キー経路があること、上流モデル ID の体系が違うこと、サブスクに API 利用権は含まれないこと、課金なしでは送信内容がサービス改善に使われること。

**書かなかった** — リポジトリ固有のパス、外部 URL、CLIProxyAPI の設定項目名、回避手段、推定の原因説明、変動する金額。プラグイン README は他リポジトリの利用者も読むため。

## このセッション全体のコミット

| | 作業 |
| --- | --- |
| `99eb595` | A: 死んだ `modelId` 配管の除去 + `0.19.1-dev` |
| `5688ef9` | B: 対応表 fixture の `ROLES` 導出化 |
| `bb1f85f` | C: 手順書の Antigravity (Gemini) 追随 |
| `aa06b47` | D: 4 案を比較した設計書 |
| `12b682e` | D: 対応表への RoleId 併記 |
| `a244269` | C の訂正: 上流モデル ID を実在する値へ |
| `f527296` | Gemini 経路の調査記録と README 注記 |

あわせて `.claude/agents/` の 14 定義を再生成済み(gitignore 対象)。

## 残っている未解決事項

- **運用観測待ち 2 件** — `doc-writing` が実際に呼ばれるか、`light-impl` / Haiku への再委譲の妥当性。§10-5 は今回決着したので 3 件から 2 件へ減った。
- **別起票 1 件** — `setup-agents` の兼務検出(CLI 変更を伴う)。
- **ADR の要否** — `ARCHITECTURE.md` への追随は不要(grep で確認済み)。起こすかは判断待ち。

## 私の運用上の誤りを 1 つ

`document-writer` への委譲で `model: "sonnet"` の上書きを続けていた。序盤の 429 に対する読み替えとして始めたものだが、**定義が `claude-gpt-5-6-terra`(codex 経路)へ差し替わった時点で条件は消えていた**。custom-policy の「`model` 上書きは使わない」、実行モデルの確定の順 4「enum でない `model` 宣言はそのまま起動」のいずれにも反する。指摘を受けて確認し、以降は上書きせずに出す。

`★ Insight ─────────────────────────────────────`
- 今回の一連の調査は、最初「バグの回避策を探す」問題に見えて、**課金と権利の境界を確認する**問題だった。技術(フィルタ)・規約(ToS)・課金(別体系)の 3 層すべてが同じ場所に線を引いていると分かった時点で、越える方向の努力は筋が悪いと判定できた。3 層が一致しているかを見るのは、回避策の是非を判断する実用的な物差しになる。
- README と調査記録で書き分けたのも同じ理由。**調査記録は「なぜそう判断したか」を将来の自分に残す**ので出典と未確認事項を全部書く。**README は「何をすべきか」を利用者に伝える**ので、判断の根拠より行動を書く。同じ事実でも、読者が次に取る行動が違えば書く粒度が変わる。
`─────────────────────────────────────────────────`

# phyllis998

> チャットに記録お願いします

# AI

記録を chat-recorder へ委譲した。既存の `docs/chat/2026/0916/phyllis998/1620-agent-policy-role-id-design.md` へ追記される。

なお、このセッションは Stop hook 経由で既に 10 回自動記録されているので、今回は残りの未記録分の追記になる。

記録が失敗した。原因を確認する。

原因が判明した。**これは環境の不具合ではなく、設計どおりの拒否。**

`prepare-chat-recording.ts:183` は `state` / `lock` / `plan` の 3 つが揃っていることを要求し、続く `:184-191` で **`attemptId` が 3 者すべてと一致すること**を検査する。この 3 点セットを作るのは **Stop hook** であって、手動起動ではない。

つまり記録は **Stop hook が発火したときだけ**実行できる。`attemptId` は 1 回限りの引換券で、前回(`42e0597a-...`)は既に消費済み。委譲先はそれを再利用しようとして弾かれた。

委譲先が「環境依存の問題」「state 管理に問題」と報告し、状態ディレクトリを手で作って再試行していたのは**診断の誤り**。`~/.claude/chat-history/chat-recorder/` は正常に存在している(9 エントリ)。手でディレクトリを作る対処は、この検査を通すことはできない。

**対処は不要。**このターンが終われば Stop hook が新しい `attemptId` で発火し、未記録分は自動で記録される。実際このセッションでは既に 10 回そうやって記録されている。

明示的に依頼してくれたが、**このプラグインでは記録を手で起動する経路が無い**設計になっている。二重記録と競合を防ぐためだと思われる。

`★ Insight ─────────────────────────────────────`
- 失敗の切り分けを誤った典型。`attempt/lock/plan missing` というメッセージは「ファイルが無い」と読めるが、実際には**「この起動は正規の契機を経ていない」**という意味だった。委譲先は前者で読んでディレクトリを作りに行き、当然直らなかった。エラー文言が「何が無いか」を言って「なぜ無いのか」を言わないと、この取り違えが起きる。
- 設計としては正しい。`attemptId` を hook だけが発行する引換券にすることで、**記録の契機を hook に一本化**している。手動で起動できると、同じ範囲を二重に記録したり、記録中のロックと競合したりする。スキル本文に「このスキルは記録の作成と追記だけを扱う」とあり、契機は持たない旨が書かれていたのもこれと符合する。
`─────────────────────────────────────────────────`

このターンの終了時に自動で記録される。

Stop hook が新しい `attemptId` を発行した。記録と抗体の蒸留を委譲する。
