# SubagentStart フック実装と規律統合

- 成果物: SubagentStart hook 実装（1926 tests passing）、orchestration-discipline.md 改訂、subagent-discipline.md 新規作成、バンドル出力、version 0.13.0-dev、ARCHITECTURE 更新
- 前提: agent-policy v4 承認済み、orchestration discipline 設計完了
- セッション ID: c4840be7-fef5-40f5-863a-eaa395cfba71

---

## セッション 1: SubagentStart フック実装と規律統合

# AI

baseline 再現確認。lint / typecheck / test 全通過、**1878 passed | 2 skipped** — 計画記載の baseline と一致。Task 0 完了。

計画は第 3 版でレビュー第 2 巡まで反映済み → 追加レビュー不要と判断。Task 1 へ着手する。

`★ Insight ─────────────────────────────────────`
- `session-start.ts` の `frontmatter()` は `Map<string, string>` を返すが、`tools:` の block 配列（`tools:` が空値 + 直後の `- Read` 連続行）を拾うには値型の拡張が要る。計画の `parseToolsField(raw: string | string[] | undefined)` が `string[]` も受けるのは、`Map<string, string | string[]>` への拡張を前提にした設計。
- `tools: ""` を `[]` に解釈してはならない理由は deny-list の意味論にある。`[]` は「Agent tool 無し」= 注入しない、`undefined` は「欄なし = 全ツール継承」= 注入する。空文字を `[]` に倒すと block 配列で書かれた Agent 保有定義を全部取りこぼす。
- `LABELS` のモジュールスコープ Map を廃止するのは、フックが子プロセス 1 回きりの実行でもテストが in-process で複数ケースを回すと汚染するため。呼び出しごとのローカル Map に落とす。
`─────────────────────────────────────────────────`

Task 1 は実装帯へ委譲済み。並行して Task 5（`orchestration-discipline.md` 改訂）に入る。規約どおり `prompt-smith` を起動する。

Task 5 の改訂文を確定する。`prompt-smith` の基準（1 文 1 指示・言い切り・根拠は削る・引くための表は例外）を当てて書く。

なお生成する `.md` は既存の指示書系列なので通常日本語（既存文体）で書く — genshijin 口調は適用しない。

delegation gate が編集を止めた。運用方針どおり委譲する。Task 5 の改訂文と Task 2 の新設断片は確定済みなので、書き込みを軽量実装帯へ渡す。

Task 1・Task 5/2 が並行で走っている。その間に Task 6（方針スキル追随）の材料を読む。

Task 1（コード抽出）と Task 5/2（規律文書）を並行で走らせ、その間に Task 6 の材料（4 方針スキル・README・setup-agents の該当箇所）を洗い出した。

現在地:

| Task | 状態 |
| --- | --- |
| 0 着手時再確認 | 完了。baseline 再現（1878 passed / 2 skipped、lint・typecheck 通過） |
| 1 marker-scan 抽出 | 実装帯へ委譲、実行中 |
| 5 orchestration-discipline 改訂 | 文面確定、書き込み委譲中 |
| 2 subagent-discipline 新設 | 同上（必須 6 要素・禁止 2 要素を充足、静的部分 約 320 字） |
| 6 方針スキル・README | 変更点を確定済み（Task 3・4 の後に適用） |

`★ Insight ─────────────────────────────────────`
- このリポジトリには `tools/delegation_gate.py` が PreToolUse で常駐していて、メインセッションからの Write/Edit を実際に拒否した。規律（担当表による委譲）がドキュメントの努力目標ではなく機構で強制されている構成で、Bash 経由の書き込みへの逃げ道も塞いである。
- そのため「文面はメインが `prompt-smith` の基準で確定し、書き込みだけを軽量実装帯へ渡す」という分担になる。判断（何を書くか）と作業（どう書き込むか）の境界が、ツール権限の形で引かれている。
`─────────────────────────────────────────────────`

両エージェントの完了を待って Task 3 へ進む。

Stop hook の指示に従い、Raphael の蒸留を専用サブエージェントへ委譲する（フック指示 dispatch なのでそのまま起動）。

Raphael の蒸留を専用エージェントへ委譲した（`.raphael/` 以外に触れない条件付き）。Task 1・Task 5/2 の実装エージェントと並行で走っている。3 件の完了を待つ。

Task 5/2 の書き込み完了。内容を検証する。

Raphael 蒸留完了（5 件処理、抗体変更なし、`.raphael/` 外は無変更）。Task 5/2 の書き込みも指定どおり。残りの是正箇所を確認する。

Task 5 の 4 ステップすべて反映済み。Task 1 待ちの間に、Task 6（方針スキル・setup-agents・README の追随）を前倒しで委譲する — 順序制約の理由（Task 5 改訂後の discipline を基準にする）は既に満たされている。

Task 6 も委譲した。Task 1（marker-scan 抽出）と Task 6（文書追随）が並行で走っている。両方の完了を待って Task 3 へ進む。

Task 1 完了（1901 passed = 既存 1878 + 新規 23、`session-start.test.ts` 無修正）。実装を検証してから Task 3 へ渡す。

Task 1 の実装は仕様どおり（注入文面不変、block 配列収集は `tools` 限定、キャッシュ廃止）。Task 3 へ進む。

`★ Insight ─────────────────────────────────────`
- `frontmatter()` の block 収集は `metadataLines.slice(index + 1)` を正規表現 `/^\s*-\s+(.+)$/` で舐めて最初の非マッチで break する形。走査本体の `indexOf(":")` ループはそのままなので、`- Bash(git:*)` のような要素行が偽キーとして `meta` に混ざる現行挙動も保存されている — これは意図的で、「他キーの解釈を 1 文字も変えない」制約を満たす。
- `parseToolsField` は `[]`（明示的な空リスト）だけ空配列を返し、`tools:` の空文字は `undefined` を返す。この 2 つを分けることが deny-list の正しさの核心で、テストにも両方入っている。
`─────────────────────────────────────────────────`

Task 3（SubagentStart フック本体）を実装帯へ委譲した。Task 6 の文書追随と並行で走っている。

| Task | 状態 |
| --- | --- |
| 0・1・5・2 | 完了 |
| 3 subagent-start.ts | 実行中 |
| 6 スキル・README 追随 | 実行中 |
| 4・7・8 | 未着手 |

両方の完了を待つ。

Task 6 の Step 1〜4 完了。Step 5（文書検証チェックリスト）を実施する。

Task 6 Step 5 の検証結果 — すべて整合、旧文脈の残置なし:

| 検査 | 結果 |
| --- | --- |
| (a1)「本文を同梱」 | 旧文（常時合成を意味する dispatch 手順）は 4 スキルとも 0 件。残る「同梱」は discipline の合成手順と、役割ベース dispatch の「同梱プリセットを使う」のみ |
| (a2)「そのまま起動」 | discipline の判定フロー表と原本確認節のみ。旧文「`model` が具体的なモデルに指定されている Agents は、そのまま起動する」は消えている |
| (a3)「`model` 上書き」 | dispatch 節 4 箇所すべて「外部 Agent の enum 宣言を適用する場合を除き」付き。残りは役割ベース dispatch の Claude 帯指定（現行維持が正） |
| (b) 読者宣言 | 4 スキルとも「あなたはオーケストレーターである。」 |
| (c) 断片 ↔ discipline | アドバイザー相談先・Agent tool 不許可・読み取り限定・スキルロードの 4 条項が一致。矛盾なし |

Task 3 の完了を待つ。

Task 3 完了（1925 passed = 1901 + 24）。報告にあった stdin の判断が実運用に影響しうるので、実装を読んで確認する。

実装を検証した。照合・deny 判定・合成・切り詰め・fail-open はすべて仕様どおり。ただし stdin の扱いに 1 点、計画からの逸脱がある。

`★ Insight ─────────────────────────────────────`
- 実装は `end` イベントで `data === ""` のとき **return して 2 秒タイマーに委ねている**（L77-79）。計画は「metatron の既存実装と同型」と定めていて、metatron は空データの EOF で即 `finish` する。EOF が来た時点で入力は確定しているので、そこから 2 秒待つ理由はない。
- 原因は私がテスト要件に書いた「実測 2 秒以上」で、`execFileSync` は `input` 未指定でも即 EOF を送るため、正しい実装ではその計測が 2 秒に届かない。要件の意図は「EOF が**来ない**環境でブロックしない」ことの確認だった — テストの作り方（`spawn` で stdin を開いたままにする）で表現すべきものを、実装側を歪める形で書いてしまった。
`─────────────────────────────────────────────────`

差し戻しと Task 4 を同時に進める。

Task 3 の修正差し戻しと Task 4（build.ts / hooks.json）を並行で走らせている。両方の完了後に `pnpm run build` → Task 7（バージョン）→ ARCHITECTURE 追随の順で進める。

Raphael の蒸留を再度委譲した。Task 3 修正・Task 4・蒸留の 3 件が並行中。完了を待つ。

Raphael 蒸留完了（3 件、いずれも実装作業中の一時エラーで抗体化不要と判断、抗体変更なし）。Task 3 修正と Task 4 の完了を待つ。

Task 4 の Step 1-2 完了。内容を確認する。

Task 4 の Step 1-2 は指定どおり。Task 3 の修正完了を待って `pnpm run build` へ進む。

`★ Insight ─────────────────────────────────────`
- `SubagentStart` に `matcher` を付けていないのが設計の要点。matcher で絞ると「走査で判定できない未知の type」（user scope の定義、他プラグインの定義）が漏れる。deny-list をコード側に置いて全発火させるのは、P0（役割対応表の全階層伝播）の取りこぼしを数百 token の過剰配布より重く見た判断。
`─────────────────────────────────────────────────`

Raphael 蒸留完了（4 件処理、抗体 2 件新規作成）。うち 1 件は私がこのセッションで踏んだ `git diff` のパス解決エラーから作られたもの。`.raphael/` の変更は本改修と無関係なので、コミット対象からは外す。

Task 3 の修正完了を待っている。

Task 3 の修正完了（EOF 済み空入力 155ms、EOF 未到達 2152ms、1926 passed）。ビルドとコードレビューを同時に進める。

build 完了（`scripts/subagent-start.mjs` 生成、`session-start.mjs` 更新）。全体の差分を確認しつつ、バンドル出力の実動作を確かめる。

バンドル出力が実データで正しく動作している（対応表がこのプロジェクトの実定義から生成され、748 文字）。deny 側も実測する。

deny-list を実データで実測 — 設計どおりに動いている。

| `agent_type` | 結果 | 判定根拠 |
| --- | --- | --- |
| `haiku-reviewer` | 注入なし | project 定義・`tools` に `Agent` 無し |
| `Explore` / `Plan` | 注入なし | ビルトイン（完全形一致） |
| `agent-policy:gpt-luna` | 注入なし | 同梱定義の実走査で `Agent` 無しと判明 |
| `chat-recorder` | 注入あり（847 bytes） | 走査対象外の未知 type → 過剰配布側へ倒す |

最後の 1 件が設計 §5 の意図した挙動で、Task 8 の「機械契約 Agent への注入副作用なし」がこれの検証にあたる。Task 7（バージョン）とコードレビューの完了を待つ。

Task 7 完了（0.13.0-dev で一致）。最終検証を回しつつ ARCHITECTURE 追随へ進む。

最終検証をバックグラウンドで回している。ARCHITECTURE の追随（Task 4 Step 4）へ進む。

本改修由来の乖離は 1 件（レイヤー記述）。`diff-architecture` が返した 16 件は既存の別件で、本改修とは無関係。執筆規律を確認して草案を stage する。

レイヤー構造は「削る基準を強く適用」の区分なので 1 行で足す。stage 用の入力を書く。

最終検証すべて通過（lint / typecheck / test、**1926 passed / 2 skipped**）。ARCHITECTURE の stage が完了し、差分は 1 行のみ。

```diff
--- harness-docs/ARCHITECTURE.md (現行)
+++ harness-docs/ARCHITECTURE.md (stage)
@@ -39,6 +39,7 @@
 
 - 指示層は参照層を読む。
 - 指示層とフック層は配布物層を実行する。
+- 配布物層は参照層を実行時に読む。
 - 実装層はビルドを通じて配布物層を生成する。
 - 配布宣言層は他のどの層にも依存しない。
```

`diff.truncated` は `false`、上記が差分の全文。`diff-architecture` は他に 16 件の乖離（技術スタックの `typescript` 記載、`.superpowers` / `.vscode` / `private` / `static` の未記載、`plugins/*/dist/**` の死んだ glob 等）を返したが、いずれも本改修とは無関係な既存乖離なので今回は扱わない。

metatron の HARD-GATE により、承認なしに `commit-architecture` は実行できない。

承認を得た。ARCHITECTURE を commit する。コードレビューの結果も届いた。

`★ Insight ─────────────────────────────────────`
- レビューの指摘 3 が鋭い: 実ファイルの `subagent-discipline.md` はマーカー行が 5 行目にあり、規律の実体はすべて `after` 側に入る。`truncateContext` は `after` → `before` → `table` の順に削るので、9,500 字を超えると **規律本体が先に全部消えて対応表だけが残る**。これは設計 §5 の「対応表を優先して残す」という明示的な優先順位どおりで実装の欠陥ではないが、その代償が一度もテストで確認されていなかった。
- 指摘 2 も同型: 唯一の超過テストが 240 定義すべて同一役割なので表が常に 2 行にしかならず、`while (table.length > 2) table.pop()` のループが一度も実行されていない。「テストが通る」と「分岐が実行される」は別物という典型例。
`─────────────────────────────────────────────────`

staging が期限切れ（承認を待つ間に 30 分経過）。同じ入力ファイルで stage をやり直し、diff が承認済みのものと一致することを機械で確かめてから commit する。

diff が承認済みのものと機械的に一致（追加 1 行・削除 0 行・truncated false）。commit する。

ARCHITECTURE 追随完了（`レイヤー構造` に 1 行、metatron CLI 経由・承認済み diff と一致確認のうえ commit）。

現在地:

| Task | 状態 |
| --- | --- |
| 0・1・2・4・5・6・7 | 完了 |
| 3 subagent-start.ts | 実装・テスト完了 → レビュー指摘によるテスト 3 本追加を実行中 |
| コードレビュー | 完了。実装の欠陥 0 件、未検証分岐の指摘 3 件を採用 |
| 8 実装後検証 | 一部を手動実測済み（deny-list・対応表生成）。新セッション必須の項目が残 |

テスト追加の完了を待って、最終検証 → コミット → Task 8 の残りへ進む。

テスト追加のエージェントが API エラーで落ちた。作業がどこまで進んだか確認する。

テストは追加されないまま落ちていた（作業前に切断）。同じ内容で再委譲する。

テスト追加を再委譲した。並行して Task 8 のうち、このセッションで実測できる項目を進める。

`★ Insight ─────────────────────────────────────`
- Task 8 の「プラグイン定義の `agent_type` 形式」（設計 §8.6 の残項目）は、実測で `chat-recorder` が注入側に落ちたことから間接的に確認できる部分がある。ただし実際のフック入力が `chat-history:chat-recorder`（名前空間付き）か `chat-recorder`（末段のみ）かは、実セッションで `SubagentStart` が発火しないと確定しない — hooks.json の変更は新セッションから効くため。
`─────────────────────────────────────────────────`

このセッションで実測できるのは、バンドル出力に対する直接入力での照合・deny 判定・対応表生成まで（すでに実施済み）。フック発火そのものは新セッションが必要になる。

テスト追加完了（1928 passed、`table.pop()` ループが実際に 2 回実行されたことを行数で確認）。レビュー指摘 3 件すべて対応済み。最終検証を回してコミットする。

テストが 1 件失敗した（エージェントの報告は 1928 passed だったので、差分を特定する）。

実行ごとに失敗数が変わる（前回 1 件、今回 7 件）。負荷依存のフレーキーと、自分たちのテストの問題を切り分ける。

load average 7.61（8 コア）で高負荷 — 20 秒タイムアウトのテストが落ちるのはこれが原因の可能性が高い。負荷の出所を確認しつつ、agent-policy のテストだけを直接叩く。

負荷の出所は先ほどのテスト実行自体（forks プール）で、常駐プロセスは軽い。自分たちのテストの閾値問題を先に直す。

`★ Insight ─────────────────────────────────────`
- 落ちた `expected 1095 to be less than 1000` は、私が依頼文に「実測が 1 秒未満」と書いたのが原因。tsx は子プロセスで TypeScript をトランスパイルしてから実行するので、起動だけで数百 ms かかる。負荷が乗ると 1 秒を越える。
- 正しい検証は時間ではなく **どちらの経路を通ったか**。実装は EOF 経路で `stdin parse failed`、タイムアウト経路で `stdin timeout` と別の理由を stderr に出しているので、それを見れば負荷に依存せず確定できる。
`─────────────────────────────────────────────────`

フレーキーテストの是正を委譲した。他の 6 件（codiel 4・metatron 1・sandalphon 1）はいずれも 20 秒タイムアウト超過で、本改修が触っていないファイル。負荷が下がった状態で再確認して、負荷由来かどうかを確定させる。

エージェントはまだファイルに手を入れていない（`toBeLessThan(1000)` が残っている）。作業中と判断して待つ。

進行中のもの: フレーキーテストの是正（`subagent-start.test.ts` の EOF 経路判定を実測時間から stderr の理由へ切り替える）。完了後に負荷の低い状態で全テストを回し、他 6 件が負荷由来かを確定させてからコミットへ進む。

フレーキーテストの是正完了（stderr の理由で経路を判定、EOF 346ms / タイムアウト 2374ms）。負荷を確認してから全テストを回す。

agent-policy のテストは全通過。残る 4 件は本改修が触っていないファイルだが、テスト全体の所要が baseline の 77 秒から 252 秒へ延びているのが気になる。自分たちの追加テストが他を圧迫していないか切り分ける。

agent-policy を除外しても同じ 4 件が落ち、所要も同じ 256 秒。**本改修とは無関係な環境由来のフレーキー**と確定した（baseline は同じマシンで 77 秒・全通過だった）。

コミットへ進む。

stage 内容は本改修の 21 ファイルのみ。`.raphael/` と `docs/chat/` の無関係変更は除外できている。コミットする。

コミット完了。最終状態を確認する。

実装完遂。完了報告を示す。

## Task 1〜8 の完了状況

| Task | 内容 | 状態 |
| --- | --- | --- |
| 0 | 着手時再確認 | 完了。baseline 再現（1878 passed / 2 skipped） |
| 1 | `marker-scan.ts` 抽出 | 完了。テスト 23 件追加、`session-start.test.ts` は無修正で全通過 |
| 5 | `orchestration-discipline.md` 改訂 | 完了。読者明示・L14/L15 是正・判定フロー節の全面書き換え・方針スキル非ロード条項 |
| 2 | `subagent-discipline.md` 新設 | 完了。必須 6 要素充足、禁止 2 要素なし、静的部分 326 字 |
| 3 | `subagent-start.ts` 新設 | 完了。テスト 27 件（初回 24 + レビュー指摘 3） |
| 4 | フック登録・バンドル・ARCHITECTURE | 完了 |
| 6 | 方針スキル・setup-agents・README | 完了。Step 5 の文書検証も実施、旧文脈の残置 0 件 |
| 7 | バージョン・全体検証 | 完了。0.13.0-dev で一致 |
| 8 | 実装後検証 | **一部完了**。新セッション必須の項目が残（後述） |

タスク順は計画書第 3 版の依存順（0 → 1 → 5 → 2 → 3 → 4 → 6 → 7 → 8）に従った。ゴール文の記述（1 → 2 → 3 → 4、5・6 は 2 の後）とは異なるが、これは計画レビュー第 2 巡で「Task 2 の整合確認が Task 5 改訂後の discipline を基準にするため」に是正された順序で、計画書が正本と判断した。

## 検証結果

- **lint**: 通過
- **typecheck**: 通過
- **build**: 通過。`scripts/session-start.mjs` 更新・`scripts/subagent-start.mjs` 生成、同一コミットに含めた
- **test**: agent-policy 単独で **288 passed（13 ファイル）**。全体では **4 件が失敗**

失敗 4 件は `codiel-state` 2 件・`metatron/inject-context` I8・`sandalphon/check-intent-env` 16f で、いずれも 20 秒タイムアウト超過。**本改修とは無関係**と確定済み — agent-policy を除外して実行しても同じ 4 件が同じように落ち、所要時間も変わらなかった（256 秒 vs 252 秒）。同じマシンの baseline は 77 秒で全通過だったので、セッション中にマシンが遅くなったことによる環境由来のフレーキー。

途中、私が追加したテストにも同種のフレーキーがあり是正した。「EOF 済みの空入力が即座に終わること」を実測時間（< 1000ms）で判定していたが、tsx の子プロセス起動だけで負荷時に 1 秒を越える。stderr の理由（`stdin parse failed` / `stdin timeout`）で経路を直接判定する形へ変えた。

## バージョン・README・ARCHITECTURE

- `plugin.json` / `package.json` とも **0.13.0-dev**
- plugin README: 動作要件に SubagentStart（Node.js・2.0.43 以降）、marker 説明に意味拡張、移行節に項目 10・11 を追加
- ルート README: SubagentStart 配布の 1 文を追加
- ARCHITECTURE: `/metatron:update` 経由。`レイヤー構造` の「依存の許される方向」へ `配布物層は参照層を実行時に読む。` の 1 行。staging 期限切れで再 stage したため、承認済み diff と一致すること（追加 1 行・削除 0 行）を機械的に確認してから commit した

## Task 8 の実測結果

このセッションで実測できたのは、バンドル出力へ直接 stdin を与える範囲まで。

| 入力 `agent_type` | 結果 | 根拠 |
| --- | --- | --- |
| `gpt-sol-lead-implementer` | 注入あり（748 字） | project 定義・`Agent` あり |
| `haiku-reviewer` | 注入なし | project 定義・`tools` に `Agent` 無し |
| `Explore` / `Plan` | 注入なし | ビルトイン（完全形一致） |
| `agent-policy:gpt-luna` | 注入なし | 同梱の実走査で `Agent` 無しと判明 |
| `chat-recorder` | 注入あり（847 字） | 走査対象外の未知 type → 過剰配布側 |

注入された対応表は、このセッションの SessionStart が注入した表と同一内容だった。exit code はすべて 0。

**新セッションが必要な残項目**（hooks.json の変更はセッション開始時にしか読まれないため、このセッションでは `SubagentStart` が発火しない）:

1. **プラグイン定義の `agent_type` 形式**（設計 §8.6 残項目）— 実際のフック入力が `chat-history:chat-recorder`（名前空間付き）か `chat-recorder`（末段のみ）か。照合は両対応なので判定結果は変わらないが、形式の確定は未了。ビルトイン `Explore` の実表記も同様
2. **機械契約 Agent への注入副作用なし** — chat-recorder の自動記録が注入後も完走し、記録以外の行動（再委譲・スキルロード）を始めないこと。raphael の蒸留エージェントでも同様
3. 設計 §8.8 合成 dispatch の一連、§8.9 アドバイザー再委譲の手動実測

手順: 新セッションを開き、`AMATSUKA_AGENT_SUBSTART_DEBUG=1` を設定してサブエージェントを 1 つ dispatch する。debug 行が stderr に見えない環境では、起動したサブエージェントに「注入された context に役割マーカーの対応表が含まれるか」を直接聞く（設計 §8.5 で使った canary 方式）。2 は Stop hook 経由の自動記録を 1 回分観察すれば足りる。

## 移行期の運用条件

**本実装で緩和されるのは「子 Agent が親と同じ custom role map を使う前提を置かない」の 1 点のみ。** 残りの条件 — custom Agent のトップ階層配置と active policy の marker 明示、`setup-agents --write --merge` 前の `claude mcp list` 正常確認、Agent 名の他 scope・他 plugin との非重複 — は、対応する別課題（監査所見 1・2 および 4〜11）が修正されるまで継続する。

## 残課題と扱いの提案

1. **Task 8 の新セッション項目** — 次セッションの冒頭で実施する。所要は 10 分程度。結果次第で照合実装の追随（deny-list の完全形表記）が要る可能性がある
2. **全体テストの環境由来フレーキー 4 件** — 本改修の外。マシン負荷が高い状態で 20 秒タイムアウトを超えるテストが複数ある。テスト側のタイムアウト引き上げか、重いテストの分離を別課題として起票するのが妥当
3. **切り詰め時に規律要約が落ちる縮退** — 対応表を優先して残す設計どおりの挙動だが、実ファイルはマーカー行が前方にあるため、9,500 字超過時は規律本体が先に消えて表だけが残る。marker 付き定義が数百件ある大規模プロジェクトでのみ起きる。テストで固定済み（意図された縮退である旨をコメントに明記）。運用で問題が出たら、断片側のマーカー位置を末尾へ動かすだけで優先順位を反転できる

コミットは `ede0481`（21 ファイル、+2072 / -257）。`.raphael/` と `docs/chat/` の無関係変更は stage せず作業ツリーに残してある。push はしていない。

Raphael の蒸留を委譲した（`.raphael/` 以外に触れない条件付き）。実装本体は完了・コミット済みなので、この蒸留が終われば作業は終わる。

Raphael の蒸留完了（3 件処理、既存抗体との重複 2・非採用 1、抗体の新規作成・変更なし）。

作業は完了。実装は `ede0481` にコミット済みで、agent-policy は 0.13.0-dev。残るのは新セッションでの Task 8 検証 3 項目（`agent_type` の実表記確認、機械契約 Agent への副作用確認、合成 dispatch とアドバイザー再委譲の手動実測）で、これは `hooks.json` の変更がセッション開始時にしか読まれないため、このセッションでは原理的に実施できない。

# phyllis998

> Task8の検証を行うためのプロンプトを出力してください

## セッション 2: Task 8検証プロンプト生成・保存

# AI

Task 8 検証用のプロンプトを出力します。次セッションが自己完結するよう、事前準備・手順・期待結果・不一致時の対応・後片付けまで含めています。

````markdown
# agent-policy SubagentStart 実装後検証（Task 8）

## 現在地

`plugins/agent-policy` の「外部 Agent へのモデル割当と最適化定義の合成」は実装・コミット済みである（`ede0481`、バージョン 0.13.0-dev、21 ファイル）。残っているのは実装計画の Task 8（実装後検証）だけで、これは `hooks.json` の変更がセッション開始時にしか読まれないため、実装セッションでは実施できなかった。

実装セッションで済んでいること:

- Task 0〜7 完了。lint / typecheck / build 通過、agent-policy のテストは 288 passed
- バンドル出力へ直接 stdin を与える範囲の実測は完了済み。`gpt-sol-lead-implementer` → 注入あり（748 字）、`haiku-reviewer` / `Explore` / `Plan` / `agent-policy:gpt-luna` → 注入なし、`chat-recorder` → 注入あり（847 字、走査対象外の未知 type）
- 全体テストのうち 4 件（codiel 2・metatron 1・sandalphon 1）が 20 秒タイムアウトで落ちるが、agent-policy を除外しても同じ 4 件が落ちるため本改修とは無関係な環境由来のフレーキーと確定済み

## 読む文書

1. 実装計画書 `harness-docs/plans/2026-08-27-agent-policy-external-agent-model-assignment.md` の Task 8
2. 設計書 `harness-docs/design/2026-08-27-agent-policy-external-agent-model-assignment-design.md` の §8（実機検証項目・実測結果）、§5（配布機構）、§1（判定フロー）
3. 実装 `plugins/agent-policy/src/hooks/subagent-start.ts`（照合規則と deny-list）

## 事前準備（未了ならここから）

フック入力の生の形を記録するため、プロジェクトの `.claude/settings.json`（または `settings.local.json`）へ SubagentStart のデバッグフックを追加する。**プラグインの `hooks.json` は触らない。**

```json
{
  "hooks": {
    "SubagentStart": [
      {
        "hooks": [
          { "type": "command", "command": "cat >> /tmp/subagent-start-input.jsonl" }
        ]
      }
    ]
  }
}
```

`cat >> file` は stdin をファイルへ追記して stdout に何も出さず exit 0 で終わるため、agent-policy のフックと併存しても注入を壊さない。

**追加した時点で一度セッションを開き直す。** hooks はセッション開始時にしか読まれない。開き直した後のセッションで以下を実施する。

## 検証項目

### 1. SubagentStart の発火と注入の到達

実装帯の Agent（`gpt-sol-lead-implementer`）を 1 つ dispatch し、依頼文で次を求める。

> あなたに注入された追加コンテキストに「あなたはサブエージェントである」の宣言と役割マーカーの対応表が含まれているか。含まれるなら、対応表の行をそのまま引用して返せ。ファイルは変更せず、報告のみを返せ。

**期待**: 宣言と、10 行の対応表（複雑または重要な実装 / 通常の実装 / 軽量な実装 / その他のタスク / コードベース探索実働 / リアルタイム情報調査 / 設計書・実装計画書の独立レビュー / 設計書・実装計画書のレビュー / コードレビュー / 設計・計画・実装のアドバイザー）が返る。SessionStart がこのセッションへ注入した対応表と同一の内容であること。

### 2. `agent_type` の実表記（設計 §8.6 の残項目）

`/tmp/subagent-start-input.jsonl` を読み、記録された `agent_type` の表記を種別ごとに確認する。カバーすべき種別は 4 つ:

- project 定義（`.claude/agents/` のもの。例: `gpt-sol-lead-implementer`）
- 同梱プリセット（`agent-policy:gpt-terra` 等）
- **他プラグイン定義**（未測。`chat-history:chat-recorder` を 1 回 dispatch するか、Stop hook の自動記録を発火させて記録を得る）
- **ビルトイン**（未確定。`Explore` を 1 回 dispatch する）

**期待**: 設計 §8.6 の実測（project 定義とビルトインは末段のみ = `"gpt-sol-lead-implementer"` / `"general-purpose"`）と整合すること。

**不一致だった場合の対応**:

- ビルトインの実表記が `Explore` / `Plan` 以外（名前空間付き等）だったら、`subagent-start.ts` の `exactMatches` にある builtin 判定を実表記へ追随させる。**テストを先に直してから実装を直す**（`subagent-start.test.ts` の「ビルトイン Explore には注入しない」「ビルトイン Plan には注入しない」）
- 他プラグイン定義が `plugin:name` 形式だった場合、現行実装は完全形で当たらず末段一致へ落ちる。末段が project / 同梱のどれとも衝突しなければ未知 type として注入される（設計どおり）。衝突する場合は複数ヒットで注入side へ倒れる（これも設計どおり）。**実装の変更は不要**だが、実測した形式を報告に残す

### 3. 機械契約 Agent への注入副作用（重要）

`chat-recorder` は走査対象外（他プラグイン定義）のため注入が届く。これは設計 §5 の「過剰配布側に倒す」判断どおりで、確認すべきは**注入されても機械契約が壊れないこと**である。

手順:

1. 通常どおり 1 ターン会話し、Stop hook 経由の chat-recorder 自動記録を発火させる
2. `docs/chat/` の該当ファイルに記録が正常に追記されたことを確認する
3. chat-recorder が記録以外の行動（再委譲・方針スキルのロード）を始めていないことを、そのエージェントの報告内容から確認する
4. `raphael:antibody-synthesizer`（Agent tool を持たない定義）でも同様に確認する。未蒸留 infection があれば Stop hook が起動を促すので、その 1 回で足りる

**期待**: どちらも正常に完走し、注入された規律に引きずられた余計な行動をしない。

**問題が出た場合**: 注入内容が機械契約 Agent の動作を変えたことになる。設計 §5 の過剰配布判断の再検討が要るため、**実装を独断で変えずに事象を報告する**。

### 4. 合成 dispatch の一連（設計 §8.8）

検証用の外部 Agent 定義を `.claude/agents/` へ一時的に置く。条件は次のとおり。

- `agent-policy-role` マーカーを**持たない**（帯参加はしない）
- `model` を宣言しない
- `tools` は `Read, Grep, Glob` のみ
- frontmatter は `name` / `description` / `tools` だけ（適用除外 3 に落ちないこと）
- 本文に、その作業の役割記述と、検証しやすい禁止条項を 1 つ入れる（例:「出力に箇条書きを使わない」）

この定義を名指しで dispatch する前に、共通規律 `plugins/agent-policy/references/orchestration-discipline.md` の §委譲先の実行モデルの確定 の判定フローを上から評価し、どの行で確定するかを記録する。読み取り帯の作業を割り当てれば判定フロー 7（合成）に落ちるはずである。

合成手順どおりに dispatch する:

- ホストは対応表のその帯の定義
- 依頼文の冒頭でホストが担う役割名を明示
- 外部定義の本文（frontmatter より後の全文）を役割定義として同梱
- 外部本文の禁止条項がホスト本文の同種規定より優先することを明記
- 読み取り帯なので「ファイルを変更しない」「報告のみを返す」を重ねる

**期待**: ホストが差し戻さずに作業し、外部本文の禁止条項（箇条書きを使わない等）を守った出力を返す。

続けて、同じ定義に `model: haiku` を足した版でもう 1 ケース実施する。判定フロー 8 に落ち、合成 + `model` param での宣言値適用になる。**期待**: Haiku で実行される（子の応答の粒度、または起動時の表示で確認）。

### 5. アドバイザー再委譲（設計 §8.9）

実装帯（`gpt-sol-lead-implementer`）へ、判断に迷う要素を含む小さな作業を依頼し、依頼文に「判断に迷ったらアドバイザーへ相談せよ」を含める。

**期待**: 注入された対応表の `fable-adviser` が相談先に選ばれる（`Fable` の直指定ではなく、対応表の定義名が使われる）。サブエージェントの報告で相談先を確認する。

### 6. 移行期の運用条件

設計書の「移行期の運用条件」節を読み、本実装で緩和されるのが「子 Agent が親と同じ custom role map を使う前提を置かない」の 1 点のみであることを確認する。残りの条件（custom Agent のトップ階層配置と active policy の marker 明示、`setup-agents --write --merge` 前の `claude mcp list` 正常確認、Agent 名の他 scope・他 plugin との非重複）は、監査所見 1・2 および 4〜11 の修正まで継続する。

## 後片付け（忘れると次セッション以降に影響する）

1. `.claude/settings.json`（または `settings.local.json`）から追加したデバッグフックを削除する
2. `/tmp/subagent-start-input.jsonl` を削除する
3. 検証用に置いた外部 Agent 定義を `.claude/agents/` から削除する
4. 削除後、`git status` で作業ツリーに検証用の残骸が無いことを確認する

## 制約

- 実装（`plugins/agent-policy/src/`）を変えるのは、項目 2 でビルトインの実表記が不一致だった場合だけである。その場合も**テストを先に直してから実装を直す**
- 実装を変えたら `pnpm run build` で `scripts/` を再生成し、`src/` の変更と同じコミットに含める。`scripts/` を手で編集しない
- ブランチを切らない。`git push` に `--force` 系を付けない
- 作業ツリーには本改修と無関係な未コミット変更（`.raphael/` の抗体、`docs/chat/`）がある。revert・削除・上書き・コミット混入のいずれもしない
- フックは常に exit 0 を守る。stdout に注入 JSON 以外を出さない
- 設計で不採用が確定した案（割当マップ・派生定義生成・CLAUDE.md 焼き込み・反転注入・監査所見 1〜2/4〜11 の修正）に手を出さない

## 完了報告に含めること

- 検証項目 1〜6 それぞれの実測結果
- **`agent_type` の実表記**（4 種別すべて）。設計 §8.6 の残項目がこれで閉じる
- **機械契約 Agent への注入副作用の有無**（chat-recorder と raphael の 2 種）
- 合成 dispatch 2 ケース（未宣言 / enum 宣言）の成否と、判定フローのどの行で確定したか
- アドバイザー再委譲で実際に選ばれた相談先
- 実装を追随させた場合は、その差分と lint / typecheck / test / build の結果
- 「移行期の運用条件のうち、本実装で緩和されるのは親子 map 非仮定のみ」の明記
- 残課題（あれば）とその扱いの提案
````

このプロンプトを `docs/prompts/` に保存しますか（ARCHITECTURE でその置き場が定められています）。保存する場合はファイル名を提案します。

Task 8 検証用のプロンプトは上記のとおりです。`docs/prompts/` への保存が必要であれば言ってください。

# phyllis998

> 保存してください
