# agent-policy に Opus 5 世代の役割を取り込む

- 日付: 2026-07-25
- 参加者: phyllis998, AI (Claude Opus 5 (1M context), effort=xhigh)
- 成果物:
  - `docs/design/2026-07-25-agent-policy-opus5-roles-design.md`
  - `docs/plans/2026-07-25-agent-policy-opus5-roles-implementation.md`
  - コミット `143995d` `b57e997` `c6fde1b` `0b1940c` `a76c948` `bd8ba83` `d93726e`(main へマージ済み)
  - `plugins/agent-policy` 0.3.5-dev → **0.4.0-dev**
- 前提:
  - `docs/chat/2026/0725/phyllis998/agent-policy-skill-slimming.md`(同日の 2 スキル簡潔化。本セッションの §4-5 はこの規律の遡及適用にあたる)
  - `plugins/agent-policy/references/cost-discipline.md`(GPT 経路キャッシュ不成立の実測)
  - `plugins/agent-policy/README.md` §設計上の確定事実(dispatch 時の model 上書き制限)

---

## セッション1: 何を Opus 5 に任せるかの設計

# phyllis998

> agent-policyプラグインの2つのスキルに、Opus5の役割を取り込みたいです。
> 現状この2つのスキルは、"Opus4.8"の役割を定義しています。
> どの部分を任せたら、よりよい品質のものができると思いますか？

# AI

**何をしたか**: CLAUDE.md の指示どおり `agent-policy:with-codex` を先にロードし、続いて `superpowers:brainstorming` を起動。プラグイン全体(2 SKILL・3 references・3 テンプレート・README・plugin.json)と `~/.claude/settings.json` を調査した。

**最初に判明した制約(これが結論を規定した)**: Agent tool の `model` 上書きは enum(`sonnet`/`opus`/`haiku`/`fable`)で、**Opus 4.8 と Opus 5 を dispatch で呼び分ける手段がない**(本セッションのツールスキーマで確認)。バージョン固定が可能なのは Agents 定義 frontmatter の `model:` のみで、README が GPT エイリアスについて記録した制約と同一構造。したがって「Opus 5 という行を担当表に足す」ことは構造的に不可能で、実質的な作業は**担当表の境界線の引き直し**になると判断した。あわせて `settings.json` が `model: "opus[1m]"` / `effortLevel: "xhigh"` であり、メインセッションが既に Opus 5 1M であることを確認。

**提案した 4 点(品質が上がる順)**:

1. Fable が持っていた「重い最終レビュー・重要な設計判断」を Opus に一本化。現行の「Opus(**または Fable が直接**)」の曖昧さが、判断のたびのエスカレーション検討コストを生み、`cost-discipline.md` 記録の Fable 約 26 ターン(≈$52)事件につながっている
2. 1M 文脈を前提とした「横断整合レビュー」の新設(並列レポートの統合・context-map + 設計書 + 差分の同時突き合わせ)
3. `with-codex` の探索統括を GPT Sol から Opus へ戻すか(この時点では推奨を保留)
4. **やってはいけないこと**: Haiku レビューを Opus に置き換える。弱い読者テストとしての価値が失われる

**意図的にロードしなかったもの**: `claude-api` スキル。トリガー定義上は「モデル選択の質問」で該当するが、memory に「139k トークンの無自覚ロードがコスト事件の真因」と記録があり、かつ本件は API 仕様ではなくユーザー自身の運用方針の話であるためロードを見送った。Opus 5 の能力については断定を避け、**タスクの形**から推奨を組み立てた。

**分岐点を 1 問ずつ確認した(4 問)**。

# phyllis998

(AskUserQuestion への回答)

- Opus 枠の扱い → **「Opus 枠は1つ(= Opus 5 に更新)」**
- with-codex の探索統括 → **「ハイブリッド: 統括=Opus 5 / 実働=GPT」**
- Fable の範囲 → **「承認ゲート + アドバイザーのみ」**
- claude-only の Opus は複雑な実装を持ち続けるか → **「持ち続ける(現行維持)」**

# AI

**決定と理由**:

- Opus 枠を 1 つに保つ判断により、`with-codex` のフォールバック記述・`context-map-guide.md` の役割→モデル割り当て・`advisor-rules.md` の 3 箇所に分岐が生えるのを回避できた
- 探索のハイブリッド化の根拠は、`cost-discipline.md` の実測(GPT Luna の input の 94% が非キャッシュ)と**タスク寿命の非対称性**。探索の統括は定義上「読んだものが積み上がる」役なのでキャッシュの効かない経路に置くと全額課金されるが、grep/read を数ターン回して終わる実働ワーカーは短命なので同じ損失を受けない

**却下された選択肢(すべて設計書 §6 に記録)**:

- 担当表を 5 層(Fable / Opus 5 / Opus 4.8 / Sonnet / Haiku)にする → dispatch で呼び分けられず、frontmatter でモデル固定した Agents 定義が別途必要になる
- Opus 5 を「メインセッション専用」として役割側で固定する → 方針が特定のセッション構成に依存し、サブエージェントとして Opus を使う場合の担当が書けなくなる
- 探索を完全に Opus へ戻す(claude-only と同形) → `with-codex` の価値(重量作業を GPT に寄せる)を大きく削る
- Fable をアドバイザー専任にし承認ゲートも Opus が持つ → コスト削減は最大だが品質のバックストップが 1 段減る

設計書を `docs/design/` に作成した(リポジトリ既存慣習に合わせ、brainstorming スキル既定の `docs/superpowers/specs/` は使わなかった)。

**執筆中に判明した波及**: Fable を「実務をしない」役にすると、`context-map-guide.md:17` の「**最上位オーケストレーター**(要件確定・承認ゲート担当)= Fable」という 1 対 1 対応が壊れる。要件確定は実務なので Opus へ降り、承認ゲートだけが Fable に残るため。この時点では「**Opus が最上位オーケストレーターを兼ねる**」という形で設計した(後にセッション1末で撤回)。

# phyllis998

(Haiku レビューの実施可否について) → **「Haiku レビューを回す」**

CLAUDE.md の必須手順と、本セッションの「依頼がない限りサブエージェントを使わない」設定が競合していたため確認した。

# AI

**Haiku レビューの結果と対応**。指摘 8 件を切り分け、**6 件を取り込み・1 件は説明で足りると判断・1 件は自分の書き方の問題**とした。

取り込んだ 6 件:

| 指摘 | 対応 |
|---|---|
| `context-map-guide.md:17` の**定義そのもの**が壊れるのに追随リストが「典型例を更新」で止まっている | §3-6 を再設計 |
| GPT テンプレートの具体的な改訂文言がない | §5-4 / §5-5 に行単位の before→after を記載 |
| `context-map-template.md:4` の現行内容が設計書から読めない | §5-3 に現物を転記 |
| README の再実行案内の具体文がない | §5-6 に文面を記載 |
| §7 の検証が §5 の全項目をカバーしていない | §7 を 5 ステップに再構成(横断 grep を追加) |
| §5 が網羅的か不明(claude-only の削除行が載っていない) | §4 冒頭に「§4=2スキル網羅 / §5=それ以外」の分担を明記 |

説明で足りると判断した 1 件: 「2 方針間の非対称の緩和策がない」→ 本設計が持ち込む差ではなく GPT 帯の有無から必然的に生じる差で、両方針は排他的に使うため混在しない。§3-5 に理由を追記。

自分の書き方の問題 1 件: Haiku が §4-2 を「新セクションの追加」と読んだ。既存章の書き換えなので見出しに明記。

**方針転換(このセッション最大のやり直し)**: Haiku の指摘を受けて §3-6 を詰め直した結果、当初の「Opus が最上位オーケストレーターを兼ねる」は **層の名前と実態がねじれる**と判明した。正しい形は「**層の名称は変えず、`要件確定` を最上位の担当列挙から戦術へ 1 語だけ移す**」で、これなら「最上位=Fable / 戦術=Opus」の典型対応が維持され、guide の変更は括弧内 1 箇所のみで済む。

**あわせて確定した派生判断**: 規律文の「計画確定」だけは役割名ではなくモデル名(`Fable の承認`)で固定する。役割名のままだと、メインセッションが Opus のとき「自分が最上位だ」と解釈して自己承認できてしまい、その曖昧さこそ Fable 縮小の目的だったため。guide は役割の定義、SKILL は役割→モデルの割り当ての宣言、という分担で整理した。

**副次効果の発見**: 探索統括が両 profile とも Opus になるため、context-map の「作成者」欄にあった **profile ごとの併記・印付けという運用が丸ごと不要**になった(§5-2)。方針の簡素化がテンプレートの簡素化に伝播した形で、意図的に取り込んだ。

---

## セッション2: スキル本文の記述スタイルの是正

# phyllis998

> このスキルは、毎回エージェントに読まれることが想定される規律です。そのためエージェントに対して与える情報は必要最小限に留めたいので、人間が読むことを想定されたような根拠などの文脈は削りたいです。agents-claude-only.md や、agents-with-codex.md が見本になります。

# AI

**誤った前提だった点**: 改訂案に根拠を本文へ持ち込みすぎていた。見本(`agents-with-codex.md` / `agents-claude-only.md`)を確認したところ、命令形の箇条書きのみで理由の説明を一切含まないスタイルだった。

**この基準で見ると、当初案の「本方針は Opus 5 世代を前提とする」という宣言文も落とすべきだった**。エージェントの振る舞いを変えるのは担当表の割り当てであって、世代の宣言は何も変えない。Opus 5 世代の能力は「担当範囲がどう引き直されたか」としてのみ現れるべきで、設計の質としてもこちらが正しい。

**残す/削るの境界を定義した**: 「**読んだ後に振る舞いが変わる文は残す、納得を与えるだけの文は削る**」。この基準により「要約の要約で判断しない」は残す(長文脈前提の行動指示)一方、「強いモデルは行間を推論で埋めるため」等は削る。

**設計書への反映**:

- §3-1 を改訂 — 世代前提の宣言文を廃止
- §3-7 を新設 — 記述スタイルの規定。見本を明記し、除外した 3 つの記述を除外した事実ごと記録
- §4-5 を新設 — この基準を**既存本文にも遡及適用**する案(4 箇所の理由句削除)。担当表の改訂とは独立に却下できる形で提示した

`:21` の「列挙順に実行順の意味はない」は**残す**判断とした(順序依存という誤読を防ぐ指示で、振る舞いを変えるため)。

# phyllis998

> 4-5の遡及適用も含めて実装計画に進んでください

---

## セッション3: 実装計画の作成と実装

# AI

**何をしたか**: `superpowers:writing-plans` で実装計画を作成。リポジトリ慣習に合わせ `docs/plans/YYYY-MM-DD-<name>-implementation.md` とし、コード変更ゼロ・Markdown のみ・検証は grep と読み合わせという性質に合わせて構成した。5 タスク(with-codex / claude-only / 根拠句削除 / references・テンプレート波及 / README・plugin.json・横断検証)に分割。

**自己レビューで実測確認した 2 点**: `grep "GPT Sol"` の期待ヒット数と、`setup/SKILL.md` がテンプレートをそのまま読む実装かどうか。後者は L39 で `{{MODEL_ALIAS}}` 置換のみと確認でき、計画から「再掲していれば追加タスク」という条件分岐を除去できた。

**実装計画書の Haiku レビュー**(方針の必須手順):

- **「最重要矛盾」として報告された 1 件は誤検出だった**。gpt-sol L3 の置換前テキストは行頭からの部分文字列で実在し、ステップにも「部分置換」と明記していた。Haiku は「置換前ブロック = 行全体」と解釈し、そのまま実行して失敗する未来を報告してきた
- ただし**同じ誤読をする実装者が実在しうる**という情報は正しいので、修正すべきは計画の記述と判断。Global Constraints に「部分置換 vs 全面置換」の区別とタスクの実行順依存を明記し、当該ステップに置換後の行全体の姿を追記した
- Haiku が挙げた「plugin.json の JSON 構文は検証できない」は**事実誤認**(`require()` は不正 JSON でパースエラーを投げる)。変更しなかった
- カバレッジ照合は「設計書 §4-1〜§4-5 / §5-1〜§5-7 の全 12 項目に対応するステップが存在、欠落なし」

# phyllis998

(実装の進め方) → **「直接実行」**

AI 側は、`cost-discipline.md` の損益分岐表「転記(完成形が全指定済み)→ オーケストレーターが直接 Write する」に該当すること、および本プロジェクトの `.claude/agents/` が空で GPT 委譲がフォールバック経路になることから、**agent-policy 自身の規律が「委譲するな」と言っている**点を根拠に直接実行を推奨した。

# AI

`superpowers:executing-plans` で実装。main ブランチだったため `feat/agent-policy-opus5-roles` を切ってから着手した。

**Task 1〜3 の結果と、期待値の誤り 1 件**:

- Task 1(with-codex)— **Step 4 の期待値が誤っていた**。計画は `11 insertions / 11 deletions` としていたが実際は `9/9`。担当表の 9 行を置換するが、`- **Sonnet**: 通常のコードレビュー。` と `- 独立したタスクが…` の 2 行は文言が変わらず差分に現れないため。差分の中身は意図どおりだったので、計画の期待値を実測に合わせて訂正してから進めた
- Task 2(claude-only)— 検証 2 件とも一発通過。両スキルの共通 3 行がすべてカウント `2`(= 完全一致)
- Task 3(根拠句削除)— 検証 2 件とも一発通過。残すべき行は各 1 件で健在、削除対象の語は 0 件

**Task 4 の期待値の誤り 1 件**:

`context-map-guide.md:41` に、スキルから削除したのと同じ根拠句(「Haiku レビューはレビュー手法の固有名であり…」)が残っていた。**設計どおりで問題なし**と判断 — §3-7 が「根拠は設計書と `references/` に置く」と定めており、references は根拠の置き場だから。スキル本文が規律だけを持ち guide が理由を持つのは意図した役割分担。私の Step 6 の期待値がこの既存行を数え落としていたので、計画を訂正した。

**Task 5 の横断 grep が設計の取りこぼしを検出(本セッションで最も価値の高い発見)**:

§3-6 で「要件確定を最上位 → 戦術へ移す」と決めたのに、設計書 §5-1 は `context-map-guide.md:17`(定義行)しか追随対象に挙げていなかった。実際には次の 2 箇所にも「最上位オーケストレーターが要件を確定する」が書かれており、放置すれば **L17 と L67 が矛盾する guide を出荷する**ところだった。

- `guide:11` 「最上位オーケストレーターが要件を確定する前には…」→ 戦術へ
- `guide:67` チェックポイント①「要件精緻化」の主担当 → 戦術へ。あわせて深さ列の「作成者が §未解決事項を要約で必ず上げる。最上位オーケストレーターは要約で受け…」を改めた(§3-2 により作成者と戦術オーケストレーターが同一になり、両者間の上流報告が消えるため)

新しい設計判断ではなく承認済み §3-6 の貫徹作業と判断し、追加コミット `a76c948` として実施。設計書 §5-1 と実装計画にも記録した。

**手を付けず報告した境界例**(判断が要るため):

- `guide:101` の例文「監査ログ保持期間は最上位オーケストレーターの判断待ち」— 承認ゲートに上げる方針事項の例として成立する
- `context-map-template.md:71` の「上流報告先」列の記入例(High=最上位 / Medium・Low=戦術)— 不可逆性の高い項目を最上位へ上げる例として成立する
- `guide:42` / `guide:68`(計画確定・計画承認の担当=最上位)— 承認ゲートは最上位に残るため正しい

**テスト**: 939 passed / 1 failed。失敗は `plugins/raphael/src/__test__/update-antibody.test.ts:121` の日付依存アサーション(`expires: "2026-08-23"` を期待)。**main に checkout して同一の失敗を再現確認**し、既存の失敗で本変更と無関係であることを確定させた。マージ後のテストもマージ前と完全に同じ結果で、新規の失敗なし。修正は作業範囲外として手を付けていない。

# phyllis998

(ブランチ処理) → **「main にローカルマージ」** / (設計書・計画書) → **「コミットする」**

# AI

docs をコミット(`d93726e`)後、main へマージしテスト再実行、ブランチ削除。main は 7 コミット進み、`agent-policy` 0.4.0-dev。

**メモリの更新**: 新規に `skill-body-discipline-only.md`(スキル本文は規律のみ・残す/削るの境界)を作成。あわせて既存の `agents-with-codex-policy.md` が「運用方針の実体はルートの `agents-with-codex.md`」と記録していたのを訂正した — 現在の CLAUDE.md は `agent-policy:with-codex` スキルを参照しており、放置すると次のセッションが「Fable がオーケストレーション・設計・監査を担う」という**今回の変更と正反対の担当表**を読みに行くため。

---

## 最終的な担当表(両スキル共通部分)

```markdown
- **Fable**: 不可逆な決定の承認ゲート(計画確定 Approve)とアドバイザー相談のみ。実務・レビューを行わない。
- **Haiku**: 設計書・計画書のレビュー(理解したこと+暗黙知抽出)。必須手順。Opus・GPT に置き換えない。
- Opus は、複数の context-map・設計書・実装差分・並列サブエージェントの全レポートを分割せず一度に読んで突き合わせる。要約の要約で判断しない。
```

`with-codex` の探索章:

```markdown
- コードベース探索は **Opus** が統括する。grep/read の反復など短命な探索実働は GPT Luna / GPT Terra にバッチ委譲し、context-map の執筆・§未解決事項の判断・要件確定への接続は Opus が行う。
```

---

注意事項・次の作業:

- `plugins/raphael/src/__test__/update-antibody.test.ts:121` のテスト 1 件が失敗中(日付依存・main でも同様・本件と無関係)。対応要否は未判断
- 作業ツリーに `.claude/settings.json` と `.raphael/antibodies/ab-2026-0724-001.md` の変更が残っている(本セッションの作業外・未コミット)
- `agent-policy:setup` を実行済みのプロジェクトは、GPT エージェント定義が旧職掌(探索統括)のままなので `setup` の再実行が必要(README に案内追記済み)
- バージョンは `0.4.0-dev` のまま。`-dev` の除去は運用検証後に判断
- main は未 push(ローカルマージのみ)
