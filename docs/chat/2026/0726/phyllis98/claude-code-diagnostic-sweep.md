# Claude Code の診断スイープと設定見直し

- 日付: 2026-07-26
- 参加者: phyllis98, AI (Claude Haiku 4.5)
- 成果物: なし (診断レポートのプレゼンテーション段階)
- 前提: なし

---

## セッション1: 診断スイープと設定見直し

# AI

Claude Code のインストール状態とプラグイン設定の包括的な診断スイープを実施。読み取り専用の検査結果を提示:

**検査結果:**
- 検査0 — インストール状態: ✅ 問題なし (native install、バージョン 2.1.220、最新)
- 検査1 — ダングリングプラグイン: `superpowers@claude-plugins-official` が `.claude/settings.json` で有効化されているが未インストール
  - 対応案: 
    - 案A: `~/.claude/settings.local.json` に `{"enabledPlugins": {"superpowers@claude-plugins-official": false}}` を追加
    - 案B: チェック済み `.claude/settings.json` から該当行を削除（チーム共有ファイルのため確認待ち）
    - 案C: 実際にプラグインをインストールしたい場合は `/plugin` で実行
- 検査2 — 重複したグローバル指示: `~/.claude/rules/context7.md` (~420 トークン、全プロジェクトで読み込まれる)
  - context7 MCP サーバーの自動注入テキスト、skill の SKILL.md、プロジェクト CLAUDE.md により3重に覆われている
  - 対応案: ファイル削除（代替カバレッジが全プロジェクトで存在するため安全）
- 検査3 — プロジェクト CLAUDE.md の簡潔性: ✅ 既に最適化済み (削除対象なし)
- 検査4 — 遅延読み込みへの移行: ✅ 不要（重要なポリシーは常時読み込み必須）
- 検査7 — バージョン: ✅ 最新
- 検査8 — auto mode 設定: user scope の `~/.claude/settings.json` に `"permissions": {"defaultMode": "auto"}` を追加することで有効化可能
- 検査9 — 読み取り専用コマンド事前承認: ✅ 対象なし（過去のdenial記録は複合文字列のため不適切）

**⚠️ 警告 (アクション不要):**
- 検査5 — フック設定の計測値: 全て高速。ただし `security-guidance` プラグインの `SessionStart` フック (timeout: 180秒) が venv 初期化時に 30-60 秒のスキップを引き起こす可能性
- 検査6 — コンテキスト常駐コスト:
  - Serena MCP ツールスキーマ: ~7.5k トークン（CLAUDE.md で使用を指示しているため意図的）
  - Context7 MCP ツールスキーマ: ~1.1k トークン（CLAUDE.md で使用を指示）
  - Skill 一覧: ~3.2k トークン（過剰ではない）

**結論:** インストール状態は清潔で最新。プロジェクト CLAUDE.md の内容は最適化済み。実際の対応が必要なのは2つ — ダングリングプラグイン削除と重複グローバル指示削除。

次のステップ: Check 1 と Check 2 についての日本語選択肢を再提示予定。

# phyllis98

> 日本語でもう一度選択肢を表示してください

---

注意事項: ユーザーが日本語での選択肢確認を要求。次のターンで Check 1, Check 2 の対応案を日本語で明確に提示する必要あり。

## セッション 2: agent-policy 改訂方針の検討

# phyllis98

> agent-policyの改訂を行います。
> 現在の方針(SKILL.mdとassets、reference)をよく読み、把握してください。
> これはすべてAIがタスクを実行する際に必ず読むものであり、人間が読むものではないため、コンテキストを圧迫したり、余計な指示を含むべきではありません。
> 現在の方針をまったく崩さず、AIに与えても無駄な根拠(人間が読む前提のもの)などや、その他重複していたり不要である要素を削除・整理したいです。
> 見本として、docs/agents-claude-only.md と docs/agens-with-codex.md があります。これくらいの粒度(これより削る場合があってもよい)で、プラグイン各文書を改訂する方針を一緒に考えてください。

# AI

agent-policy プラグインの現在の方針ドキュメント（SKILL.md、assets、reference）を読んで構造を把握しました。
サンプル（`docs/agents-claude-only.md`、`docs/agents-with-codex.md`）を参照し、
AIへの指示と人間向け説明を分離する改訂方針の相談に入ります。

## セッション 3: テンプレートの確認

# phyllis98

> 現在の方針に、各GPT Agentsのテンプレが正確に沿っているかも確認お願いします。

## セッション 4: 改訂方針への判断

# phyllis98

> 基本このプラグインは汎用的なもので、方針にもテンプレにも、プロジェクトの固有規則等を含めるべきではありません。
>
> 2について: 必要である肩書は残してよいが、不要であれば削除してください。
> 3について: Opusフォールバックを追加してください。
> 4について: こちらも対応お願いします。

## セッション 5: agent-policy 改訂の実装

# phyllis98

> 基本このプラグインは汎用的なもので、方針にもテンプレにも、プロジェクトの固有規則等を含めるべきではありません。
>
> 2について: 必要である肩書は残してよいが、不要であれば削除してください。
> 3について: Opusフォールバックを追加してください。
> 4について: こちらも対応お願いします。

# AI

改訂方針を受けて実装を進めました。

**廃止・統合**
- `references/cost-discipline.md` を削除、規則（スキルロード規律・損益分岐閾値・検証コミットの寄せ・GPTキャッシュ補正）をSKILLのコスト規律節に統合。実測根拠は削除。

**圧縮と整理**
- `context-map-guide.md`: 125行→84行。自己紹介・「強制語の扱い」節・「map作成者」節を削除、用語を表化、チェックポイント表から「作業フェーズ」「mapの役割」列を削除、例示2つは半分に圧縮。
- `setup/SKILL.md`: 案内例3例を1例に、バージョン履歴ベースの上書き判断を「役割定義が食い違う場合」という内容ベースに置換、リポジトリ固有のgitignore例示を削除。
- `context-map-template.md`: 末尾の注記と警告を1行に統合。

**テンプレートの方針整合**
- Sol / Terra / Luna から不要な肩書説明を削除。Sol の「Feature Architect」肩書削除。
- Opus フォールバック（Fable を起動できない場合は Opus）を Sol / Terra のアドバイザー節に追記。
- 「探索補助」を「探索実働」に統一。
- プロジェクト固有の Anthropic API クライアント制約を削除（CLAUDE.md由来）。
- 各テンプレートのルーティング文を3箇所から When to invoke 直後の1箇所に集約。

実装中、cost-discipline への参照は `docs/cost-survey-results.md`（人間向け文書）にのみ残す。バージョン 0.4.0-dev に据え置き。

# phyllis98

> cost-survey-resultsは、コスト規律を作成するに至った経緯などを人間向けに残す文書のため、これは削除しません。

AI改訂内容を確認、cost-survey-results.md は人間向けドキュメントのため削除しない指示。

---

注意事項: 実装作業完了。バージョン据え置き。


## セッション 6: コミット&プッシュ実行

# phyllis98

> コミット&プッシュお願いします

# AI

コミットとプッシュを実行しました。

---

注意事項: なし
