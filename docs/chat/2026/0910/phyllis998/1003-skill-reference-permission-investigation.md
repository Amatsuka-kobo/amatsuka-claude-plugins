# Claude Code スキルのリファレンス読込権限調査

- 日付: 2026-09-10
- 参加者: phyllis998, AI
- 成果物: 並列調査実行(外部情報側・実装側コード根拠。結果待ち)
- 前提: ユーザースコーププラグイン内 references の読込にユーザー許可が発生することを確認。同一参照をスキル内に配置した場合の読取経路と権限判定の相違を詳細に調査
- セッション ID: 0c527b5f-baa6-4bd3-a5be-ccb221f02a5e

---

## セッション 1: Claude Code スキルのリファレンス読込権限要件を並列調査

# phyllis998

> プラグインを使用するとき、ユーザースコープにインストールした場合referencesの文書を読むのにユーザーへの許可が必要であることがわかりました。(別プロジェクトに配置された文書であるため)
> これをskills内のスキルに配置したとき、ユーザーへの確認は発生するのかどうか、外部情報も交えて詳細に調査してください

# AI

★ Insight ─────────────────────────────────────
- Claude Code の Read 許可判定は「cwd 内か否か」が軸。ユーザースコープ プラグインは `~/.claude/plugins/cache/<marketplace>/<plugin>/<version>/` → cwd 外 → プロンプト対象。
- 一方 SKILL.md 本体は Skill tool がハーネス側でロード → Read tool を経由しない → プロンプト無し。この非対称が今回の核心。
- 争点: `skills/<name>/` 配下へ置いた補助文書を SKILL.md が参照するとき、読み取り経路が Read tool のままなのか、ハーネスの skill バンドル扱いになるのか。
─────────────────────────────────────────────────

調査 2 本 並列 dispatch。

調査 2 本 並列実行中。

- 外部情報側: 公式ドキュメント + GitHub で permission 判定規則、プラグインディレクトリの自動許可、Skill 補助ファイルの読込経路
- 実装側: ローカル Claude Code バンドルを grep → `references/` 配下 と `skills/<skill>/` 配下 の判定差の有無をコード根拠で確定

完了通知待ち。結果 突き合わせて結論出す。

調査 2 本、完了待ち。
