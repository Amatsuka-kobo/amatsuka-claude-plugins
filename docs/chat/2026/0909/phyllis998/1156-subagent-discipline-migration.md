# subagent-discipline 廃止の技術的可否と移行案

- 日付: 2026-09-09
- 参加者: phyllis998, AI (Claude Haiku 4.5)
- 成果物: agent-policy のコードベース探索、hook 注入方式の分析、orchastration-discipline 統合の技術的課題の特定
- 前提: `plugins/agent-policy/references/orchestration-discipline.md`、`plugins/agent-policy/references/subagent-discipline.md`、`plugins/agent-policy/src/hooks/subagent-start.ts`
- セッション ID: 17d04b61-2e19-44c4-9220-6c1b39ee542f

---

## セッション 1: subagent-discipline 廃止可否と移行案の調査

# phyllis998

> agent-policyのorchestration-disciplineには、サブエージェントの規律が含まれますが、それとは別にsubagent-disciplineも存在します。サブエージェントを起動する側がその規律を直接注入する形にし、subagent-disciplineを廃止したいです。まずはそれが可能かどうか調査してください

# AI

探索を `system-planner`（コードベース探索統括）へ委譲。並行して、既読 3 ファイルからの初期所見。

`★ Insight ─────────────────────────────────────`
- `subagent-discipline.md` は単なる参照文書ではなく **hook の注入テンプレート**。`plugins/agent-policy/src/hooks/subagent-start.ts:271` あたりで `CLAUDE_PLUGIN_ROOT/references/subagent-discipline.md` を読み、`<!-- marker-table -->` 行を役割マーカー対応表で差し替えて `additionalContext` に流す。
- 注入は条件付き。対象 Agent の `tools` に `Agent` が無いときと、ビルトイン `Explore`/`Plan` のときはスキップ。つまり「再委譲しうるサブエージェント」だけが受け取る設計。
- `orchestration-discipline.md` は冒頭で既に「『サブエージェントは〜』で始まる条項は、依頼文への転記と、生成した定義の本文でサブエージェントへ届ける」と宣言済み。**転記経路は既に規律として存在する**。
`─────────────────────────────────────────────────`

## 初期所見

**条項の重複度 — 5 条項中 4 つは orchestration-discipline に既出**

- 再委譲先は対応表優先 → §担当表末尾「SessionStart フックが役割マーカーの対応表を注入したときは…」に相当
- アドバイザーは対応表 advisor / 無ければ Fable / 起動不可なら差し戻し → §モデル別役割の運用 に同文
- アドバイザーに Agent tool 不許可・「助言のみ」明記 → 同上に同文
- 指定スキルだけロード → 同上に同文
- 読み取り作業を実装役割へ再委譲時の明記 → オーケストレーター向け条項として類似形あり（サブエージェント視点の記述は無い）

**廃止の技術的な壁は条項ではなく `<!-- marker-table -->`**

役割マーカー対応表は hook が実行時にスキャンして生成する動的データ。文書に静的に書けない。廃止するなら、起動側が dispatch のたびに対応表を依頼文へ転記する必要がある。

その転記は可能。オーケストレーターは SessionStart で対応表を受け取っている（このセッションでも受け取り済み）。ただし**再委譲するサブエージェントは対応表を hook からしか受け取っていない**ため、親が依頼文へ載せない限り孫は対応表を持てない。ここが設計の分岐点。

## 判断待ちの論点

いま探索へ出しているのは、この壁の周辺:

1. hook 注入方式を選んだ理由が設計書に明示されているか（「依頼文転記では不十分」と判断した記録があるか）
2. `subagent-start.test.ts` が fragment の何を固定しているか
3. hooks.json・build.ts・README・plugin.json の追随範囲

探索結果が返り次第、可否の結論と移行案を出す。

探索エージェントの結果待ち。返り次第、廃止可否と移行案を報告する。

## セッション 2: 案A採用・設計書の起草要件を確定し system-planner へ委譲

# AI

探索エージェント実行中。結果待ち。

探索結果が返った。判断材料を自分で突き合わせる。衝突点だけ直接確認する。

探索完了。衝突点も自分で確認した。結論を出す。

`★ Insight ─────────────────────────────────────`
- hook が運んでいるのは **2 種類の荷物**。(A) 静的な規律 5 条項、(B) 実行時スキャンで作る役割マーカー対応表。ユーザーの要望は (A) だが、廃止判断を左右するのは (B)。
- 決定的な事実: `subagent-start.test.ts:465-486` が「**大きな対応表の下では条項 3 が現に届いていない**」ことをコメント付きでテストに固定している。`MAX_CONTEXT_CHARS = 9500` の切り詰めが after 側の規律行から落とすため。hook 注入は確実な配布チャネルではない。
- `orchestration-discipline.md:3` が既に「『サブエージェントは〜』で始まる条項は、依頼文への転記と、生成した定義の本文でサブエージェントへ届ける」と宣言済み。転記経路は新設ではなく **既存規律の一本化**。
`─────────────────────────────────────────────────`

## 結論: 可能。実装改修は 2 箇所のみ

context-map: `.claude/context-maps/2026-09-09-subagent-discipline-removal.md`

`subagent-discipline.md` への参照は全 95 箇所あるが、**編集対象になる実装は 2 箇所だけ**。

- `src/hooks/subagent-start.ts:263` — 実行時読み込み
- `src/hooks/__test__/subagent-start.test.ts:230` — テスト用複製

`skills/` `agents/` `commands/` からの参照は 0 件。`build.ts` は `references/` をバンドルしない。README 本文に文字列 `subagent-discipline` は無い。残り 93 箇所は `harness-docs/` 34・`docs/` 14・`.raphael/` 45 の記録類で、追随不要。

### 条項の重複 — 「対応なし」はゼロ

| 条項 | orchestration-discipline | 転記宣言(L3)の射程 |
|---|---|---|
| 2 アドバイザーの解決 | L55 に逐語ほぼ一致 | 内（主語「サブエージェントは」）|
| 3 アドバイザーに Agent tool 不許可 | L57 に逐語ほぼ一致 | 内 |
| 5 指定スキルだけロード | L58-59 に逐語ほぼ一致 | 内 |
| 1 再委譲先は対応表優先 | L60 に部分対応 | **外**（主語「SessionStart フックが〜」）|
| 4 読み取り再委譲時の明記 | L51-53 に部分対応 | **外**（主語「オーケストレーターは〜」）|

条項 1・4 は主語を「サブエージェントは」へ書き換えれば射程に入る。数行の手直し。条項 4 は `assets/roles/ja/explore-lead.md:21` が既に同等文を持つ。

## 推奨: 断片ファイルだけ廃止し、hook は対応表専用として残す

3 案の比較。

| 軸 | 案A 断片のみ廃止 | 案B hook ごと廃止 | 案C 現状維持 |
|---|---|---|---|
| ユーザー要望の充足 | ○ | ○ | × |
| 対応表の機械的保証 | 維持 | **失う**（転記漏れ＝孫が対応表を持てない）| 維持 |
| 切り詰め問題 | 緩和（9500 枠を対応表が独占）| 消滅 | 残る |
| 改修規模 | 小（fragment 読み込み系 4 シンボル削除、テスト 17 ケース差し替え）| 中（hook 実装・hooks.json・build.ts・scripts 再生成）| ゼロ |
| 規律の重複 | 解消 | 解消 | 残る |

**案A を推奨**。対応表は動的データで文書に静的に書けず、`delegation-gate` は `agent_id` があれば return するのでサブエージェントに発火しない。SubagentStart が唯一の自動経路であり、これを失うと再委譲する孫エージェントは親の転記に完全依存する。規律テキストの転記漏れは「規律を知らない」で済むが、対応表の転記漏れは「委譲先が解決できない」で作業が止まる。

## 判断待ちの論点 3 件

**1. 本日の設計決定との衝突**

`harness-docs/design/2026-09-09-agent-policy-band-catalog-consolidation-design.md` §6.3 が今日「`subagent-discipline.md` L10 は変更せず残す」を決定済み。対の実装計画は「無変更であること」を T16 の確認項目に据えている（計画自体は直近 2 コミットで完了済みと見られるが、`orchestration-discipline.md` に未コミット差分 34+/36− が残る）。

ただし §6.3 の根拠「L10 を削除すると届く経路が**完全に消える**」には 2 つの綻びがある。

- `explore-lead.md:21` が同等文を持つため、explore-lead 役割については既に二重経路
- 切り詰めにより、大きな対応表の下では規律行が**現に届いていない**（テストが固定している事実）

§6.3 は「配布経路は現状のまま、文書の重複を削るか」への答えであり、本件は「配布経路そのものを変えるか」を問うている。問いが違うので覆すというより上書きになるが、記録として整理が要る。

**2. 未コミット差分の扱い**

`orchestration-discipline.md` の見出しが working tree で「§役割の帯」→「§担当表」へ改名済みだが、`skills/custom-policy/SKILL.md:10` は依然「§役割」を指す。参照ずれが未解消。本件に着手する前に、この差分をコミットするか破棄するかを決める必要がある。

**3. 転記漏れの検知手段**

hook は spawn ごとの機械的保証、転記は遵守依存。`two-profile-design` §2.3 に「スキル本文の命令形強化・最優先宣言・output style は効果なし、効いたのは PreToolUse deny のみ」という実測が残っている。ただしこれは自動委譲の抑止に関する測定で、依頼文本体に書いた指示の遵守とは別。必要なら `PreToolUse: Agent` に nudge を足す選択肢がある。

なお、「依頼文への転記だけでは不十分」と断定した記述は、指定 5 文書のどこにも**無い**。二段フックの P0 は「role map の全階層伝播」であり、規律の配布はそこへの相乗り。

---

案A で進めるか、案B まで踏み込むか。論点 1・2 の扱いも含めて指示があれば、設計書の起草へ移る。

調査結果は前ターンの報告のとおり。案A（断片ファイルのみ廃止・hook は対応表専用として残す）を推奨。判断待ちは 3 件（§6.3 との衝突・未コミット差分の扱い・転記漏れの検知手段）。指示を待つ。

調査は完了済み。結論は前々ターンの報告のとおり。

**要点**: `subagent-discipline.md` の廃止は可能。実装の改修対象は 2 箇所のみ（`src/hooks/subagent-start.ts:263` と `src/hooks/__test__/subagent-start.test.ts:230`）。5 条項すべてに `orchestration-discipline.md` の対応記述があり、うち 2 条項は主語を「サブエージェントは」へ直せば既存の転記宣言（L3）の射程に入る。

**推奨は案A** — 断片ファイルだけ廃止し、SubagentStart hook は役割マーカー対応表の注入専用として残す。対応表は実行時スキャンで作る動的データで文書に書けず、サブエージェントへ届く自動経路が SubagentStart しか無いため。

**着手前に決めることが 3 件**:
1. 本日の設計書 §6.3「`subagent-discipline.md` L10 は残す」との衝突をどう記録するか
2. `orchestration-discipline.md` の未コミット差分（34+/36−、見出し改名による SKILL.md の参照ずれ未解消）を先に片付けるか
3. 転記漏れの検知手段を置くか（`PreToolUse: Agent` の nudge）

指示があれば設計書の起草へ移る。

# phyllis998

> Aで進めてください

# AI

案A で進める。設計書の起草前に、未コミット差分の中身を確認する。

未コミット差分の実態を確認した。`## 役割` → `## 担当表` の見出し改名とテーブル整形で、直近コミット群の続き。本件と矛盾しないので、この上に積む。

`★ Insight ─────────────────────────────────────`
- 未コミット差分は冒頭の定義文「以下で『担当表』とは、この文書の §役割 の表を指す」を**削除**している。一方 `skills/claude-model-policy/SKILL.md:10` は今も「その文書の §役割 にある」を指す。参照先の節名が消えた状態。本件と同じファイル群を触るので、随伴修正の候補として設計書に載せる。
- `references/` は現在 23,651 B。`subagent-discipline.md` は 919 B しかない。**削除の動機はサイズではなく、配布経路の一本化**。設計書の根拠をサイズ削減に置くと筋を誤る。
`─────────────────────────────────────────────────`

要件を確定させ、設計書の起草を `system-planner`（設計書・実装計画書の作成）へ委譲する。

設計書の起草を委譲した。渡した要件は以下。

**確定事項（設計書で覆させない）**
- 案A 採用。案B（hook ごと廃止）は不採用として記録させる
- `subagent-discipline.md` 削除、SubagentStart は対応表専用へ
- `orchestration-discipline.md` の条項 1・4 を「サブエージェントは」主語へ整備し、L3 の転記宣言の射程に入れる
- テスト 17 ケースの個別処理、README 追随、バージョン更新

**設計書に判断させる 4 論点**
1. §6.3 の上書き方法（既存設計書は書き換えず、新設計書で上書き）
2. 転記漏れの検知手段（`PreToolUse: Agent` の nudge、`delegation-gate` への相乗り可否）
3. `SKILL.md` の `§役割` 参照ずれの随伴修正
4. バージョンの上げ幅

設計書の中身は通常の日本語で書かせている。原始人口調が要るなら言ってくれ。

出来上がり次第、`haiku-reviewer` → `grok-docs-reviewer`（独立レビュー）の順で通し、両者の指摘の採否を判断してから提示する。

設計書の起草を待っている。完成したらレビュー 2 本を通して提示する。

## セッション 3: サブエージェント規律：方針A決定と設計委譲

# phyllis998

> SKILL.mdも修正しているので確認してください

# AI

`custom-policy/SKILL.md` が変更されている。会話開始時の git status には無かった。差分を確認する。

`custom-policy/SKILL.md` の変更内容と、それが問題である理由を確認した。

`★ Insight ─────────────────────────────────────`
- 変更は L10 の一文 **削除**。`担当表(役割名・RoleId・種別・Agent Tool の可否・Claude モデル)はその文書の §役割 にある。` が丸ごと消えた。
- mtime は `12:13:55`（設計書エージェント実行中）。パーミッションも `644` で、他のファイルの `664` と異なる。書き手が違う。
- `claude-model-policy/SKILL.md` は mtime `11:07:43` で未変更。**同じ一文を今も持っている**。片方だけ消えて非対称になった。
`─────────────────────────────────────────────────`

## この変更の評価: 不完全。戻すべき

**問題 1 — 本日の設計決定 §6.2 と衝突する**

`band-catalog-consolidation-design` §6.2 は「表の所在を指す 1 文を冒頭に置き、既存の L10『共通規律を併せて読み、これに従う』と合わせて**到達性を担保する**」と決めている。今回の変更はその「所在を指す 1 文」を削除しており、決定の趣旨に反する。

参照ずれ（`§役割` という節名が `§担当表` へ改名済み）を直す方法は 2 つある。削除ではなく **`§担当表` への書き換え**が §6.2 に沿う。

**問題 2 — 非対称**

2 つの SKILL が同じ役割の一文を持つのに、`custom-policy` だけ消えた。`claude-model-policy` は壊れた参照（`§役割`）を保持したまま。どちらの状態も正しくない。

**問題 3 — 制約違反の可能性**

設計書エージェントへの依頼文には「あなたが書いてよいファイルは設計書 1 本だけ。プラグインのソース・references・テスト・README・既存の設計書は一切変更しない」と明記していた。SKILL.md の随伴修正は**論点 3 として判断させる対象**であり、実行させる対象ではない。

エージェントへ事実確認を送る。

現状の事実:

| ファイル | 状態 | mtime |
|---|---|---|
| `custom-policy/SKILL.md` | L10 の所在案内が**削除**済み | 12:13:55 |
| `claude-model-policy/SKILL.md` | 壊れた参照 `§役割` を**保持** | 11:07:43（未変更）|
| `orchestration-discipline.md` | 見出しが `§担当表` へ改名済み | 11:56:01 |

3 つが揃って初めて整合するが、今はどれとも噛み合っていない。

**推奨**: `custom-policy/SKILL.md` の変更を revert し、本件の随伴修正として両 SKILL を同時に `§担当表` へ書き換える。設計書の論点 3 で正式に扱わせる。

revert する場合は `git checkout -- plugins/agent-policy/skills/custom-policy/SKILL.md` になるが、この変更がユーザー自身の手によるものなら消してはいけない。エージェントへ確認を送ったので、その回答を待つ。

**確認したいこと**: `custom-policy/SKILL.md` の変更は、ユーザー自身が行ったものか。それともエージェントの逸脱か。前者ならそのまま活かして `claude-model-policy` 側を揃える方向で設計させる。

前ターンの確認事項を繰り返す。

`custom-policy/SKILL.md` の変更（L10 の所在案内の削除）は、あなた自身の手によるものか、設計書エージェントの逸脱か。

- **あなたの変更なら** — そのまま活かし、`claude-model-policy/SKILL.md` 側も揃える方向で設計させる
- **エージェントの逸脱なら** — revert し、両 SKILL を `§担当表` へ書き換える案を設計書の論点 3 で正式に扱わせる

エージェントへ事実確認を送ってあるので、回答が返れば判別できる。設計書の執筆自体は続行中。

## セッション 4: 政策スキル参照の非対称性を確認、統合方案の決定を待機

# AI

設計書エージェントと、送った事実確認の回答を待っている状態。

未解決のまま残っているのは 1 点 — `custom-policy/SKILL.md` の変更があなたの手によるものか、エージェントの逸脱か。回答があれば、それに応じて設計を進める。

# phyllis998

> 私が自分で行った変更です

# AI

了解。revert しない。私が「制約違反の可能性」「戻すべき」と書いたのは誤り。エージェントへ訂正を送る。

残る問題は非対称だけ。`claude-model-policy/SKILL.md` は同じ一文を今も持っている。

訂正を送った。

## 現状の整理

| ファイル | 状態 |
|---|---|
| `orchestration-discipline.md` | 見出しが `§担当表` へ改名済み。冒頭の定義文は削除済み |
| `custom-policy/SKILL.md` | **あなたの変更**。所在案内の一文を削除。参照ずれ解消 |
| `claude-model-policy/SKILL.md` | 未変更。壊れた参照 `§役割` を保持 |

`claude-model-policy` を `custom-policy` に揃えるのが自然だが、`band-catalog-consolidation-design` §6.2 が「表の所在を指す 1 文を冒頭に置き、到達性を担保する」と決めているため、揃えるならその決定を本設計書で上書きする形になる。設計書に 2 案を比較させ、推奨を出させる。

なお `claude-model-policy` は claude プロファイル専用で、このセッション（custom プロファイル）では読み込まれない。実害が出るのは claude プロファイルのセッションだけ。

設計書の完成を待つ。

設計書エージェントの完成を待っている。訂正済みの前提（`custom-policy` の変更は既定、`claude-model-policy` の扱いを 2 案比較）で執筆が続いている。

完成したら `haiku-reviewer` → `grok-docs-reviewer` の順でレビューを通し、両者の指摘の採否を判断してから提示する。

## セッション 5: 政策スキル 2 点を同期、参照ずれ再発の経路を塞ぐ

# phyllis998

> claude-model-policyも修正しました

# AI

両方修正済み。非対称は解消。ただし `claude-model-policy` に句点が 2 つ残っている（`これに従う。。`）。タイプミスなので直す。

句点を 1 文字削除。両 SKILL が同一の文面になった。

`★ Insight ─────────────────────────────────────`
- これで 3 ファイルが揃った。共通規律の見出しは `§担当表`、両 SKILL は節名を指さず「共通規律を併せて読み、これに従う」だけ。**節名への参照そのものが消えた**ので、今後 `orchestration-discipline.md` の見出しを変えても SKILL 側の追随が要らなくなる。参照ずれの再発経路が塞がった。
- 副作用として、`band-catalog-consolidation-design` §6.2 が担保しようとした「表の所在を指す 1 文」は無くなった。到達性は「共通規律を全文読む」前提に一本化された。この設計変更は本件の設計書に記録が要る。
`─────────────────────────────────────────────────`

設計書エージェントへ論点 3 の消滅を通知した。残る論点は 3 つ（§6.2/§6.3 の上書き記録、転記漏れの検知手段、バージョンの上げ幅）。

完成したらレビュー 2 本を通して提示する。

設計書エージェントの完成待ち。前提は 3 度更新済み（案A 確定、`custom-policy` はユーザー変更、`claude-model-policy` も揃って論点 3 消滅）。

完成次第、`haiku-reviewer` → `grok-docs-reviewer` を通して提示する。

設計書エージェントの完成を待っている。

## セッション 6: 政策スキル 2 点を同期、参照ずれを塞いだ

# AI

設計書エージェントの完成を待っている。

設計書が完成した（66 KB / 13 章）。レビュー 2 本を並列で起動する。

設計書が完成し、レビュー 2 本を並列で走らせている。

## 設計書の判断（4 論点）

| 論点 | 結論 |
|---|---|
| §6.3 の上書き | §1.1 に上書き表。L260「経路が完全に消える」を**事実誤認として訂正**。反証 2 点を明記 |
| 転記漏れの検知 | **置かない**。`delegation-gate` の matcher は `Edit\|Write\|NotebookEdit\|mcp__.*` で `Agent` を含まず、相乗り不成立。§12-1 へ先送り |
| SKILL.md | 適用済みを確定事項として §1.2 に記録。§6.2 の上書き |
| バージョン | `0.17.1-dev` → **`0.18.0-dev`**（マイナー）|

## 依頼文の前提に 5 件の食い違いが見つかった

設計書エージェントが実物で裏を取り、私が渡した前提のうち 5 件を訂正している。

1. **実装計画は「進行中」ではなく完了済み** — `a01c39d` / `183774d` でコミット済み。着手順序の調整は不要
2. **テスト件数が違う** — 「28 ケース中 17 件」ではなく、`it.each` 展開後 **38 ケース中 13 件**が断片依存。私が context-map の数字をそのまま渡した
3. **`delegation-gate` への相乗りは前提が成立しない** — `PreToolUse: Agent` に登録されているのは `parallel-nudge.ts` の方
4. **隠れた分岐があった** — 断片が消えると非 custom プロファイルの `additionalContext` が `NO_MARKERS` の 1 行だけになる。§6.2 で注入維持を選択
5. **README L11 は変更不要** — 記述が実体と一致していた

`★ Insight ─────────────────────────────────────`
- 2 の食い違いは、`it.each` を使ったテストの数え方の問題。context-map 作成時は `it` の記述箇所を数え、設計書執筆時は展開後のケース数を数えた。テスト整理の設計では**展開後の数**が正しい — 削除・差し替えの単位が展開後のケースだから。
- 3 は私の依頼文の誤り。「delegation-gate へ相乗りできるか検討せよ」と書いたが、そのフックは `Agent` を matcher に持たない。実物を読ませたことで潰れた。
`─────────────────────────────────────────────────`

## 未解決事項 7 件

判断待ちとして上がっているもの:

1. 転記漏れ検知を後から足す判断時期と基準
2. `hooks/hooks.json` L2 の description 修正の採否（要件外の随伴修正）
3. `bundledAgentsDir()` の死んだ経路（常に 0 件）を残すか
4. `README.md` L210 / L214 が共通規律の節を「「役割」節」と呼ぶ食い違い
5. `orchestration-discipline.md` L58 と L59 の規律重複
6. `grok-researcher` が廃止済みとされながら対応表に出続けている件
7. 過去の会話記録の「残す」結論を上書きした旨を記録に残すか

レビュー 2 本が返り次第、指摘の採否を判断し、設計書を読んだ上で提示する。

Haiku レビューが返った。指摘の 1 つ（コミットハッシュが確認できない）を先に検証する。

Haiku レビューが返った。指摘の 1 件は誤りと判明した。

**指摘 5-1「コミットハッシュ `a01c39d` / `183774d` が git log で確認できない」は誤り**。両方とも実在する。Haiku は tools が `Read` / `Grep` / `Glob` に限られ `Bash` を持たないため、git log を引けなかった。設計書の欠陥ではない。

## 実質的な指摘（採否は独立レビューと突き合わせてから確定）

| # | 深刻度 | 内容 |
|---|---|---|
| 2 | **high** | §7.5 新設 2「サブエージェントは、再委譲するとき、**自身が受け取った**『サブエージェントは〜』で始まる条項を転記する」の射程が不明。親から渡された分だけか、5 条項全部か、生成定義の本文由来かで転記セットが変わる |
| 1 | medium | 所在案内の削除と到達性の担保が逆向き。「共通規律は冒頭から読む」前提に依存するが、同じ設計書が「他の節から読み始めた読者は所在を知る手掛かりを失う」と認めている |
| 6 | medium | 条項 4 の到達経路が `explore-lead` のみ。`complex-impl` など他の生成定義を持つ役割でも `_common.md` に条項 4 が無ければ依頼文転記が唯一の経路になる点が、リスク表に書かれていない |
| 4 | medium | §9.1 No. 14 の「fixture 簡素化」の中身が曖昧。テスト先行（T1）で書けない |
| 3 | medium | 条項 1 の主語変更で「注入元が SessionStart か SubagentStart か」の区別が文面から消える |

指摘 2 が最も重い。転記の射程が決まらないと、実装者が条項セットを判断で埋めることになる。

独立レビュー（`grok-docs-reviewer`）の結果を待って、両者を突き合わせてから採否を決める。

独立レビューが「このままでは着手すべきではない」と判定した。指摘の中で即座に検証できるもの（F11: 既存テストが working tree で既に落ちているはず）を確認しつつ、設計書本体を読む。

既存テストが**既に落ちている**。独立レビューの F11 が的中した。

```
FAIL plugins/agent-policy/src/agents/__test__/discipline-role-table.test.ts
Error: 規律の「役割」節が見つからない
Tests  8 failed | 5 passed (13)
```

独立レビューの中核指摘（claude/none プロファイルで規律の自動経路が消える）を裏取りする。

独立レビューの中核指摘はすべて裏が取れた。設計書のままでは着手できない。

## 1. 先に潰すべき問題 — テストが既に落ちている

```
FAIL  src/agents/__test__/discipline-role-table.test.ts
Error: 規律の「役割」節が見つからない   ← /^## 役割$/m を要求
Tests  8 failed | 5 passed (13)
```

原因は本件ではなく、`orchestration-discipline.md` の未コミット差分（`## 役割` → `## 担当表`）にテストが追随していないこと。本件に着手する前に、この差分を確定させる必要がある。

## 2. 設計の根幹が崩れている — claude / none プロファイルで規律が全消失する

裏取りした事実を並べる。

**`two-profile-design` §6.4 が「規律断片は全分岐で配布を続ける」を明示的に決定していた**

> 対応表の合成だけ injection に連動させる … **規律断片(サブエージェント宣言・アドバイザー規律等)は全分岐で配布を続ける。** claude 運用に旧 custom 定義が残っている場合に、子がマーカー表を根拠に GPT / Grok へ再委譲して沈黙故障が復活する経路を塞ぐ。

テストがこの契約を固定している。

```
it.each(["custom", "none"])("%s の分岐でも規律断片を配布する", ...)
  expect(...).toContain("あなたはサブエージェントである")
```

設計書は `consolidation` §6.2 / §6.3 だけを上書き対象にし、**この決定を上書き表に入れていない**。断片の存在理由そのものである決定を、記録せずに捨てることになる。

**claude プロファイルでは代替経路が 1 つも無い**

| 経路 | custom | claude / none |
|---|---|---|
| SubagentStart の対応表 | 配られる | `NO_MARKERS` の 30 字のみ（`isCustomInjection` が偽）|
| `markerTable()` 冒頭行（条項 1 相当）| 乗る | **乗らない**（表を組まない）|
| 生成定義 `_common.md`（条項 0/2/3/5）| 届く | **無い**。委譲先は `general-purpose` / `Explore` |
| 依頼文への転記 | 唯一 | 唯一 |

`claude-model-policy/SKILL.md` は「`readonly` の役割はビルトイン `Explore`、`impl` の役割は `general-purpose` へ委譲する」と書いている。`general-purpose` は設計書 §10 が言う「定義が無い役割の受け皿」ではなく、**claude プロファイルの impl 全役割の第一委譲先**。

つまり廃止後、claude / none では条項 2/3/4/5 が機械的に届く経路がゼロになる。設計書 §6.7 が「nudge を置かない」根拠にした「条項 1 は `markerTable()` が運ぶ」も、このプロファイルでは成立しない。

`★ Insight ─────────────────────────────────────`
- 設計書 §2 の「規律の配布は断片の主目的ではなく、対応表の配布への**相乗り**である」という再解釈が、崩壊の起点。two-profile §6.4 を読むと、規律配布は相乗りではなく**独立した決定事項**として書かれている。過去の決定を「相乗りだった」と読み替えたことで、上書き表から漏れた。
- 独立レビューが機能した典型例。Haiku レビューは文書内の整合性（射程の曖昧さ、章間の矛盾）を拾ったが、**文書の外にある過去の決定との衝突**は独立レビューだけが見つけた。設計書だけを読んでいたら通っていた。
`─────────────────────────────────────────────────`

## 3. 選択肢

claude / none で条項 2/3/4/5 の機械的保証を失うことを、受け入れるかどうかが分岐点。

| 案 | 内容 | claude/none の規律 | ユーザー要望の充足 |
|---|---|---|---|
| **A-1** | 受容して廃止。README で告知 | 転記のみ | ○ |
| A-2 | 廃止 + `PreToolUse: Agent` の nudge を同時新設 | 転記 + nudge（効果未検証）| ○ |
| A-3 | claude/none だけ断片配布を残す | 現状維持 | △（ファイルが残る）|
| C | 廃止を見送り、主語整備だけ行う | 現状維持 | × |

**推奨は A-1**。根拠は 2 つ。

- §6.4 が塞ごうとした「子がマーカー表を根拠に GPT/Grok へ再委譲する沈黙故障」は、対応表を custom 限定にした時点で既に塞がれている。claude では表そのものが配られない。断片の全分岐配布は二重の保険であり、一次の防御ではない。
- このリポジトリの運用は custom。custom では `_common.md` が条項 0/2/3/5 を運ぶ。実害を受けるのは claude / none プロファイルの利用者で、README の移行節で告知できる。

ただし「claude プロファイルの利用者が受ける機械的保証の喪失を受け入れる」判断は、プラグイン提供者であるあなたのもの。A-2 を採れば喪失を埋められるが、nudge の効果は未検証で、全 spawn の固定費になる。

## 4. 判断待ち

1. **未コミット差分をどうするか** — コミットするか、テストを追随させるか。本件着手の前提
2. **A-1 / A-2 / A-3 / C のどれで進めるか**
3. `two-profile` §6.4 と `2026-08-27` §5 の内容契約を、上書き表へ追加する（どの案でも必須）

方針が決まれば設計書を改訂させる。数値のずれ（F1〜F5: 参照回数 12→9、ブロック数 27→28、削除ブロック 3→4、サイズ見積 +798→+904 B）は改訂時にまとめて訂正させる。

# phyllis998

> claude-model-policyの"担当表の「Claude モデル」列のモデルを、dispatch 時の `model` 上書きで指定して起動する。担当表の「種別」が `readonly` の役割はビルトイン `Explore`、`impl` の役割は `general-purpose` へ委譲する。"は誤りです。claude-model-policyでも、ビルトインエージェントは第一候補になりません。

# AI

`claude-model-policy` の記述自体が誤りとのこと。私の前ターンの分析はその記述を前提にしていたので、結論が変わる。正しい姿を確認する。

`claude-model-policy` L18 と `custom-policy` L21 が同じ記述を持っている。両方とも「`readonly` はビルトイン `Explore`、`impl` は `general-purpose` へ委譲する」。

## 私の分析への影響

前ターンで「claude プロファイルでは委譲先が `general-purpose` / `Explore` なので、生成定義経由の規律が届かない」と結論したが、これは誤った記述に基づいていた。委譲先がプロジェクトの Agent 定義なら、その定義本文が規律を運ぶ経路が残る。

ただし完全には消えない問題が 2 つある。

- `markerTable()` が claude / none では呼ばれないため、条項 1 相当の冒頭行は届かない（独立レビュー F6）
- `_common.md` 由来の定義は `setup-agents`（custom 専用）が生成する。claude プロファイルの利用者が持つ定義に規律が入っている保証はない

正しい解決順が分からないと、設計書の到達性モデルを直せない。

方針が決まった。設計書の改訂を指示する。両レビューの指摘は私が採否を判断して渡す。

`★ Insight ─────────────────────────────────────`
- 「役割マーカー付き定義を第一候補にする」なら、claude プロファイルでも生成定義本文が規律を運ぶ経路が残る。前ターンで「規律が全消失する」と書いた結論は、訂正後の前提では**成立しない**。ただし対応表が注入されない以上、条項 1 相当（`markerTable()` 冒頭行）だけは依然として届かない。
- この訂正は本件の到達性モデルの土台なので、設計書の §4.2 到達経路表と §10 リスク表を書き直させる必要がある。
`─────────────────────────────────────────────────`

# phyllis998

> この問題はsetup-agentsにも波及します。このスキルはcustomしか見ていません。

> このスキルのウィザードでClaudeモデルだけか、カスタムモデルも使用するかを選ぶ必要があります

# AI

問題が連鎖している。全体像を整理する前に、`setup-agents` の規模を確認する。

`setup-agents` は 29 KB のスキル + `src/agents/` の 7 モジュール。ただし `claudeEnums` は既に候補として扱っている（`ok` の真偽にかかわらず常に返る）。Claude モデルだけを選ぶこと自体は既に可能で、欠けているのは**ウィザードの分岐と案内文の出し分け**。

## 問題が 4 段に連鎖している

```
E. テストが既に赤（未コミット差分にテストが未追随）        ← 独立。今すぐ潰せる
      ↓
C. setup-agents が custom 専用（Claude だけの構成を選べない）
      ↓
F. claude プロファイルでも対応表が要るのでは？             ← 未決。ここが本丸
      ↓
B. 両 SKILL.md の委譲先解決が誤り（マーカー付き定義が第一候補）
      ↓
A. subagent-discipline 廃止（元の依頼）
```

`★ Insight ─────────────────────────────────────`
- **F が本丸**。claude プロファイルで役割マーカー付き定義を第一候補にするなら、どの定義がどの役割かを知る手段が要る。現状 `isCustomInjection` が偽のときは対応表を組まないので、claude では役割→定義の対応が誰にも分からない。
- しかし `two-profile-design` §6.4 は対応表を custom 限定にした理由を明記している — 「claude 運用に旧 custom 定義が残っている場合に、子がマーカー表を根拠に GPT / Grok へ再委譲して**沈黙故障が復活する経路を塞ぐ**」。単純に claude へ広げるとこの防御が外れる。
- 両立させる道はある: claude プロファイルで対応表を組むとき、**`model` が Claude enum でない定義を除外する**。沈黙故障の経路（GPT/Grok 定義への再委譲）はそれで塞がる。ただしこれは `two-profile` §6.4 の決定を書き換える変更。
`─────────────────────────────────────────────────`

## 元の依頼との関係

A（`subagent-discipline` 廃止）は、F の答えに依存する。

- claude で対応表を注入するなら → `markerTable()` 冒頭行が全プロファイルで届く → 条項 1 の到達経路が回復し、設計書 §6.7 の根拠が成立する
- 注入しないなら → claude では対応表も規律も届かず、転記だけが頼りになる

つまり F を決めずに A の設計書を改訂しても、到達性モデルを書けない。

## 進め方の判断が要る

規模が違う 3 つが混ざっている。

| | 内容 | 規模 |
|---|---|---|
| E | `discipline-role-table.test.ts` を `## 担当表` へ追随 | 数行 |
| C+F+B | プロファイル設計の見直し（setup-agents の claude 対応、対応表の配布範囲、委譲先解決の訂正）| 大。`two-profile` §6.4 を覆す |
| A | 規律断片の廃止 | 中。設計書は書けているが到達性モデルの書き直しが要る |

**推奨は分割**。E を今すぐ潰し、C+F+B を独立した設計として起こし、A はその後に改訂する。理由は、C+F+B が `two-profile-design` のプロファイル設計そのものの改訂であり、規律断片の廃止とは判断軸が違うから。1 つの設計書に詰めると、レビューも実装も粒度が粗くなる。

ただし「連鎖しているから一体で見たい」という判断もあり得る。

**確認したいこと 2 点**:

1. **claude プロファイルで対応表を注入するか。** 注入するなら、Claude enum 以外の `model` を持つ定義を除外して `two-profile` §6.4 の防御を保つ形でよいか
2. **C+F+B を A と分けるか、1 つの設計書にまとめるか**

どちらも答えが出れば、設計書エージェントへ改訂または新規起草を指示する。

前ターンの確認事項を待っている。

1. **claude プロファイルで対応表を注入するか** — 注入するなら、Claude enum 以外の `model` を持つ定義を除外して `two-profile` §6.4 の「沈黙故障を塞ぐ」防御を保つ形でよいか
2. **`setup-agents` の claude 対応・委譲先解決の訂正を、`subagent-discipline` 廃止と分けるか、1 つの設計書にまとめるか**

どちらも答えが出れば、設計書の改訂または新規起草を指示する。

なお `discipline-role-table.test.ts` が現在 8 件落ちている件（`## 役割` → `## 担当表` の未追随）は、どの方針でも先に潰す必要がある。指示があれば今すぐ直す。

## セッション 7: 役割対応表の注入範囲と設計書の分割戦略を判断する

# AI

2 点の確認待ち。

1. **claude プロファイルで対応表を注入するか**（Claude enum 以外の `model` を持つ定義を除外する形で `two-profile` §6.4 の防御を保つか）
2. **`setup-agents` の claude 対応と委譲先解決の訂正を、`subagent-discipline` 廃止と分けるか、まとめるか**

`discipline-role-table.test.ts` が 8 件落ちている件も、指示があれば先に直す。

# phyllis998

> 先に確認させてください。
> 注入される対応表の役割とは、使用可能なエージェントのうちrole-idを持つエージェントを知らせ、移譲元が迷わないようにするためで合ってますか？

# AI

対応表の実装を読んで正確に答える。

はい、その理解で合っています。実装がそのとおりになっています。

`markerTable()` は `.claude/agents/` をスキャンして `agent-policy-role` マーカーを持つ定義だけを拾い、**役割 → 定義名（＋ vendor）** の対応行を組み立てます。

```
次の Agent は役割マーカーを宣言している。担当表の該当する役割は、これらを優先して使う。
同じ役割に複数あるときは依頼内容に近いものを選ぶ。
- 複雑または重要な実装: gpt-sol-lead-implementer (gpt)
- コードベース探索実働: gpt-terra-explorer (gpt) / grok-researcher (grok)
```

`★ Insight ─────────────────────────────────────`
- 対応表が解いている問題は「**担当表の役割名**（`complex-impl` など、共通規律が定義する抽象的な役割）を、**このプロジェクトに実在する定義名**へ落とす」という一段の変換。担当表だけでは役割は分かっても委譲先の名前が分からず、`.claude/agents/` を見ただけでは定義はあっても「どれがどの役割か」が分からない。
- だから対応表は**静的に書けない**。プロジェクトごとに定義名が違い、実行時のスキャンでしか作れない。これが「フックごと廃止（案B）」を退けた理由でもあります。
- 「同じ役割に複数あるときは依頼内容に近いものを選ぶ」の一行が示すとおり、対応表は**一意な解決**ではなく**候補の提示**。最終判断は委譲元に残っています。
`─────────────────────────────────────────────────`

補足すると、対応表が届く相手は 2 種類あります。

- **メインセッションのオーケストレーター** — SessionStart が注入。custom 系のときだけ
- **再委譲しうるサブエージェント** — SubagentStart が注入。`tools` に `Agent` を含む定義と、未知の `agent_type` が対象

つまり「委譲元が迷わないように」の「委譲元」は、親だけでなく**再委譲する子・孫も含む**。二段フックにした理由がここにあります（`2026-08-27` 設計書 §5 の P0 が「role map の全階層伝播」）。

なお、`labelOf(role) === undefined` の定義は表から落ちます。未知の role-id を書いた定義は無視されるということです。

対応表の役割については前ターンで回答済み。あなたの理解のとおりです。

保留中の判断が 2 点あります。

1. **claude プロファイルで対応表を注入するか** — 注入するなら Claude enum 以外の `model` を持つ定義を除外して `two-profile` §6.4 の防御を保つ形でよいか
2. **`setup-agents` の claude 対応と委譲先解決の訂正を、`subagent-discipline` 廃止と分けるか、まとめるか**

`discipline-role-table.test.ts` が 8 件落ちている件も、指示があれば先に直します。

## セッション 8: 役割対応表の注入・委譲戦略・ゴール確定

# AI

2 点の判断待ちです。

1. **claude プロファイルで対応表を注入するか**（Claude enum 以外の `model` を持つ定義を除外して `two-profile` §6.4 の防御を保つ形でよいか）
2. **`setup-agents` の claude 対応と委譲先解決の訂正を、`subagent-discipline` 廃止と分けるか、まとめるか**

`discipline-role-table.test.ts` の 8 件失敗も、指示があれば先に直します。

# phyllis998

> 1. 注入します。Claudeモデル以外を指定しているものを除外します。
> 2. まとめます。
>
> setup-agentsは、最初にClaudeかCustomかを聞くようにしたいです。
> 対応表に列挙できなかった役割の委譲先は、定義されたエージェントを第一候補とし、合うものが無ければビルトインエージェントに委譲します。これはClaudeとCustom共通の規律です。

> 1. 注入します。Claudeモデル以外を指定しているものを除外します。
> 2. まとめます。
>
> setup-agentsは、最初にClaudeかCustomかを聞くようにしたいです。
> 対応表に列挙できなかった役割の委譲先は、定義されたエージェントを第一候補とし、合うものが無ければビルトインエージェントに委譲します。これはClaudeとCustom共通の規律です。
>
> また、このセッションのゴールを、設計書・実装計画書・/goalコマンドに渡す、Dynamic Workflowを使って行う実装プロンプトの出力に設定します。
> 実装計画書は、Haikuレビュー・独立レビューと合わせて、今回は規模が大きいためAstraによるレビューも実施してください。

# AI

方針が確定した。ゴールが「設計書 + 実装計画書 + Dynamic Workflow 実装プロンプト」に変わり、スコープも 4 件の統合になった。まず `/goal` の形式と過去の実装プロンプトの型を確認する。

`/goal` の実体はこのリポジトリにも `~/.claude/commands/` にも無い。過去の実装プロンプト（`docs/prompts/`）が「以下を /goal コマンドの入力として使う」の形式なので、それに倣う。

要件を確定させ、設計書エージェントを再開して全面改訂させる。

設計書エージェントを再開し、全面改訂を指示した。設計書の中身を熟知しているので、新規起草より早い。

## セッションのゴールと工程

```
[1] context-map 更新 + 設計書 全面改訂        ← 実行中
[2] 設計書レビュー: Haiku + 独立
[3] ユーザー承認
[4] 実装計画書
[5] 計画書レビュー: Haiku + 独立 + Astra      ← 規模が大きいため 3 本
[6] ユーザー承認
[7] Dynamic Workflow 実装プロンプトを docs/prompts/ へ出力
```

## 確定した要件 9 件

| | 内容 |
|---|---|
| R1 | `setup-agents` の冒頭に「Claude のみ / Custom」の選択を置く |
| R2 | claude でも対応表を注入。Claude モデル以外の `model` を持つ定義は除外 |
| R3 | 委譲先解決を claude / custom 共通に。対応表に無い役割は**定義されたエージェントが第一候補**、無ければビルトイン |
| R4 | 両 SKILL.md の「ビルトインへ委譲する」記述を R3 の形へ訂正 |
| R5 | `subagent-discipline.md` 削除、SubagentStart は対応表専用 |
| R6 | 規律配布を依頼文への転記へ一本化、条項 1・4 の主語整備 |
| R7 | 上書き記録を 3 設計書 × 5 箇所へ拡張 |
| R8 | `discipline-role-table.test.ts` の追随（現在 8 件失敗） |
| R9 | README・バージョン |

`★ Insight ─────────────────────────────────────`
- R2 と R3 が入ることで、独立レビューが「着手不可」とした最大の欠陥（claude プロファイルで規律も対応表も届かない）が構造的に解消される。対応表が全プロファイルへ届けば `markerTable()` 冒頭行が条項 1 を運び、定義が第一候補なら定義本文が条項 0/2/3/5 を運ぶ。**廃止の前提条件が後から整備される形**になった。
- 設計書に「プロファイル別の到達性モデル」を表で書かせている。旧設計書が着手不可になった原因は、custom の経路を 3 プロファイル全部に一般化したこと。表にすれば同じ誤りを繰り返せない。
`─────────────────────────────────────────────────`

レビューで採用した指摘 10 件と却下 1 件（コミットハッシュの実在は確認済み）も渡した。`/goal` の実体は環境になかったので、`docs/prompts/` の過去プロンプトと同じ形式で書く。「Dynamic Workflow」の具体的な指定（Workflow ツールを指すのか）は、プロンプト作成の段階で確認する。

設計書の全面改訂を待っている。完成したら Haiku レビューと独立レビューを並列で通す。

## セッション 9: 設計書全面改訂指示・要件統合(R1-R9)・セッションゴール確定

# AI

設計書の全面改訂を待っている。context-map の更新 → 設計書の順で進んでいる。

設計書が完成した（108 KB / 13 章）。レビュー 2 本を並列で起動する。

設計書が完成し、レビュー 2 本を並列で走らせている。

## 設計書の主な判断

**R1 — `setup-agents` の分岐**: ステップ 0（言語判定）の後に「ステップ 0b: 構成の選択」を新設。Claude のみならステップ 1（`--list-live-models`）とステップ 3 を飛ばし、候補を Claude enum 4 値に固定。**CLI へ新フラグを足さない** — `modelIsAvailable` / `resolveVendor` / `targetsFor` が Claude enum を照会なしで通すため。

**R2 — 除外規則**: 2 条件の論理積。`model` が未宣言 / `inherit` / Claude enum、**かつ** `vendor` が未宣言 / `claude` / `none`。`model` が主（沈黙故障の直接原因）、`vendor` が従（`inherit` + `gpt` の組み合わせを捕捉）。

**R6 — 転記の射程**: **全条項**を採用。「関わるもの」も「自身が受け取った」も判断を残すため却下。条項 0 のサブエージェント版も新設し、計 9 条項の連続ブロックになる。

## プロファイル別の到達性

| 状態 | 変わること |
|---|---|
| custom（成功）| 対応表は不変。規律が断片 → 依頼文転記へ。委譲先解決の文面が共通規律へ移動 |
| custom（フォールバック）| SessionStart は Claude 実行可能な定義だけの表に。SubagentStart は照会できず custom の表を配り続ける（既知の乖離は残存）|
| claude | **対応表が新たに届く**（本リポジトリで 6 行 / 約 400 B、5 役割）。GPT/Grok 定義は表に載らず沈黙故障の経路は塞がれたまま |
| none / 未設定 / 未知 | 変化なし。`NO_MARKERS` の固定文のみ |

`★ Insight ─────────────────────────────────────`
- `two-profile` §6.4 の沈黙故障対策を、**「表を出さない」から「表から外す」へ塞ぎ方を変える**という設計。防御の目的を保ったまま、claude プロファイルでも役割解決ができるようになる。決定を覆すのではなく、同じ目的をより細かい粒度で達成する形。
- 前回の指摘 (c)「削除は 4 ブロック」が、R2 の導入で **6 ケース / 5 ブロック**に増えた。`claude` 分岐の期待値が反転するテストデータが 1 件増えたため。要件が変われば前提の実測値も変わる、という当然のことだが、数字だけ引き継ぐと事故になる箇所。
`─────────────────────────────────────────────────`

未解決事項が 10 件。うち判断が要りそうなのは、`inherit` を Claude 実行可能に含める帰結（2 プロファイルで意味が異なる）、除外規則を `model` だけにするか `vendor` も見るか、規律 L58 / L59 の重複統合（転記対象が 9 → 8 条項に減る）あたり。

レビュー 2 本が返り次第、指摘の採否を判断して提示する。

Haiku レビューが返った。判定は「実装に着手可能な水準」。前回の「着手不可」から改善している。

## 指摘（high 2 件）

| # | 内容 |
|---|---|
| 1 | §7.4 で規律 L60 を新設 2 へ置き換えるが、**L60 を削除する指示が明記されていない**。放置すると主語の違う条項が並置される |
| 2 | `setup-agents` の現況記述が二重に読める。「実装上は両構成に対応できる」のか「用途が custom 限定」なのか。本改修で `description` をどう直すか明示が要る |

medium 4 件は、`custom(フォールバック)` の親子乖離を「bug ではなく既知の受容」と注記すること、§5 の変更前/変更後の見出しを時系列で分けること、`setup-agents` の分岐表に「既存定義の確認」欄が無いこと、除外規則の根拠の主従関係。

前回 high だった「転記の射程が不明」は、全 9 条項を転記する方式の採用で解消されている。

独立レビューの結果を待って、両者を突き合わせてから採否を決める。

独立レビューが要件レベルの矛盾を見つけた。最重要の 1 件を先に検証する。

独立レビューの 1-1 を検証した。指摘のとおり。

```ts
// テスト L81-84
if (!separatorCells.every((cell) => cell === "---"))
    throw new Error("役割の表区切り行が不正")
```
```
// working tree の区切り行
| -------------------------------- | -------------------- | ... |
```

見出しを `## 担当表` に直しても、次は区切り行で落ちる。**フェーズ 0 は 2 箇所直さないと緑にならない**。

## 最重要 — R2 と R3 が論理的に打ち消し合っている

これが独立レビューの中核指摘で、要件レベルの矛盾。

```
R2: claude では Claude 以外の定義を対応表から除外する（沈黙故障を塞ぐため）
        ↓
R3: 対応表に載らなかった役割は、プロジェクトの定義を第一候補にする
        ↓
結果: 除外した gpt-luna / gpt-sol / grok-* が、順 2 で第一候補として拾い直される
```

このリポジトリで `AMATSUKA_AGENT_AUTO_INJECTION=claude` にすると、こうなる。

- 順 1（表）に載るのは 5 役割だけ — `code-review` / `advisor` / `doc-review` / `design-plan` / `explore-lead`
- 「通常の実装」は表に無い → 順 2 で `gpt-luna` の `description`（「通常のコーディング…」）が合致 → **`model: claude-gpt-5-6-luna` で dispatch**
- プロキシが無ければ沈黙故障

`★ Insight ─────────────────────────────────────`
- 設計書 §1.1 は「§6.4 の懸念は維持し、**塞ぎ方を変える**（表を出さない → 表から外す）」と書いた。しかし表から外しても、外した定義がディスク上に残っている限り、順 2 が拾う。**塞ぎ方を変えたのではなく、迂回路を開いた**ことになる。
- 設計者が部分的に気づいていた証拠がある — 独立レビュー役割にだけ「対応表に無いときは省略し、他の役割で代行しない」という例外が置かれている。その例外は 1 役割ぶんの穴しか塞いでいない。
- 順 1 と順 2 で候補集合の絞り込みが違う、というのが根本。順 1 はフックが `claudeRunnable` で絞るが、順 2 は規律の文面で「プロジェクトの Agent 定義」としか書いていない。
`─────────────────────────────────────────────────`

**推奨する解決**: 順 2 の候補にも同じ絞り込みを課す。規律の文面をこう変える。

> 順 2 の候補は、現在のセッションで実行できる定義に限る。claude プロファイル、および custom がフォールバックしたセッションでは、`model` に Claude 以外を指定した定義を候補にしない。

オーケストレーターは定義の frontmatter を読めるので判定できる。R2 の除外規則が表の生成だけでなく委譲先解決全体に効く形になる。

## その他の指摘（採用）

| # | 内容 |
|---|---|
| 1-1 | フェーズ 0 は区切り行パーサも直さないと緑にならない（**検証済み**）|
| 1-4 | 「none は何も変わらない」は事実誤認。現状 none でも断片 5 条項が配られており、廃止後は消える。none は方針スキルも注入されないので、規律の到達経路が `_common.md` だけになる |
| 1-5 | 共通規律 L64 / L136 が「方針の『実行役割の解決順』」を指す。SKILL の節を消すとダングリング参照になる |
| 2-3 | `setup-agents` の「Claude のみ」がステップ 1b / 6b / `--yes` を突き抜ける。6b は `RECOMMENDED`（GPT 含む）から選ばせ、1b の被覆判定はベンダーを見ない。R1 が言う「以後の分岐を出し分ける」がステップ 4 までで終わる |
| 1-3 | 転記条項数が §6.8.1 で 8、他で 9。実数は 9 |
| 1-10 | README 変更後文が「SubagentStart も同じ規則」と断言するが、フォールバック時は SessionStart だけが絞る |
| 4-6 | 手書き Claude 定義（`model: claude-sonnet-4-6` のような実 ID）は enum に無く除外される。沈黙故障は起きないが順 1 に載らない |

Haiku 側の high 2 件（L60 の削除指示が無い、`setup-agents` の現況記述が二重に読める）も採用。

## 判断待ち

**1. R2 × R3 の矛盾をどう塞ぐか。** 推奨は上記のとおり「順 2 の候補も現プロファイルで実行可能なものに限る」。他に「claude では順 2 を使わず順 1 → 順 3」「順 2 をマーカー無し定義に限定」という案もある

**2. `setup-agents` の「Claude のみ」を、ステップ 1b / 6b / `--yes` まで貫徹させるか。** 貫徹させるなら CLI フラグ追加を含む改修になり、R1 のスコープが広がる

この 2 点が決まれば設計書を改訂させ、実装計画書へ進む。

# phyllis998

> 1. 対応表に乗らなかった役割を選定するときもClaudeモデル以外を選定対象から除外する
> 2. 貫徹させる
>
> それと、R2,R3について、"Claude以外を除外"というより、Customプロファイルで"Claude以外も対象に含む"という考え方にしたいです。

# AI

方針が確定した。特に 3 点目の概念反転は、設計の書き方そのものを変える。

`★ Insight ─────────────────────────────────────`
- 「Claude 以外を除外する」（減算）と「custom では Claude 以外も含む」（加算）は、同じ集合を作るが**既定値が逆**。減算は「全定義が基本、claude では例外的に削る」、加算は「Claude 定義が基本、custom で外部を足す」。
- 加算の方がプロファイルの意味と一致する — claude プロファイルとは「Claude モデルだけで構成する」ことであって、「全部使うが危険なものを削る」ことではない。実装でも `claudeRunnable()` で弾く形から、プロファイルが候補集合を決める形になり、判定の置き場所が 1 つに集まる。
- そして候補集合が 1 つに決まれば、対応表の生成にも順 2 の選定にも同じものを使える。R2 と R3 の矛盾は、集合を 2 箇所で別々に決めていたから起きた。
`─────────────────────────────────────────────────`

設計書の改訂を指示する。

設計書の改訂を指示した。第 3 版になる。

## 指示の骨子

**A-1 概念の反転** — 「除外」の語彙を捨て、候補集合をプロファイルが決める形へ。実装シンボル名も加算側で命名し直させる。

**A-2 候補集合の共有** — 順 1（対応表の生成）と順 2（表に載らなかった役割の選定）に同じ集合を適用。custom フォールバック時も同様。これで独立レビューが見つけた迂回路が構造的に塞がる。

**A-3 `setup-agents` の貫徹** — 「CLI フラグを足さない」判断を撤回。突き抜け経路 4 つ（ステップ 1b の被覆判定、6b の候補、`--yes`、`parseArgs` のエラー文）と `POLICIES` の「Claude のみ(レガシー)」ラベルまで塞がせる。

**B-1〜B-10** — フェーズ 0 の区切り行パーサ（検証済み）、none の到達性の訂正、ダングリング参照 L64 / L136、規律 L60 の置き換え明記、条項数 8→9、既存テスト 4 件との衝突など。

**却下 2 件** — 未コミット差分の件（無関係な dirty まで数えた指摘）と `setup-agents` の 30KB 規則（主語が違う）。

改訂が返ったら、レビュー体制を判断する。第 2 版で独立レビューが要件レベルの欠陥を見つけているので、第 3 版も両レビューを通すのが妥当だと考えている。そのうえで承認をもらい、実装計画書（Haiku + 独立 + Astra の 3 本立て）へ進む。

設計書の第 3 版を待っている。完成したらレビュー 2 本を通し、承認後に実装計画書へ進む。

## セッション 10: agent-policy プロファイル統一設計の確定

# AI

設計書の第 3 版を待っている。

設計書の第 3 版が完成した（128 KB / 14 章）。レビュー 2 本を並列で起動する。

`★ Insight ─────────────────────────────────────`
- A-2 の解決が巧い。候補範囲をサブエージェントへ届ける手段として、**`markerTable()` の表の 2 行目に候補範囲の 1 行を出す**方式を採った。サブエージェントは方針スキルを読まないので、候補範囲を知る自動経路が他に無い。切り詰めが先頭 2 行を必ず残す性質を利用している。
- ただしその性質を使うと、切り詰め時に**役割行が 1 行も残らない**ケースが出る。従来の「先頭 2 行」は「冒頭の説明行 + 最初の役割行」だった。ここはレビューに投げる。
`─────────────────────────────────────────────────`

設計書の第 3 版が完成し、レビュー 2 本を走らせている。コンテキストは 32%（319.5k / 1M）で、実装計画書まで進む余裕はある。

## 改訂で入った設計

**A-1 加算モデル** — `CandidateScope = "claude-only" | "with-external"` を `policies.ts` に置き、`runsOnClaude()` / `candidateScopeFor()` / `candidateAgents(marked, scope)` で構成。規律の文面も「候補は〜である。custom 構成では、これに加えて〜も候補に含める」の加算形。第 1 版の `claudeRunnable` は「減算を含意するため使わない」と訂正表に記録。

**A-2 候補範囲の共有** — 共通規律を「候補」と「解決順」の 2 小節に分け、候補の定義を解決順の**前**に置いた。順 2 を「対応表に無く、**候補のうちに**担当範囲が合うものがある」と書き、「候補の範囲は、対応表にある役割にも無い役割にも同じように当てる」を明文化。custom フォールバック時は SessionStart も `claude-only` を使う。

**A-3 `--scope <claude|custom>`** — 既定は injection 値から導き、解決不能なら `claude`。塞いだ経路 7 つ。6b の推奨の出所を `RECOMMENDED` から `ASSIGNMENTS["claude-model-policy"]` へ切り替え（各役割 1 件になるため質問が 1 つ減る）、`--yes` のモデル一覧を 9 件から 4 件へ出し分け、拒否メッセージも `use --scope claude|custom to choose the candidate scope` へ。`POLICIES` のラベルから「(レガシー)」を削除。

## 実測で判明した差分

- **フェーズ 0 は 3 箇所**。見出し正規表現、区切り行を `/^-+$/` へ緩める、`FORBIDDEN_ROLE_TABLE_HEADINGS` への追加
- **転記 9 条項 = 約 1,885 B / 約 630 token**（既存 5 が実測 985 B）
- `session-start.test.ts` で表の不在を固定するのは 4 件ではなく **9 件**。ただし 9 件とも変更後も無変更で通る（fixture が候補外の定義しか置かないか、役割 ID が解決できないため）。必要なのは L213 の改題だけ

レビューが返り次第、指摘を精査して提示する。承認が出たら実装計画書（Haiku + 独立 + Astra の 3 本立て）へ進む。

Haiku レビューが返った。指摘は low 2 件のみ（表見出しの再利用、「7 経路」という数え方が実際は 9 項目）。前版の high 2 件は解消されている。

私が投げた切り詰めの論点には、こう答えが返ってきた。

> `truncateTable()` は `lines.length > 2` まで削り続けるため、最低限度は [説明行, scope 行] の 2 行

つまり**役割行が 0 行になるケースは実在する**。Haiku はこれを仕様どおりと読んでいるが、従来の「先頭 2 行」は「説明行 + 最初の役割行」で、少なくとも 1 役割は残っていた。そこが変わる。

`★ Insight ─────────────────────────────────────`
- 変化そのものは設計として筋が通る。役割行が全部落ちても scope 行が残れば、サブエージェントは「候補は Claude 定義だけ」と知った上で順 2 へ進める。**候補範囲を知らずに順 1 の残骸を見る**より、範囲を知って順 2 で選ぶ方が安全。
- ただし「巨大な対応表では順 1 が空になる」ことを設計書が明示しているかは別問題。切り詰めの意味が変わったのに、既存の切り詰めテストが従来の期待のままなら見落としになる。独立レビューがコードで裏を取るはずなので、その結果と突き合わせる。
`─────────────────────────────────────────────────`

独立レビューの結果を待って、両者を突き合わせてから提示する。

独立レビューが 5 件の「着手前に閉じるべき」項目を出した。採否を判断する。

`★ Insight ─────────────────────────────────────`
- **F-4 が明確なバグ**。`--scope` の既定を `policyForInjection` から導く設計だが、この関数は trim しかせず旧 3 値（`with-codex` 等）を `undefined` にする。一方フック側の `isCustomInjection` は lowercase + 旧 3 値を custom 扱い。**同じ env で CLI とフックが違う結論を出す**。`CuStOm` や `with-codex` のとき CLI は `claude-only`、フックは `with-external`。
- **P-1 が A-2 の趣旨と衝突している**。custom がフォールバックしたセッションで、親（SessionStart）は `claude-only` に絞るのに、子（SubagentStart）は injection 値しか見ないので `with-external` のまま。子の表に GPT/Grok が載る。設計書の §0 は「構造的に解消」と書き、リスク 4 は「直すな」と書いていて、同じ文書内で両立していない。
`─────────────────────────────────────────────────`

## 採用した指摘（5 件が着手前提）

| # | 内容 | 判断 |
|---|---|---|
| F-4 | `--scope` 既定の導出関数がフックと不一致 | 採用。`candidateScopeFor` 側へ揃える |
| P-1 | フォールバック時に子が `with-external` を持ち続ける | 採用。**解決案の比較が要る** |
| F-2/F-3 | `markerTable` 第 3 引数で既存テスト 6 箇所が壊れる。「アサーションは変えない」は撤回が要る | 採用 |
| SCOPE_LINES | 表 2 行目の文面が enum 4 値だけで、`inherit`・未宣言・vendor 条件を含まない。機械判定と食い違う | 採用 |
| F-14/O-2 | `--model-id gpt-sol --scope claude` が通る。6b の bash フェンスに `--scope` を渡す指示が無い | 採用 |

P-1 だけは解決に選択肢がある。子はプロキシ照会できないので、フォールバックの事実を知る手段が無い。`two-profile` 設計は「SubagentStart から `/v1/models` を照会する案は spawn ごとの HTTP 往復を要するため採らない」と却下済み。ただし**フォールバックした事実を記録して読む**なら HTTP は不要。設計書に案を比較させ、推奨を出させる。

## 検証で判明したこと（切り詰めの論点）

独立レビューがコードで確認した結果、112 定義フィクスチャでは役割行は 0 にならない（役割行 1 本 ≈ 1.1k 文字なので 7 本程度残る）。役割行 0 になるのは 1 行が 9,300 文字を超える場合だけで、現実的には起きにくい。

ただし既存テスト L446-458 が `fullLines.slice(1)` を役割行とみなしており、`- ` で始まらない scope 行が入ると**必ず失敗する**。テスト書き換えは必須。

改訂を指示する。

第 4 版の改訂を指示した。着手前に閉じる 5 件と、併せて反映する 13 件。

## 判断が残っているのは A-2 の 1 件だけ

custom フォールバック時に子が `with-external` を持ち続ける問題。3 案を比較させている。

| 案 | 内容 |
|---|---|
| 1 | フォールバックの事実を記録し、SubagentStart が読む。`two-profile` が却下した「spawn ごとの HTTP 往復」は不要 — 記録を読むだけなので |
| 2 | 受容。§0 の「構造的に解消」を「custom 成功と claude では解消、フォールバック時は子に残る」へ正確に書き直す |
| 3 | 設計書が提案する別案 |

案 1 は状態ファイルの寿命・並行セッション・後片付けの設計が要る。案 2 は正直だが、A-2 要件（候補集合を等しく適用）の趣旨からは後退する。推奨と軸の比較を出させたうえで、あなたの判断を仰ぐ。

## 却下したもの

- `scripts/*.mjs` にも `markerTable` がある → ビルド成果物なので手で編集しない。設計書に書く必要なし
- `hooks.json` の description の扱い → 判断は維持。ただし出典が無いことをリスクに 1 行残させる
- `grok-researcher.md` の残存 → スコープ外のまま

改訂が返ったら、A-2 の推奨をあなたに提示して承認をもらい、実装計画書（Haiku + 独立 + Astra）へ進む。

設計書の第 4 版が完成した（152 KB）。A-2 の結論を検証してから提示する。案 1 を退けた決定打が「`SubagentStart` の入力にセッション識別子があるか明示なし」なので、そこを確認する。

どのフックも `session_id` を読んでいない。設計書の「明示なし」は正確で、確証のない前提の上に状態管理を積まない判断は妥当。

## 設計書が確定した

`harness-docs/design/2026-09-09-agent-policy-profile-unification-design.md`（152 KB）

**A-2 の結論** — 案 3（規律の条項で埋める）を採用。案 1（フォールバックの事実を記録して `SubagentStart` が読む）を退けた理由が説得的だった。

> 同じプロジェクトで custom 成功セッションとフォールバックセッションが並走すると、成功側の子まで `claude-only` に倒れて外部委譲が全停止し、**塞ごうとした沈黙故障より被害が広くなる**

セッション単位に鍵を切れば回避できるが、その前提（`SubagentStart` にセッション識別子が来るか）が確認できない。加えて `session-start.ts` の「ファイルは書かない」方針を破る。案 3 は共通規律に 1 条項足すだけでコード変更ゼロ。

**その他の結論**

- A-1: CLI 既定は `candidateScopeFor` を使う（`policyForInjection` は使わない）。フックと同じ関数なので定義上ずれない
- A-3: 「アサーションは変えない」を撤回。`markerTable` の呼び出しは本番 3 + テスト 6 の 9 箇所。書き換えが要る 2 件を個別に列挙
- A-4: `SCOPE_LINES` の文面を候補条件と揃える。コストは約 120 B / spawn
- A-5: `--model-id` 経路にもフィルタ追加。SKILL の bash フェンス 12 本の経路表を作成（要 8 / 不要 4）

## 承認をお願いしたい点が 1 つある

設計書が自己申告している弱点。

> **§6.13 の案 3 は遵守依存であり、機械的保証を持たない。** 転記漏れ時の縮退先が現行挙動と同じで悪化しないこと、案 1 が並行セッションでより広い被害を生むことを根拠に選んだが、これは「保証の強さを下げる」判断である。

`★ Insight ─────────────────────────────────────`
- この改修全体が、**機械的保証（フック）を規律（LLM の遵守）へ置き換える**方向を持っている。断片廃止も、A-2 の案 3 も同じ性質。個々の判断は妥当でも、積み上がると「フックが守っていたものが規律に移った」状態になる。
- 引き換えに得るのは、配布経路の一本化と、claude 構成でも役割解決ができるようになること。トレードオフとして受け入れるかどうかが、承認の実質的な中身。
`─────────────────────────────────────────────────`

承認いただければ、次は実装計画書へ進む。レビューは Haiku + 独立 + Astra の 3 本立て。その後に `/goal` へ渡す Dynamic Workflow 実装プロンプトを出力する。

なお設計書の全文確認が必要なら、章を指定してもらえれば要約する。§0（第 2 版で変わったこと）と §6.13（A-2 の 3 案比較）が判断の中心。

# phyllis998

> 承認します

# AI

承認を受けた。設計書が要件 R1〜R9 をすべて満たし、着手を妨げる判断が残っていないことを確認した — Approve する。

実装計画書の作成を委譲する。

実装計画書の作成に入った。

## 進捗

```
[1] context-map 更新 + 設計書                    ✓ 完了（第4版・承認済み）
[2] 設計書レビュー: Haiku + 独立                  ✓ 完了（2巡）
[3] ユーザー承認                                  ✓ 完了
[4] 実装計画書                                    ← 実行中
[5] 計画書レビュー: Haiku + 独立 + Astra
[6] ユーザー承認
[7] Dynamic Workflow 実装プロンプト → docs/prompts/
```

## 計画書に求めた特殊要件

実装が Dynamic Workflow で走るので、通常の WBS より制約を足した。

- **タスクの独立性が判定できること** — どれが同じファイルを触り、どれが前の出力に依存するか。並列に投げられる組を明記
- **入力と出力が閉じていること** — 実装担当は設計書全文を読まない前提。タスクごとに転記すべき節番号を書かせる
- **検証が機械的に判定できること** — 「テストが通る」ではなく、どのテストファイルの何が緑になれば完了か

あわせて、フェーズ 0（既存 8 failed の解消）を最優先に置くこと、`markerTable` の第 3 引数化で**一時的に赤になる区間**とその解消タイミングを明示すること、`.serena/memories/` はサブエージェントに委譲できない（Serena のツールを持たない）のでオーケストレーター自身のタスクにすることを指示した。

未コミット差分を revert してはならない旨も計画書に明記させている。

完成したらレビュー 3 本を並列で通す。

実装計画書の完成を待っている。

## セッション 11: 実装計画書の3者レビュー・改訂・承認と実装プロンプト作成

# AI

実装計画書の完成を待っている。完成したらレビュー 3 本（Haiku + 独立 + Astra）を並列で通す。

実装計画書が完成した（52 KB / 9 章 / 20 タスク）。レビュー 3 本を並列で起動する。

実装計画書ができ、レビュー 3 本を並列で走らせている。

## 計画の構成

**20 タスク / 9 フェーズ**

| | 内容 | 完了条件 |
|---|---|---|
| F0 | 既存 8 failed の解消 | `discipline-role-table.test.ts` が 13 passed |
| F1 | 候補集合の正本（`policies.ts`）| `policies.test.ts` 全緑 |
| F2 | `markerTable` 第 3 引数化と 9 箇所の追随 | 振る舞いは変えない |
| F3 | 3 フックのプロファイル対応 | 断片が不在 |
| F4 | `setup-agents` の `--scope` | `setup-agents.test.ts` 全緑 |
| F5 | 指示書と付随文書 | 規律の条項数 9、`実行役割の解決順` が 0 件 |
| F6 | `description` | 「custom プロファイル専用」が 0 件 |
| F7 | バージョンと build | 両 json が `0.18.0-dev` |
| F8 | 統合検証・実機・保護パス更新 | Done 条件 31 項目 |

役割の内訳は、複雑または重要な実装 6 / 通常の実装 6 / 軽量な実装 2 / コードレビュー 1 / 探索実働 1 / オーケストレーター 4。

`★ Insight ─────────────────────────────────────`
- **フェーズ境界をすべて緑にした**のが計画の設計判断。設計書 §9.1 は「フェーズ 2〜4 は一時的に赤 → 緑」としていたが、Dynamic Workflow はフェーズ単位で検証するので、赤をタスク内部に閉じ込める粒度へ調整している。設計判断の変更ではなく、実行形態に合わせた分割。
- T3 で `markerTable` の署名変更と 9 箇所の追随を **1 タスクに閉じた**のもそのため。分割すると T6 まで赤が続く。
- 「緑だが意味を失う」区間を 1 件明示しているのが良い。T3 以降、`subagent-start.test.ts` L465-486 の `firstRoleLine` が scope 行を指すので、テストは通るが検証の意味を失う。T5 が削除するまでこの緑を根拠にするな、と注記されている。**通っているテストが嘘をつく区間**を先回りして記録した形。
`─────────────────────────────────────────────────`

設計書との食い違いは 1 件のみ。`setup-agents.test.ts` L274 の拒否テストは `toContain(flag)` と `toMatch(/removed|廃止/i)` しか見ないため、新メッセージでも通り、差し替えが不要と判明した。

レビュー 3 本が返り次第、指摘を精査して提示する。

Haiku レビューが返った。矛盾は medium 2 件 / low 2 件で軽微だが、**7 タスクで「依頼文だけでは情報が足りない」**という指摘が出た。Dynamic Workflow は実装担当が設計書全文を読まない前提なので、これは実質的な問題。

| タスク | 足りないもの |
|---|---|
| T5 | **`import path` を削除対象から外す**理由と位置（`bundledAgentsDir()` L275 が使う）。「想定される失敗 7」にはあるが T5 本体の記述に無い |
| T2 | 転記対象の §5 が 5.1〜5.7 と広い。T2 に要るのは 5.1 と 5.2-5.3 だけ |
| T8 | L60 を削除する理由（新設 2 への置き換え）が計画書に無い |
| T11 | L277 / L295 を特に見落とすなという根拠。frontmatter を触らない制約が T11 本文に無い |
| T12 | README の行番号は列挙されているが「何を何へ変えるか」が無い |
| T15 | grep コマンドの正確な形（`scripts/` の除外）|
| T17 | コードレビューの観点（命名・fail-open・deny-list）の検証基準 |

`★ Insight ─────────────────────────────────────`
- 「想定される失敗と対策」の節に書いた注意が、**タスク本体の記述には反映されていない**というパターンが複数ある（T5 の `import path` が典型）。人間が計画書を通読するなら拾えるが、依頼文だけを受け取るサブエージェントには届かない。Dynamic Workflow 特有の落とし穴。
- 対策は 2 つ。計画書のタスク記述側へ注意を引き上げるか、依頼文の生成規則に「該当タスクの『想定される失敗』を必ず転記する」と書くか。後者の方が計画書の重複を避けられる。
`─────────────────────────────────────────────────`

独立レビューと Astra のゲートレビューを待って、3 本を突き合わせてから改訂を指示する。

Astra のゲートレビューが返った。**判定は「着手すべきでない」**。8 件の必須項目。設計判断は覆さず、計画の訂正で解消できる内容。

`★ Insight ─────────────────────────────────────`
- **#4 が最も鋭い**。T12 / T15 の検証条件が「`subagent-discipline` の文字列が 0 件」なのに、設計指定の 0.18 移行説明にはその文字列が**入っている**。正しく実装すると検証が落ちる。しかも担当が緑に戻そうとして必要な説明を削る誘因になる。実行時参照と履歴・移行説明を区別していないのが原因。
- **#2 も設計書内の機械的な不一致**。新設テストが期待する `外部ベンダーのモデルを指定した定義は委譲先にしない` が、正本の `SCOPE_LINES` の実際の文面（`それ以外の定義は委譲先にしない。`）に含まれていない。設計どおり転記して実装すると必ず失敗する。
`─────────────────────────────────────────────────`

## 必須 8 件

| # | 内容 |
|---|---|
| 1 | T4 の「既存テストは改題だけ」が未知役割通知と矛盾。`hidden` 定義が新候補条件を満たすため、定義名と通知の両方が出る |
| 2 | 新設テストの期待文字列が正本の出力に含まれない（設計書内の不一致）|
| 3 | scope 導入で `setup-agents.test.ts` L142-159 / L224-238 / L286-340 と `delegation-gate.test.ts` のヘルパーが影響を受ける。棚卸しが要る |
| 4 | T12 / T15 のゼロ件条件が、必須の移行説明を禁止している |
| 5 | 検証順序が未確定。T19（最終確認 31 項目）の**後**に T20 が置かれ、T16 の build 中に T17 が差分を読む経路も残る |
| 6 | T11 の転記対象が「変更 2〜7」だが、ステップ 7 の改修は**変更 8**。T7 の `§7.12` は参照先が存在しない |
| 7 | bash フェンスは 12 本ではなく **17 本**。計画の表自体も「要 9 行 / 不要 3 行」で集計と合わない |
| 8 | ルートの build は**全 workspace** が対象だが、差分確認が対象プラグインに限定。他プラグインの生成物変化が検出されない |

着手後で対応可能なものが 3 件（#9 Serena memory と自動メモリの取り違え、#10 Done #15 の確認手段、#11 T18 の実機検証の証拠指定）。

Astra はまた、私が計画書に伝え損ねていた未コミット差分を 1 件見つけている — `orchestration-discipline.md:35` の「役割へ委譲してよい」→「サブエージェントへ委譲してよい」。これも revert してはいけない差分。

独立レビュー（grok）の結果を待って、3 本を突き合わせてから改訂を指示する。

3 本のレビューが揃った。**Astra と grok が独立に同じ欠陥を 6 件指摘**しており、信頼度が高い。

`★ Insight ─────────────────────────────────────`
- grok が**設計書側に残った矛盾**を見つけた。§6.9 は「`policyForInjection` を使わず `candidateScopeFor` を使う」と決めたのに、§7.15 の実装コードは `policyForInjection(...) === "custom-policy" ? ...` のまま。第 4 版で判断は直したが、コード例が追随していない。T7 の依頼文が両方を転記すると、担当は同一タスク内で矛盾する指示を受ける。
- もう 1 つ、Astra が §9.2 の新設テストの期待文字列 `外部ベンダーのモデルを指定した定義は委譲先にしない` が、正本の `SCOPE_LINES` の実際の出力（`それ以外の定義は委譲先にしない。`）に含まれないことを発見。**設計どおり実装すると必ず失敗する**。
- どちらも設計判断の変更ではなく、承認された決定に文書内の別箇所が追随していない不整合。設計書の修正が要る。
`─────────────────────────────────────────────────`

## 2 者一致（確実な欠陥）

| 内容 | Astra | grok |
|---|---|---|
| T4 の L213 が `unknownRoleBlock` 導入で落ちる。`hidden` と `未知の役割 ID` の両方 | #1 | 6/13 |
| T12/T15 のゼロ件条件が移行説明を禁止 | #4 | 15/16/24 |
| T7 の既存テスト追随が不足 | #3 | 14/23 |
| T7 の転記節 §7.12 が誤指定 | #6 | 2 |
| bash フェンスが 12 本ではなく 17 本 | #7 | 5 |
| T11 の転記から変更 8 が漏れ | #6 | 21 |

grok は #3 をより具体的に詰めていた — `inheritedTestEnv` が `AMATSUKA_AGENT_AUTO_INJECTION` を削除するため、新仕様では既定が**常に** `claude-only` になる。`--model-id gpt-*` を使う `it` が 24 件、`--models` が 7 件。全部が影響を受ける。

## grok の単独指摘（鋭い）

**T8 が `## 委譲先の解決` を担当表**見出し**の直後に置くと、`extractRoleBandSection` が次の `## ` までしか取らないため表を失い、T1 が直した 8 件が再失敗する。** 計画の「`## 担当表` の直後」が、節の直後か見出しの直後か曖昧。設計 §7.6 は「担当表**節**の直後」と正しく書いている。

## Astra の単独指摘

検証順序（T19 の後に T20 がある、T16 の build 中に T17 が差分を読む）、build が全 workspace 対象なのに差分確認が対象プラグイン限定、Serena memory と自動メモリの取り違え。加えて、私が計画へ伝え損ねた未コミット差分 `orchestration-discipline.md:35`（「役割へ委譲してよい」→「サブエージェントへ委譲してよい」）。

改訂を指示する。

改訂を指示した。設計書 2 箇所 + 計画書の必須 8 件 + 併せて直す 7 件。

## レビュー 3 本の役割分担が機能した

| レビュー | 見つけたもの |
|---|---|
| Haiku（理解の言語化）| 7 タスクで依頼文の情報不足。「想定される失敗」の注意がタスク本体に反映されていないパターン |
| grok（独立検証）| 設計書内に残った矛盾（§6.9 と §7.15）。`inheritedTestEnv` が既定 scope になる連鎖。T8 の挿入位置が T1 の成果を壊す経路 |
| Astra（最終ゲート）| 検証条件が成果物を否定する自己矛盾。build の書き込み範囲。検証順序と再検証規則 |

`★ Insight ─────────────────────────────────────`
- **3 者が同じ欠陥を別の角度から見つけた**のが T4 の L213。grok はコードを読んで「`markerTable` は未知 RoleId を表から落とすが、`unknownRoleBlock` は同じ RoleId を別ブロックで通知する」と機構を特定し、Astra は「改題だけでは通らない」と結論を出した。片方だけでは弱い指摘が、両方揃うと確定する。
- 一方 Haiku は 7 タスクの情報不足を拾ったが、これは他 2 者が見ていない領域。**Dynamic Workflow で実装担当が設計書を読まない**という前提を効かせた指摘で、コードを読んでも出てこない。
`─────────────────────────────────────────────────`

改訂が返ったら、修正が効いているかを確認したうえで、あなたに提示する。承認後に `/goal` へ渡す実装プロンプトを書く。

なお、設計書の修正 2 件は承認済みの決定に文書内の別箇所が追随していない不整合の訂正で、設計判断そのものは変えていない。もし「設計書に手を入れるなら再承認したい」ということであれば言ってほしい。

改訂が完了した。設計書 2 箇所と計画書 15 件、すべて反映されている。

## 申し送られた 2 点への判断

**実測値の訂正** — 私が「24 件」と伝えた `--model-id gpt-*` の既存ケースは、実測で **39 宣言 / 展開後 42 ケース**だった。`check()` に加えて `writeArgs()`（L1402-1414）も `--model-id gpt-sol` を既定で渡すため。私が独立レビューの数字をそのまま渡し、裏を取らなかった。計画には実測値が入っている。

**T7 を分割するか** — 分割しない。

`★ Insight ─────────────────────────────────────`
- T7 は「`--scope` を足す」と「既存 42 ケースに `--scope custom` を足す」の 2 つに見えるが、**分割できない**。フラグが存在しない状態で `--scope custom` を先に書くとエラーになり、CLI 実装を先にすると既存 42 ケースが赤になる。緑 → 緑で分ける切れ目が無い。
- フェーズ境界を全部緑に保つのが計画の骨格で、Dynamic Workflow はフェーズ単位で検証する。ここを崩すと他フェーズの判定も緩む。42 ケースの追随は `--scope custom` を足すだけの機械的作業で判断を伴わないので、1 タスクに入れても事故りにくい。
- 代償は T7 の実行時間。Workflow のタイムアウトに掛からないよう、実装プロンプトで注意を書く。
`─────────────────────────────────────────────────`

## 主な修正

- **B-1**: T4 の既存テストを期待値変更 + 改題（`not.toContain` → `toContain` へ反転）。新設は 8 → **10 ケース**
- **B-3**: T15 の検査を 10 項目の表に分離。実行時参照はゼロ件、README は**移行項目 5 の 1 行だけ**が残る形へ。`grep` の終了コードではなく `wc -l` の値で判定する規則も追加
- **B-6**: フェンス表を実測で作り直し。**17 フェンス / 18 コマンド、要 12 コマンド（11 フェンス）/ 不要 6**
- **B-7**: フェーズ 8 を `T16 → T15/T17 並列 → T18 → T20 → 再検証 → T19` へ。各検証結果に `HEAD` と差分ハッシュを添えて、修正前の成功報告を流用させない
- **C-2**: 「依頼文の生成規則」7 項目を新設。その 4 番目に「§6 のうち該当タスクの行を必ず転記する」

タスク総数 20、フェーズ 9（0〜8）は不変。

## 承認をお願いしたい

実装計画書が確定した。着手を妨げる判断は残っていない。

- 設計書: `harness-docs/design/2026-09-09-agent-policy-profile-unification-design.md`
- 実装計画書: `harness-docs/plans/2026-09-09-agent-policy-profile-unification-implementation.md`（72 KB / 20 タスク / 9 フェーズ）

承認いただければ、`/goal` へ渡す Dynamic Workflow 実装プロンプトを `docs/prompts/` に書いて、このセッションのゴールに到達する。

なお設計書は 2 箇所直している（§7.15 の既定導出を `candidateScopeFor` へ、§9.2 の期待文字列を正本に合わせて）。どちらも承認済みの決定に文書内の別箇所が追随していなかった不整合の訂正で、設計判断は変えていない。再承認が必要なら言ってほしい。

# phyllis998

> 承認します

# AI

承認を受けた。最後の成果物、`/goal` へ渡す実装プロンプトを作成する。

`★ Insight ─────────────────────────────────────`
- プロンプトの書き手には計画書を熟知した `system-planner` を使う。実装プロンプトは計画書の派生物なので、同じ担当が書く方が転記の忠実度が上がる。
- プロジェクト規約が「AI 向け指示書は `prompt-smith:prompt-smith` で作る」と定めているのでロードさせる。実測 10,763 B でコスト規律の 30KB 上限内。
`─────────────────────────────────────────────────`

実装プロンプトの作成に入った。これでセッションのゴールに到達する。

## セッションの成果物

```
[1] context-map                                   ✓ .claude/context-maps/2026-09-09-subagent-discipline-removal.md
[2] 設計書                                        ✓ 承認済み（レビュー2巡）
[3] 実装計画書                                    ✓ 承認済み（レビュー3本）
[4] Dynamic Workflow 実装プロンプト               ← 作成中
```

プロンプトには、過去 2 本の形式に倣わせつつ、この回に固有の 3 点を書かせている。

- **Workflow ツールを使い、`workflow-authoring` スキルを先にロードする**こと。フェーズは計画書の 0〜8 に対応し、境界はすべて緑
- **T7 を分割しない**理由。`--scope` が無い状態で `--scope custom` を先に足せず、CLI 実装を先に入れると 42 ケースが赤になる。緑 → 緑の切れ目が無い
- **実機検証（T18）が実行できないときは、ユニットテストで代替完了にせず未検証として報告する**こと

長さは 150 行までに制限した。計画書と設計書へ参照させ、内容を二重化させない。プロンプトの役割は「どの文書を正本とし、どう進め、何を守り、何をもって完了とするか」に絞る。

完成したら全文を提示する。

実装プロンプトの完成を待っている。これがセッション最後の成果物になる。

## セッション 12: 実装プロンプト完成、成果物 4 点確定

# AI

実装プロンプトの完成を待っている。

実装プロンプトが完成した。全文を確認する。

セッションのゴールに到達した。成果物 4 点が揃っている。

## 成果物

| | パス |
|---|---|
| context-map | `.claude/context-maps/2026-09-09-subagent-discipline-removal.md` |
| 設計書 | `harness-docs/design/2026-09-09-agent-policy-profile-unification-design.md` |
| 実装計画書 | `harness-docs/plans/2026-09-09-agent-policy-profile-unification-implementation.md` |
| 実装プロンプト | `docs/prompts/2026-09-09-agent-policy-profile-unification-implementation-prompt.md`（81 行）|

プロンプトは `---` の下をそのまま `/goal` に渡せる。

`★ Insight ─────────────────────────────────────`
- 最後の制約が効いている — 「設計書 §11 で不採用が確定した案に手を出さない。**custom フォールバック時の食い違いは既知の受容、`none` で規律が届かなくなるのは意図的な縮退**」。この 2 つは実装中に「バグだ」と判断されて勝手に直される典型で、直されると設計判断が崩れる。レビューが「実装者はこの差を直そうとしないこと」と警告した箇所が、そのままプロンプトの防波堤になった。
- 「`prompt-smith:prompt-smith` の評価工程は回さない」も同種。設計書が決めた文面が書き換わってしまう。ツールを使えと指示すると、そのツールの標準手順まで走る。
`─────────────────────────────────────────────────`

## この調査が辿った経路

最初の問いは「`subagent-discipline` を廃止できるか」だった。答えは「できる」だが、そこに至るまでに 3 段の連鎖が出た。

1. 廃止すると claude / none プロファイルで規律の自動経路が消える（独立レビューが発見、着手不可判定）
2. 原因を辿ると、両 SKILL の委譲先解決の記述が誤っていた（ビルトインが第一候補になっていた）
3. 直すには `setup-agents` が custom 専用である前提を崩す必要があった

結果として、廃止の前提条件を先に整備する形の統合改修になった。当初の 1 ファイル削除が、20 タスク / 9 フェーズになっている。

レビューは設計に 2 巡、計画に 3 本。独立レビューが 2 回とも要件レベルの欠陥を見つけ、Astra が「検証条件が成果物を否定する」自己矛盾を出した。どちらも設計書だけを読んでいたら通っていた種類の指摘だった。

## 残っていること

未解決事項 13 件は設計書 §12 にあり、いずれも本改修の実装に影響しない。`grok-researcher.md` の残存、`en/_common.md` の言語経路、規律 L58/L59 の重複あたりが将来の候補。

`discipline-role-table.test.ts` の 8 件失敗は現時点でも赤のままだが、実装セッションのフェーズ 0 で最初に解消される。今このセッションで直すこともできる。必要なら言ってほしい。

# phyllis998

> goalコマンドに渡せる文字数は4000文字以下という制約があります。プロンプトを4000文字以下にしてください

# AI

現在のサイズを測る。

4,954 文字。**954 文字（約 20%）削る**必要がある。

`★ Insight ─────────────────────────────────────`
- 削る候補ははっきりしている。「参考（旧決定）」3 本のパス列挙は設計書 §1 が上書き内容を記録しているので不要。Done 条件の 8 項目も計画書 §7 の 31 項目を参照させれば足りる。理由の説明文（「〜のため」）も、指示だけ残せば削れる。
- 逆に**絶対に削れない**のは、実装者が知らないと事故る情報 — フェーズ境界が緑であること、依頼文の生成規則、8 件失敗が 2 段であること、T7 を分割しないこと、未コミット差分の保護、そして「不採用案をバグと判断して直すな」の 2 件。文字数の圧力でここを削ると、レビュー 5 本ぶんの防波堤が消える。
`─────────────────────────────────────────────────`

圧縮を指示した。目標 3,800 文字前後（余裕を持たせて）。

削る対象と残す対象を明示的に分けた。

**削る** — 旧決定 3 本のパス列挙（設計書 §1 が記録済み）、Done 条件の 8 項目（計画書 §7 を参照させる）、理由の説明文、重複。

**残す 18 項目** — 実装者が知らないと事故るもの。特に 2 件は理由ごと残させた。

- custom フォールバック時の候補範囲の食い違いは**既知の受容**
- `none` で規律が届かなくなるのは**意図的な縮退**

この 2 つは実装中に「バグを見つけた」と判断されて直される可能性が高い。直されると設計判断が崩れる。理由が無いと納得できず、結局直してしまう。

書き終えたら文字数を実測させ、18 項目の残存を自己照合させる。返ってきたら私も照合する。

プロンプトの圧縮を待っている。完成したら文字数と 18 項目の残存を私も照合する。
